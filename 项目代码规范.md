# Codex Agent Switch 项目代码规范

> 文档类型：Project Coding Standards  
> 项目暂定名称：Codex Agent Switch  
> 简称：CAS  
> 当前目标版本：V0.1  
> 文档职责：统一 CAS 项目的代码组织、命名、模块依赖、Rust / TypeScript 编码规范、错误处理、日志、异步编程、持久化访问、Tauri IPC、配置处理以及代码质量要求。

---

# 0. V0.1 实现基线

V0.1 使用 Responses-first、Provider-neutral、Windows-first 路线。`deepseek-v4-flash` 由只读 Preset / Model Definition 描述并复用 `GenericResponsesAdapter`；禁止为品牌创建无行为差异的 Core 类型或条件分支。Custom Responses Provider 必须走同一 Application、Domain、Config Compiler 与 IPC 链路。

Profile、Model Discovery、Runtime Probe、Import / Adopt 与 macOS / Linux 平台实现在 P1 前不得进入 V0.1 production module / command registration / migration。详细代码示例若涉及这些能力，视为未来规范。完整开发前必须先以可重复测试通过 ADR 0014 的四项 PoC Gate。

---

# 1. 文档目标

本规范用于保证 CAS 在多人协作和 AI Agent 参与开发时仍然保持：

```text
结构稳定
职责清晰
代码可读
依赖可控
错误可追踪
实现一致
安全边界明确
长期可维护
```

本规范重点解决：

```text
代码应该放在哪里？

一个模块可以依赖谁？

Rust 类型怎么命名？

React 组件怎么组织？

Tauri Command 应该写多少逻辑？

Repository 如何实现？

错误应该在哪里转换？

什么时候允许 unwrap？

什么时候使用 async？

日志应该记录什么？

配置文件处理代码放在哪里？

如何避免 Provider-specific if/else 扩散？

如何避免 GUI 与后端业务逻辑重复？
```

---

# 2. 技术栈基线

V0.1 推荐技术栈：

```text
Desktop
Tauri 2

Frontend
React
TypeScript
Vite

Backend / Core
Rust

Database
SQLite

Database Access
SQLx

TOML Editing
toml_edit

Serialization
serde
serde_json

Async Runtime
Tokio

HTTP
reqwest

CLI
clap

Logging / Tracing
tracing
tracing-subscriber
```

除非存在明确技术理由，不得在相同职责上重复引入多套框架。

例如禁止同时使用：

```text
reqwest
+
ureq
+
hyper client
```

分别实现不同 Provider。

---

# 3. 总体编码原则

整个项目遵循：

```text
明确优于聪明

简单优于抽象过度

组合优于继承式设计

显式优于隐式

领域语义优于技术语义

类型约束优于注释约束

错误返回优于 panic

小模块优于 God Object

稳定边界优于快速跨层调用
```

---

# 4. 代码首先表达职责

禁止出现：

```text
utils.rs
common.rs
helpers.rs
manager.rs
service.rs
misc.rs
```

这种不断承载不相关逻辑的万能模块。

文件和类型名称必须表达真实职责。

例如：

```text
provider_repository.rs

codex_config_reader.rs

agent_config_mapper.rs

secret_store.rs

configuration_compiler.rs
```

优于：

```text
utils.rs

manager.rs

common.rs
```

---

# 5. 项目目录基线

推荐：

```text
codex-agent-switch/
│
├── apps/
│   └── desktop/
│
├── crates/
│   ├── cas-core/
│   ├── cas-application/
│   ├── cas-storage/
│   ├── cas-codex/
│   ├── cas-provider/
│   ├── cas-secret/
│   ├── cas-platform/
│   └── cas-cli/
│
├── binaries/
│   └── cas-helper/
│
├── resources/
│   ├── provider-presets/
│   ├── agent-presets/
│   └── model-metadata/
│
├── migrations/
│
├── docs/
│
└── tests/
```

实际开发可以根据 Cargo Workspace 调整目录，但模块职责不得改变。

---

# 6. Cargo Workspace

Rust 项目使用：

```text
Cargo Workspace
```

统一管理：

```text
crate
dependency version
lint
build profile
```

根目录：

```toml
[workspace]
members = [
    "crates/*",
    "binaries/*",
    "apps/desktop/src-tauri"
]
```

不得让各 crate 随意使用不同版本的同一核心依赖。

---

# 7. Workspace Dependency

公共依赖优先定义于：

```toml
[workspace.dependencies]
```

成员 crate：

```toml
serde.workspace = true
tokio.workspace = true
thiserror.workspace = true
```

避免：

```text
cas-core serde 1.x
cas-storage serde 另一个版本
cas-provider 再单独声明
```

---

# 8. Crate 职责边界

## `cas-core`

允许：

```text
Domain Entity
Value Object
Domain Enum
Domain Validation
Domain Error
Repository Port / Domain Port
```

禁止：

```text
SQLx
reqwest
Tauri
toml_edit
OS API
filesystem implementation
```

---

# 9. `cas-application`

允许：

```text
Use Case
Application Service
Transaction Coordination
Port Coordination
Cross-domain orchestration
```

禁止直接：

```text
写 SQL
操作 TOML AST
调用 Windows Credential API
操作 React
```

---

# 10. `cas-storage`

负责：

```text
SQLite
SQLx
Repository Implementation
Persistence Mapping
Migration integration
```

禁止：

```text
Provider HTTP
Codex Config Patch
Agent Business Decision
```

---

# 11. `cas-codex`

负责所有：

```text
Codex-specific knowledge
```

例如：

```text
CODEX_HOME
config.toml
agents/*.toml
model_provider
model_catalog_json
Codex Version Capability
Configuration Compiler
Codex Config Reader
Codex Config Writer
Codex Config Mapper
```

Codex 配置字段不得散落到其他 crate。

---

# 12. `cas-provider`

负责：

```text
Provider Adapter
HTTP Client
Provider Connection
Model Discovery
Compatibility Probe
Responses Adapter
```

禁止：

```text
修改 Agent
修改 Profile
修改 Codex config.toml
```

---

# 13. `cas-secret`

负责：

```text
Secret abstraction
Credential lifecycle
Secret-sensitive type
Secret storage port
```

不得依赖：

```text
Agent
Profile
React
Codex Configuration Compiler
```

---

# 14. `cas-platform`

负责隔离：

```text
Windows
macOS
Linux
```

差异。

例如：

```text
Application Data Path
Executable Path
Secret Store Backend
Process APIs
Filesystem platform behavior
```

---

# 15. `cas-cli`

属于：

```text
Presentation Adapter
```

只能调用：

```text
cas-application
```

不得直接：

```text
打开 SQLite
修改 config.toml
访问 Secret Store
```

---

# 16. Desktop Backend

Tauri Backend 同样属于：

```text
Presentation Adapter
```

关系：

```text
React
  ↓
Tauri Command
  ↓
Application Service
```

Tauri Command 不承担业务实现。

---

# 17. 依赖方向

允许：

```text
Presentation
    ↓
Application
    ↓
Core
```

Infrastructure 实现 Application / Core 所定义的 Port。

禁止：

```text
cas-core
→ cas-storage

cas-core
→ cas-codex

cas-core
→ Tauri

cas-storage
→ React

cas-provider
→ cas-storage repository internals
```

---

# 18. 禁止循环依赖

例如：

```text
cas-provider
→ cas-application
→ cas-provider
```

属于错误设计。

出现循环依赖通常意味着：

```text
职责划分错误
```

应重新定义 Port，而不是通过：

```text
feature flag
dynamic global
```

规避。

---

# 19. Dependency Rule

新增依赖前必须回答：

```text
为什么已有依赖不能完成？

该依赖进入哪个 crate？

是否会污染 Domain？

维护是否活跃？

是否引入大量 transitive dependencies？

是否涉及 native dependency？

是否影响跨平台？
```

禁止为了一个小工具函数引入大型 crate。

---

# 20. Domain 不引用 Framework Type

错误：

```rust
pub struct Provider {
    pub url: reqwest::Url,
}
```

如果 `Provider` 属于纯 Domain，则不应该暴露基础设施实现类型。

可以使用：

```rust
ProviderEndpoint
```

等领域值对象。

---

# 21. Strong Type

核心 ID 使用强类型。

例如：

```rust
pub struct ProviderId(Uuid);

pub struct ModelId(Uuid);

pub struct AgentId(Uuid);

pub struct ProfileId(Uuid);

pub struct CredentialId(Uuid);
```

禁止核心业务大量使用：

```rust
String
```

互相代表所有 ID。

---

# 22. Strong ID 不允许隐式混用

以下代码应无法编译：

```rust
repository.find_provider(model_id)
```

因为：

```text
ModelId
≠
ProviderId
```

利用类型系统阻止错误。

---

# 23. Business Key 独立类型

例如：

```rust
pub struct ProviderKey(String);

pub struct AgentKey(String);

pub struct ProfileKey(String);
```

这样可以统一验证：

```text
字符范围
长度
规范化
```

---

# 24. ID 与 Key 不混淆

```text
ID
=
内部稳定身份

Key
=
稳定业务标识
```

不得：

```rust
ProviderId(String::from("deepseek"))
```

这种语义混用。

---

# 25. Value Object

具有验证规则的数据应优先建立 Value Object。

例如：

```text
ProviderKey
AgentKey
ProviderEndpoint
ModelIdentifier
ReasoningEffort
CredentialId
```

不要让：

```text
String validation
```

散落在不同 Use Case。

---

# 26. Value Object 构造

推荐：

```rust
impl ProviderKey {
    pub fn parse(value: impl Into<String>) -> Result<Self, DomainError> {
        ...
    }
}
```

禁止：

```rust
ProviderKey(pub String)
```

然后任何地方都能创建非法值。

---

# 27. Entity Construction

业务实体优先通过：

```text
Constructor
Factory
Builder
```

建立合法状态。

例如：

```rust
Provider::create(...)
```

而不是整个项目随处：

```rust
Provider {
    ...
}
```

构造。

---

# 28. Public Field

核心 Domain Entity 不建议所有字段：

```rust
pub
```

应该通过：

```text
getter
domain method
```

控制状态变更。

---

# 29. Domain Mutation

推荐：

```rust
provider.disable();

agent.bind_model(model_id);

profile.rename(name);
```

而不是：

```rust
provider.enabled = false;
agent.model_id = model_id;
```

避免绕过业务校验。

---

# 30. Enum

稳定有限集合使用：

```rust
enum
```

例如：

```rust
pub enum CompatibilityLevel {
    Native,
    Compatible,
    GatewayRequired,
    Unsupported,
    Unknown,
}
```

禁止魔法字符串：

```rust
if status == "compatible"
```

散落。

---

# 31. Enum Persistence

Domain Enum 与数据库字符串映射：

```text
集中定义
```

不要每个 Repository 自己写一套：

```rust
match ...
```

如果需要转换，建立：

```text
Persistence Mapper
```

---

# 32. Unknown Variant

与外部系统交互的枚举应考虑：

```text
未来出现未知值。
```

但 Domain 核心枚举不需要为了所有未来可能性无意义加入：

```rust
Other(String)
```

具体判断依据：

```text
该数据是否来自不受 CAS 控制的外部系统。
```

---

# 33. Option 使用

真正存在：

```text
不存在 / 未知
```

语义时使用：

```rust
Option<T>
```

不要通过：

```text
空字符串
0
UNKNOWN_STRING
```

代替。

---

# 34. Boolean 使用

只有真正二元状态才使用：

```rust
bool
```

例如：

```text
enabled
```

如果存在：

```text
Supported
Unsupported
Unknown
```

则必须使用 Enum。

---

# 35. Rust 文件命名

统一：

```text
snake_case.rs
```

例如：

```text
provider_repository.rs
configuration_compiler.rs
model_compatibility.rs
```

---

# 36. Rust Type 命名

统一：

```text
PascalCase
```

例如：

```rust
ProviderRepository
ConfigurationCompiler
ModelCompatibility
```

---

# 37. Rust 函数

统一：

```text
snake_case
```

例如：

```rust
find_provider_by_key()
compile_configuration()
detect_codex_home()
```

---

# 38. 函数命名必须表达动作

避免：

```rust
process()
handle()
do_it()
execute()
manage()
```

没有上下文的通用命名。

推荐：

```rust
apply_configuration()
resolve_model_binding()
validate_provider_endpoint()
detect_external_modification()
```

---

# 39. Boolean 函数

使用：

```text
is_
has_
can_
supports_
requires_
```

例如：

```rust
is_enabled()
has_credential()
supports_tool_calling()
can_delete_provider()
```

---

# 40. `get_` 使用

纯字段 getter 不强制：

```text
get_name()
```

推荐：

```rust
name()
```

真正执行查询：

```rust
find_by_id()
load_by_id()
resolve_by_key()
```

明确区分。

---

# 41. Rust Module Visibility

默认：

```text
private
```

只有真正需要跨模块调用才：

```rust
pub(crate)
```

真正公共 API 才：

```rust
pub
```

不要习惯性全部 `pub`。

---

# 42. `pub(crate)` 优先

项目内部使用而无需暴露 crate API 时：

```rust
pub(crate)
```

优于：

```rust
pub
```

缩小可维护 API 面。

---

# 43. `mod.rs`

推荐现代 Rust 模块结构。

例如：

```text
provider/
├── mod.rs
├── entity.rs
├── capability.rs
└── error.rs
```

或者：

```text
provider.rs
provider/
├── capability.rs
└── error.rs
```

项目内保持统一。

---

# 44. 文件大小

不设机械硬限制。

但出现：

```text
单文件 > 800~1000 行
```

通常需要检查是否承担多个职责。

不要为了满足行数限制把一个完整逻辑机械拆成几十个微文件。

---

# 45. 函数大小

函数应完成：

```text
一个明确动作。
```

出现大量：

```text
嵌套判断
几十个局部变量
多个外部系统
```

时应拆分。

重点看：

```text
职责
```

而不是绝对行数。

---

# 46. Early Return

推荐：

```rust
if !provider.enabled() {
    return Err(...);
}
```

避免：

```rust
if provider.enabled() {
    if ...
        if ...
```

多层嵌套。

---

# 47. Match

对于 Domain Enum：

优先使用：

```rust
match
```

并保持：

```text
exhaustive
```

不要：

```rust
_ => ...
```

无条件吞掉未来新增业务状态。

---

# 48. `_` 分支

只有当：

```text
明确所有未列出的情况语义相同
```

时使用。

核心业务 Enum 尽量显式列举。

---

# 49. `unwrap`

生产代码默认禁止：

```rust
unwrap()
expect()
```

用于：

```text
外部输入
文件
数据库
网络
用户配置
环境变量
```

---

# 50. 允许 `expect`

可以用于：

```text
程序员不变量
且失败代表代码 Bug
```

例如：

```rust
Regex::new(STATIC_PATTERN)
    .expect("static regex must compile");
```

但必须非常有限。

---

# 51. 测试代码

Test 中允许合理：

```rust
unwrap()
expect()
```

提高可读性。

---

# 52. Panic

业务错误不得：

```rust
panic!
```

例如：

```text
Provider 不存在
Codex 配置非法
Credential 缺失
```

都属于：

```text
Result
```

路径。

---

# 53. Error Strategy

推荐：

```text
Domain Error
Application Error
Infrastructure Error
Presentation Error
```

分层。

不要定义一个：

```rust
enum AppError {
    Everything(...)
}
```

然后整个项目共享。

---

# 54. Domain Error

只表达业务语义。

例如：

```rust
pub enum AgentError {
    InvalidKey,
    ModelBindingRequired,
    UnsupportedCapability,
}
```

不得包含：

```text
SQLx Error
reqwest Error
std::io::Error
```

---

# 55. Infrastructure Error

例如：

```rust
pub enum StorageError {
    Unavailable,
    ConstraintViolation,
    Corrupted,
    Unexpected,
}
```

内部可以：

```text
保留 source error
```

用于日志。

---

# 56. Application Error

Application 层将：

```text
Domain Error
Infrastructure Error
External Error
```

转换为稳定业务错误。

例如：

```text
PROVIDER_NOT_FOUND
AGENT_NAME_CONFLICT
CODEX_CONFIG_NOT_WRITABLE
```

---

# 57. UI 不显示原始 Rust Error

错误：

```text
Os { code: 5, kind: PermissionDenied ...}
```

不直接作为普通用户提示。

Presentation 获得：

```text
error code
safe message
optional diagnostic details
```

---

# 58. `thiserror`

Rust Error 类型优先：

```text
thiserror
```

统一派生。

避免手写重复：

```rust
impl Display
impl Error
```

---

# 59. `anyhow`

核心库代码原则上不以：

```text
anyhow::Error
```

作为公共错误 API。

可以在：

```text
CLI entrypoint
binary bootstrap
```

这种最外层程序边界有限使用。

---

# 60. Error Source

基础设施错误应保留：

```text
source
```

方便内部诊断。

但：

```text
source
```

不直接序列化给 UI。

---

# 61. Error Code

跨 Tauri IPC / CLI 输出的业务错误应具有稳定：

```text
error_code
```

例如：

```text
CODEX_NOT_FOUND
PROVIDER_AUTH_FAILED
MODEL_INCOMPATIBLE
```

不要让前端通过：

```text
匹配英文错误字符串
```

判断错误类型。

---

# 62. Async 边界

Async 用于真正的：

```text
I/O
```

例如：

```text
HTTP
Database
Filesystem async
Process
```

纯 Domain：

```text
Validation
Capability resolution
Configuration plan calculation
```

保持同步。

---

# 63. 不为了统一全部 async

错误：

```rust
async fn validate_agent(...)
```

只是因为调用方是 async。

如果没有异步 I/O：

```rust
fn validate_agent(...)
```

即可。

---

# 64. 不阻塞 Tokio Runtime

耗时阻塞操作：

```text
平台 API
大文件压缩
同步 OS Credential 调用
```

如果不可异步，应：

```text
spawn_blocking
```

隔离。

但普通极小文件读取无需过度优化。

---

# 65. 锁

避免长时间持有：

```text
Mutex
RwLock
```

跨：

```text
await
```

尤其禁止：

```rust
let guard = state.lock().await;
provider_client.call().await;
```

长期持锁。

---

# 66. Global Mutable State

禁止：

```text
static mut
全局可变 HashMap
全局 Provider Registry 可随意修改
```

Application State 通过：

```text
显式依赖注入
```

传递。

---

# 67. Service Construction

推荐：

```rust
ApplicationContext {
    provider_repository,
    agent_repository,
    secret_service,
    codex_service,
}
```

由 Composition Root 创建。

不要在业务方法内部：

```rust
SqlitePool::connect(...)
ReqwestClient::new()
SecretStore::new()
```

---

# 68. Dependency Injection

不要求引入 DI Framework。

Rust 使用：

```text
Constructor Injection
Trait Port
Arc
```

即可。

---

# 69. Trait 使用原则

Trait 用于：

```text
真正存在替换实现的边界
```

例如：

```text
ProviderRepository
SecretStorage
CodexEnvironment
ProviderAdapter
```

不要每一个 Struct 都：

```text
Trait + Impl
```

为了“架构感”。

---

# 70. Trait 位置

Port Trait 放在：

```text
使用它的上层
```

而不是：

```text
实现它的 Infrastructure
```

例如：

```text
cas-application
定义 ProviderRepository port
```

`cas-storage` 实现。

---

# 71. Async Trait

如果 Trait 方法确实需要异步，可使用当前稳定实现方式或项目统一的 async-trait 策略。

项目只能选择：

```text
一种
```

不要部分 native async trait、部分 `async_trait` 随意混用。

---

# 72. Repository 不泄漏 SQLx

错误：

```rust
async fn find(...) -> Result<Provider, sqlx::Error>
```

正确：

```rust
async fn find(...) -> Result<Option<Provider>, StorageError>
```

---

# 73. Repository `NotFound`

查询单个实体通常：

```rust
Result<Option<T>, Error>
```

Repository 不一定把：

```text
不存在
```

直接转为 Error。

由 Application Use Case 判断：

```text
不存在是否属于业务错误。
```

---

# 74. `save`

Repository 方法语义必须明确。

避免：

```text
save()
```

同时承担：

```text
insert or update or upsert or replace everything
```

且没人知道规则。

可以根据场景使用：

```text
insert
update
upsert
replace_bindings
```

---

# 75. Upsert

只有业务语义真的是：

```text
不存在创建，存在更新
```

时使用。

不要为了 SQL 方便全部：

```text
ON CONFLICT DO UPDATE
```

掩盖业务错误。

---

# 76. SQL

复杂 SQL 优先写成：

```text
清晰多行 SQL
```

而不是动态字符串拼接。

例如：

```rust
sqlx::query!(
    r#"
    SELECT id, provider_key, name
    FROM providers
    WHERE id = ?
    "#,
    id
)
```

---

# 77. SQL 字段

SQL 明确列出字段。

禁止：

```sql
SELECT *
```

作为正式 Repository 查询。

---

# 78. SQL Injection

任何用户输入必须：

```text
bind parameter
```

禁止：

```rust
format!("SELECT ... {}", user_input)
```

拼接 SQL。

---

# 79. Dynamic ORDER BY

如果需要动态排序：

使用：

```text
白名单 Enum
```

映射列名。

不得直接接受用户字符串拼 SQL。

---

# 80. Database Transaction

事务入口尽量位于：

```text
Application Use Case
```

或者 Persistence 提供的：

```text
Unit of Work
```

边界。

不要 Repository A 自己提交一半，然后 Repository B 再单独提交。

---

# 81. Transaction 参数

需要共享 Transaction 时，Repository 实现必须采用项目统一方式。

不要同时存在：

```text
repository method 自己 BEGIN
```

和：

```text
外部传 transaction
```

两种不一致模式。

---

# 82. Migration

Migration 文件：

```text
只包含 Schema 变更
```

不混入：

```text
业务初始化逻辑
Provider Preset
Agent Prompt
```

静态 Preset 属于：

```text
resources
```

---

# 83. TOML 操作

所有 Codex shared config：

```text
必须通过 cas-codex
```

并使用：

```text
toml_edit AST
```

处理。

其他模块不得：

```rust
std::fs::write("~/.codex/config.toml", ...)
```

---

# 84. Config DTO

Codex-specific TOML 映射使用专用类型。

例如：

```rust
CodexProviderConfig
CodexAgentConfig
```

这些类型只存在：

```text
cas-codex
```

不要污染 Domain：

```rust
Provider {
    wire_api: ...
}
```

除非该字段确实属于领域协议概念。

---

# 85. Config Mapper

映射统一：

```text
Domain
↓
Codex Mapper
↓
Codex Config Representation
```

禁止：

```text
UI
直接拼 TOML
```

---

# 86. Configuration Compiler

Compiler 必须尽可能：

```text
Pure
Deterministic
```

不能：

```text
写文件
查数据库
访问网络
读 Secret
```

这些输入必须提前准备。

---

# 87. Configuration Plan

Plan 使用强类型 Operation。

例如：

```rust
enum ConfigOperation {
    Create(CreateResource),
    Update(UpdateResource),
    Delete(DeleteResource),
    NoOp,
}
```

不要使用：

```text
Vec<String>
```

描述操作。

---

# 88. File Path

路径使用：

```rust
Path
PathBuf
```

不要到处：

```text
String path
```

---

# 89. Path Join

禁止：

```rust
format!("{}/agents/{}", base, filename)
```

使用：

```rust
base.join("agents").join(filename)
```

保证跨平台。

---

# 90. Home Expansion

`~` 展开集中于：

```text
Platform / Codex Environment
```

其他业务代码使用：

```text
resolved PathBuf
```

---

# 91. Path Canonicalization

需要身份比较时进行：

```text
canonical / normalized path
```

但不要对：

```text
尚不存在的目标文件
```

盲目调用 canonicalize 后失败。

应有明确路径规范化策略。

---

# 92. Atomic File Write

不得把原子写逻辑散落多个模块。

统一封装：

```text
AtomicFileWriter
```

负责：

```text
temp
flush
replace
cleanup
```

---

# 93. Hash

文件或 Fragment Hash 使用统一：

```text
HashService
```

或者集中 helper。

避免：

```text
模块 A SHA-256
模块 B xxHash
模块 C 自己比较字符串
```

导致 Ownership 行为不一致。

---

# 94. Canonicalization

Fragment Hash 的 Canonical Representation 必须集中实现。

同样逻辑不能在：

```text
Config Reader
Config Writer
Diagnostics
```

分别实现三次。

---

# 95. Provider Adapter 命名

通用 Provider Adapter：

```rust
ResponsesProviderAdapter
```

而不是：

```rust
DeepSeekProvider
```

如果 DeepSeek 没有特殊行为。

---

# 96. Provider-specific 分支

以下属于严重 Code Smell：

```rust
if provider.key() == "deepseek" {
    ...
} else if provider.key() == "openrouter" {
    ...
}
```

出现时必须优先考虑：

```text
Preset Data
Capability
Adapter
Strategy
```

是否才是正确表达。

---

# 97. Model-specific 分支

同理禁止：

```rust
if model.model_id() == "deepseek-v4-flash" {
    ...
}
```

用于决定通用能力。

应该读取：

```text
ModelCapability
Compatibility
Metadata
```

---

# 98. Exception

只有：

```text
Provider / Model 真正存在无法数据化的协议异常
```

才允许专用 Adapter。

并必须封装在：

```text
cas-provider
```

内部。

---

# 99. HTTP Client

统一构建：

```text
ProviderHttpClient
```

管理公共：

```text
timeout
TLS
redirect
user-agent
logging
```

Provider Adapter 不各自随意初始化 `reqwest::Client`。

---

# 100. HTTP Response

HTTP Raw Response 不穿透 Application。

Adapter 将其转换为：

```text
Domain-compatible result
```

例如：

```rust
ProviderCheckResult
DiscoveredModel
CapabilityEvidence
```

---

# 101. Retry

Retry 必须通过：

```text
统一策略
```

实现。

禁止 Adapter 内：

```rust
for _ in 0..10 {
   send().await
}
```

自行重试。

---

# 102. Serialization

外部 API DTO 与 Domain Entity 分离。

例如：

```rust
struct ResponsesModelDto {
    id: String,
}
```

只存在：

```text
provider adapter / transport
```

然后转换：

```text
DTO
→
Domain / Integration Result
```

---

# 103. `serde(rename_all)`

外部 JSON 可以：

```rust
#[serde(rename_all = "snake_case")]
```

统一映射。

但不能为了 API 格式修改 Domain 命名。

---

# 104. Unknown JSON

对于 Provider 响应：

```text
只解析需要字段
```

不要求把完整响应建模。

同时通过：

```text
serde 默认忽略未知字段
```

提高 Forward Compatibility。

---

# 105. Required Field

真正关键字段缺失：

```text
返回 Protocol Error
```

而不是：

```text
unwrap_or_default()
```

继续制造错误状态。

---

# 106. TypeScript 基线

Frontend 使用：

```text
TypeScript Strict Mode
```

必须启用：

```json
{
  "strict": true
}
```

不得通过：

```text
大量 any
```

绕开类型系统。

---

# 107. 禁止 `any`

生产代码原则上禁止：

```typescript
any
```

外部未知数据使用：

```typescript
unknown
```

然后显式解析。

---

# 108. Type Assertion

避免：

```typescript
value as Provider
```

绕过校验。

Tauri IPC 返回数据应有：

```text
明确 API 类型
```

必要时运行时校验。

---

# 109. 前端目录

推荐：

```text
src/
├── app/
├── pages/
├── features/
├── entities/
├── components/
├── hooks/
├── lib/
├── api/
└── styles/
```

不要按：

```text
components/
services/
utils/
```

三个万能目录堆全部代码。

---

# 110. Feature Organization

例如：

```text
features/
├── provider-create/
├── agent-edit/
├── profile-activate/
└── configuration-apply/
```

业务行为按 Feature 聚合。

---

# 111. Entity UI

可复用领域展示：

```text
entities/provider/
entities/model/
entities/agent/
```

例如：

```text
ProviderStatusBadge
ModelDisplayName
AgentIcon
```

---

# 112. Shared Component

只有真正：

```text
无业务语义
```

的 UI 才进入：

```text
components/
```

例如：

```text
Button
Dialog
Input
EmptyState
StatusDot
```

---

# 113. Page

Page 负责：

```text
页面布局
数据查询组合
Feature 组合
```

不实现：

```text
Provider 业务规则
Codex Apply
Secret Storage
```

---

# 114. React Component

组件使用：

```text
PascalCase
```

文件：

```text
ProviderCard.tsx
AgentList.tsx
ModelPicker.tsx
```

如果项目决定统一：

```text
kebab-case file
```

也可以，但整个前端必须统一。

推荐当前项目使用：

```text
PascalCase component file
camelCase non-component file
```

---

# 115. Hook

自定义 Hook：

```text
useXxx
```

例如：

```text
useProviders()
usePendingChanges()
useAgentForm()
```

不要：

```text
useManager()
useCommon()
```

---

# 116. Hook 职责

Hook 用于：

```text
React 生命周期
UI state composition
query abstraction
```

不作为后端业务逻辑替代品。

---

# 117. API 层

所有 Tauri Command 调用统一封装：

```text
api/
```

例如：

```typescript
providerApi.list()
agentApi.update()
configurationApi.apply()
```

组件不要到处：

```typescript
invoke("xxx")
```

直接调用。

---

# 118. Tauri Command Name

Command 采用清晰领域动作：

```text
provider_list
provider_create
provider_update

agent_list
agent_update

configuration_apply
diagnostics_run
```

不要：

```text
do_action
execute
process
```

---

# 119. Command Handler

Command Handler 应保持：

```text
薄
```

例如：

```rust
#[tauri::command]
async fn provider_create(
    state: State<'_, AppState>,
    request: CreateProviderRequest,
) -> Result<ProviderResponse, ApiError> {
    state
        .provider_service
        .create(request.try_into()?)
        .await
        .map(Into::into)
        .map_err(Into::into)
}
```

禁止在 Handler：

```text
SQL
文件 Patch
Provider HTTP
几十行业务判断
```

---

# 120. IPC DTO

Tauri IPC 使用专门：

```text
Request DTO
Response DTO
```

不要直接序列化完整 Domain Entity 作为长期公共 API。

---

# 121. 为什么分离 DTO

避免：

```text
Domain 字段一改
前端 API 全部跟着破坏
```

同时避免暴露：

```text
内部字段
Ownership Metadata
Secret Reference internals
```

---

# 122. IPC Field Naming

前后端统一：

```text
camelCase
```

JSON。

Rust：

```rust
#[serde(rename_all = "camelCase")]
```

例如：

```text
providerId
modelId
createdAt
```

---

# 123. API Request Validation

前端校验用于：

```text
用户体验
```

后端必须再次：

```text
业务校验
```

不能因为：

```text
React 已经 required
```

就认为 Rust 可以信任输入。

---

# 124. 前端 Error Handling

API 层将 Tauri Error 转成：

```typescript
AppError
```

例如：

```typescript
type AppError = {
  code: string;
  message: string;
  details?: unknown;
};
```

组件不解析 Rust stack trace。

---

# 125. Server State

Provider / Agent / Profile 等 CAS 持久化状态属于：

```text
Backend State
```

前端不要建立第二份长期 Source of Truth。

---

# 126. UI Local State

适合 React Local State：

```text
Dialog open
Form draft
Search query
Selected tab
Expanded section
```

不适合：

```text
完整 Provider 数据库镜像
Agent 全局永久状态
```

---

# 127. 状态管理

V0.1 不因为“以后会复杂”就引入重型全局状态框架。

如果 React Query / TanStack Query 类工具适合 Tauri Query，可以使用。

否则保持简单：

```text
API
+
Hooks
+
Local State
```

---

# 128. Form State

Form Draft 与 Backend Saved State 分离。

不要用户每输入一个字符：

```text
立即写 SQLite
```

除非产品明确要求 autosave。

---

# 129. Boolean Naming TypeScript

同样：

```text
isEnabled
hasCredential
canApply
supportsReasoning
```

而不是：

```text
enabledFlag
credential
available
```

语义模糊。

---

# 130. TypeScript Enum

前端对于后端稳定枚举，可以使用：

```text
string union
```

例如：

```typescript
type CompatibilityLevel =
  | "NATIVE"
  | "COMPATIBLE"
  | "GATEWAY_REQUIRED"
  | "UNSUPPORTED"
  | "UNKNOWN";
```

通常比 TypeScript `enum` 更轻。

---

# 131. Magic String

禁止组件直接：

```typescript
if (status === "UNSUPPORTED")
```

到处自行定义行为。

应集中：

```text
Status mapping
```

例如：

```typescript
compatibilityPresentation(status)
```

---

# 132. 前端 Mapping

显示文字、Badge、Icon 等：

```text
Presentation Mapping
```

集中定义。

避免：

```text
Agents 页一套颜色
Models 页另一套颜色
```

---

# 133. CSS

推荐：

```text
CSS Variables
+
统一 Design Token
```

不要组件内大量：

```text
#1F2D3D
13px
17px
23px
```

硬编码。

---

# 134. Inline Style

除动态计算确有必要外，不使用大量：

```tsx
style={{ ... }}
```

代替统一样式系统。

---

# 135. UI Library

如果引入组件库：

```text
只能作为基础组件能力
```

不得让 UI Library 的默认设计破坏 CAS 已有 UI 规范。

避免混用多个完整组件库。

---

# 136. 图标

统一一套图标库。

例如：

```text
Lucide
```

不要同时：

```text
Lucide
Heroicons
FontAwesome
Emoji
```

表达同类操作。

---

# 137. 注释原则

代码注释优先解释：

```text
为什么这样做

重要业务边界

不明显的不变量

兼容性原因

安全原因
```

不要解释明显语法。

---

# 138. 错误注释

不要：

```rust
// 设置 enabled 为 false
provider.enabled = false;
```

应该：

```rust
// Provider 被禁用后保留现有绑定，避免重新启用时丢失用户配置。
provider.disable();
```

---

# 139. 总括性注释

一段复杂业务逻辑前可以添加：

```rust
// 先验证 CAS-owned 片段未被外部修改，再基于最新 TOML AST 生成补丁，
// 防止使用旧缓存覆盖用户刚刚修改的非 CAS 配置。
```

优于每一行都写注释。

---

# 140. Rust Doc Comment

公共领域类型和关键 Port 使用：

```rust
/// ...
```

说明：

```text
职责
关键语义
重要不变量
```

不要把实现细节全部写进 Doc Comment。

---

# 141. TODO

禁止：

```text
TODO
```

长期没有上下文。

推荐：

```rust
// TODO(v0.2): support gateway-backed providers after the protocol
// translation contract is finalized.
```

如果有 Issue，可引用 Issue。

---

# 142. FIXME

`FIXME` 只用于：

```text
已知错误或技术债
```

不能把未实现功能全部标为 FIXME。

---

# 143. 中文还是英文注释

代码标识符统一：

```text
English
```

代码注释推荐：

```text
English
```

保持开源项目可读性。

用户界面文案由 i18n 管理。

开发文档可以使用中文。

---

# 144. 用户可见文案

前端不得把大量英文字符串散落组件。

使用：

```text
i18n key
```

或者至少集中管理。

因为 CAS 预期可分享给其他用户。

---

# 145. i18n

V0.1 如果支持：

```text
中文
英文
```

所有用户可见文案必须从：

```text
locale resources
```

读取。

不要：

```tsx
<Button>保存</Button>
```

同时其他页面：

```tsx
<Button>Save</Button>
```

混杂。

---

# 146. Provider Preset 文案

Preset 的：

```text
name
description
```

如需要多语言，应：

```text
元数据与展示文案分离
```

不要复制多个 Provider Preset 只为了语言。

---

# 147. 日志框架

Rust 统一：

```text
tracing
```

禁止同时：

```text
println!
eprintln!
log
tracing
```

混杂实现正式日志。

---

# 148. `println!`

仅允许：

```text
CLI 正式输出
cas-helper stdout contract
```

等明确终端输出。

内部日志不使用 `println!`。

---

# 149. Log Level

统一：

```text
ERROR
WARN
INFO
DEBUG
TRACE
```

语义：

```text
ERROR
当前操作失败，需要处理

WARN
存在异常或风险，但操作可能继续

INFO
关键生命周期事件

DEBUG
开发诊断信息

TRACE
极细粒度内部行为
```

---

# 150. INFO

适合：

```text
application started

migration completed

provider created

configuration apply committed

restore completed
```

不要：

```text
每次数据库 SELECT
```

都 INFO。

---

# 151. DEBUG

适合：

```text
resolved provider adapter

configuration operation count

codex home detected

provider request completed
```

不得包含 Secret。

---

# 152. TRACE

可以用于：

```text
详细内部流程
```

但仍必须遵守：

```text
Secret Redaction
```

安全规则。

---

# 153. Correlation ID

长流程如：

```text
Apply
Restore
Provider Probe
```

建议使用：

```text
operation_id / transaction_id
```

关联日志。

---

# 154. Logging Entity

使用：

```text
ProviderId
AgentId
TransactionId
```

而不是把完整实体 Debug 输出。

---

# 155. Secret Logging

所有安全规范优先于普通调试便利。

禁止：

```rust
debug!(?request);
```

如果 Request 可能包含 Secret。

---

# 156. Sensitive Type

Secret 类型禁止：

```text
Debug 明文
Serialize
Clone 无限制
```

具体要求以安全规范为准。

---

# 157. Clone

Domain 普通不可变 Value Object 可以合理 `Clone`。

大型 Aggregate 或 Secret 不要为了 borrow checker 方便到处：

```rust
.clone()
```

---

# 158. `Arc`

只在：

```text
跨 task / service 共享长期依赖
```

时使用。

不要所有 Domain Entity：

```rust
Arc<Provider>
```

化。

---

# 159. Interior Mutability

避免：

```text
Arc<Mutex<DomainEntity>>
```

作为业务状态管理。

持久化状态通过：

```text
Repository
```

更新。

---

# 160. Application State

Tauri 全局 State 只保存：

```text
长期无状态 Service
Connection Pool
HTTP Client
Repository Handles
```

不要存：

```text
当前正在编辑的 Agent
当前页面 Form
```

---

# 161. Cache

任何 Cache：

```text
必须可失效
可重新生成
不能成为 Source of Truth
```

代码类型名称建议明确：

```text
ModelDiscoveryCache
```

不要叫：

```text
ModelStore
```

误导职责。

---

# 162. Feature Flag

Cargo Feature 只用于：

```text
编译时真正可选能力
```

例如：

```text
platform-specific
experimental gateway
```

不要使用大量 Feature Flag 模拟：

```text
运行时 Provider 配置。
```

---

# 163. `cfg`

平台分支集中在：

```text
cas-platform
```

禁止其他模块大量：

```rust
#[cfg(target_os = "windows")]
```

散落。

---

# 164. Platform Path

例如：

```text
AppData
Keychain
Executable Extension
```

都通过 Platform Adapter。

---

# 165. Windows 特殊代码

放：

```text
platform/windows/
```

macOS：

```text
platform/macos/
```

Linux：

```text
platform/linux/
```

不要单文件里面几百个：

```rust
#[cfg(...)]
```

---

# 166. CLI

CLI Command 定义和 Application 调用分离。

例如：

```text
commands/
├── status.rs
├── doctor.rs
├── apply.rs
└── profile.rs
```

Command 负责：

```text
解析参数
调用 Application
格式化输出
退出码
```

---

# 167. CLI 不直接查询 Repository

即使为了：

```text
cas status
```

方便，也必须调用：

```text
StatusQueryService
```

而不是：

```rust
sqlite_repository.list_agents()
```

---

# 168. CLI 输出与日志分离

stdout：

```text
用户请求结果
```

stderr：

```text
错误/必要提示
```

Tracing 日志不能污染：

```text
machine-readable stdout
```

---

# 169. Machine-readable Output

未来 CLI 如支持：

```text
--json
```

必须：

```text
输出稳定 DTO
```

不得把日志混在 JSON 前后。

---

# 170. `cas-helper`

`cas-helper` 是安全敏感二进制。

代码要求：

```text
极少依赖

极少分支

不访问网络

不读取 cas.db

不读取项目

不加载 Provider Preset

不使用 Tauri
```

---

# 171. Helper Main

入口应保持简单：

```text
parse args
↓
validate credential id
↓
secret store read
↓
stdout
↓
exit
```

不要引入完整：

```text
ApplicationContext
```

---

# 172. Helper stdout

禁止普通日志写 stdout。

Helper 日志：

```text
stderr only
```

且安全规范优先。

---

# 173. Provider Preset

Preset 使用：

```text
静态 schema
```

加载后必须：

```text
validate
```

才能进入 Registry。

---

# 174. Preset Loader

Preset Loader 只负责：

```text
读取
解析
Schema Validation
```

不负责：

```text
创建 Provider
调用 API
```

---

# 175. Preset Data

Preset 内禁止：

```text
可执行脚本
Rust code path
arbitrary command
```

代码中不得给这种字段预留：

```text
exec: String
```

避免未来被错误使用。

---

# 176. Resource Loading

内置 Resource 读取集中：

```text
ResourceRegistry
```

不要页面自己：

```text
read provider-presets/deepseek.json
```

---

# 177. Schema Version

Preset / Model Metadata 的：

```text
schemaVersion
```

转换逻辑集中。

不支持版本：

```text
明确报错
```

不能：

```text
best effort 乱解析
```

---

# 178. Serialization Boundary

内部 Domain Entity 不直接用于：

```text
Preset JSON
Tauri JSON
Database Row
Codex JSON
```

四种场景。

每个边界建立自己的 Mapping。

---

# 179. Mapping Function

简单映射可以：

```rust
impl TryFrom<Dto> for DomainType
```

复杂带外部依赖映射使用专用：

```text
Mapper
Assembler
```

避免 `From` 内做数据库查询。

---

# 180. `From` 与 `TryFrom`

不会失败：

```rust
From
```

可能失败：

```rust
TryFrom
```

禁止：

```text
转换失败却 silently default。
```

---

# 181. Validation Placement

基础格式：

```text
Value Object
```

验证。

跨实体业务规则：

```text
Domain / Application
```

验证。

外部系统能力：

```text
Integration / Application
```

验证。

不要所有 Validation 都塞：

```text
validate.rs
```

万能文件。

---

# 182. Duplicate Validation

前端可以提前提示：

```text
Provider Key 已存在
```

但数据库 Unique Constraint 和后端 Domain 仍必须保证。

---

# 183. Command / Query

Application 方法名称表达：

```text
Command
Query
```

例如：

```text
create_provider()
update_agent()
activate_profile()

list_models()
get_agent_detail()
get_status()
```

---

# 184. Side Effect

Query 原则：

```text
无业务副作用。
```

例如：

```text
get_provider()
```

不能顺便：

```text
自动修复配置
刷新 Credential
修改数据库
```

---

# 185. Diagnostics

Diagnostics Query 默认：

```text
只读
```

符合既有产品规范。

Repair 必须单独 Use Case。

代码层也不能：

```text
run_diagnostics()
```

内部偷偷：

```text
fix_config()
```

---

# 186. Apply

Apply 是显式 Mutation Use Case。

不得由：

```text
save_agent()
```

自动调用。

必须保持：

```text
Save
≠
Apply
```

这一产品不变量在代码 API 层同样成立。

---

# 187. Method Naming

因此：

```text
save_agent()
```

不能内部：

```text
write Codex agent.toml
```

应该：

```text
保存 CAS State
```

另有：

```text
apply_configuration()
```

---

# 188. Profile Activation

同样：

```text
activate_profile()
```

只修改 CAS Desired State。

不能隐式：

```text
apply_configuration()
```

除非未来产品明确改变该规则。

---

# 189. Side Effect Visibility

高影响方法名称必须显式。

例如：

```text
delete_provider()
restore_configuration()
apply_configuration()
replace_credential()
```

不要：

```text
sync()
update()
```

隐藏真实副作用。

---

# 190. Transaction Naming

配置事务使用：

```text
ApplyTransaction
```

数据库事务不要也命名：

```text
ApplyTransaction
```

避免混淆。

数据库可称：

```text
DbTransaction
```

---

# 191. Time

Domain 需要当前时间时：

不要到处：

```rust
Utc::now()
```

对于可测试的重要业务流程，使用：

```text
Clock
```

抽象。

例如：

```text
Apply Transaction
Snapshot
Verification Evidence
```

---

# 192. Random / UUID

类似：

```text
IdGenerator
```

可以集中提供。

不要为了测试：

```text
每个方法硬调 Uuid::new_v4()
```

导致状态难复现。

不要求所有 ID 都通过 Trait，如果复杂度不值得，可以保持简单。

---

# 193. Deterministic Compiler

Configuration Compiler 禁止使用：

```text
当前时间
随机 UUID
HashMap 随机顺序
```

影响最终配置文本。

同一输入：

```text
必须产生稳定输出。
```

---

# 194. Ordering

输出：

```text
Agent
Provider
Model Catalog
```

列表时，如果顺序会影响 Hash / Diff：

必须：

```text
显式排序。
```

不要依赖：

```text
HashMap iteration order。
```

---

# 195. Collection

需要稳定顺序：

```text
Vec
BTreeMap
BTreeSet
```

根据语义使用。

不要为了方便默认：

```text
HashMap
```

然后后期再排序。

---

# 196. ID Map

纯内存快速 lookup：

```text
HashMap
```

合理。

最终 deterministic rendering 前必须排序。

---

# 197. String Formatting

用户可见消息不在 Domain 里大量拼接。

Domain 返回：

```text
Error Code / structured issue
```

Presentation / i18n 负责文字。

---

# 198. Domain 不知道 UI 文案

错误：

```rust
return Err("请先配置模型".into());
```

正确：

```rust
return Err(AgentError::ModelBindingRequired);
```

---

# 199. Provider Error Message

Provider 原始安全错误可以作为：

```text
diagnostic detail
```

但业务层仍保留：

```text
standard error code。
```

---

# 200. Code Duplication

少量重复：

```text
优于错误抽象。
```

只有发现：

```text
稳定、语义相同
```

的重复后再抽象。

---

# 201. Generic Helper

不要创建：

```rust
fn do_everything<T, U, V>(...)
```

仅为了消除几行重复。

---

# 202. Abstraction Rule

一个抽象至少应该拥有：

```text
明确职责
稳定边界
多个合理调用者
```

而不是因为：

```text
“未来可能用”
```

提前设计。

---

# 203. V0.1 YAGNI

明确不为以下能力提前污染核心代码：

```text
Cloud Sync
Marketplace
Dynamic Plugin
Agent Workflow
Model Router
Load Balancer
Billing
Remote Account
Profile Management
Provider Model Discovery
Model Runtime Probe
Import / Adopt
macOS / Linux Platform Implementation
```

可以保留边界，但不提前实现复杂框架。

---

# 204. Feature-specific Code

未来功能优先：

```text
新增模块
```

而不是：

```text
修改所有已有核心类型加入 nullable 字段。
```

---

# 205. Backward Compatibility

公开持久化格式、Preset Schema、IPC 如果已经正式发布：

变更必须考虑：

```text
Migration
Versioning
Compatibility
```

内部 Private API 可以更自由重构。

---

# 206. Public API

V0.1 不是 SDK。

因此 Rust crate 没必要无意义暴露大量：

```text
pub API。
```

优先保持内部实现可调整。

---

# 207. Documentation

复杂核心算法需要：

```text
模块级说明
关键方法 Doc Comment
```

例如：

```text
Configuration Ownership
Fragment Fingerprint
Apply Transaction
Capability Resolution
```

普通 CRUD 不需要写长篇注释。

---

# 208. Architecture Decision

如果实现必须偏离已有架构文档的重要原则：

不能直接：

```text
偷偷改代码。
```

需要：

```text
更新对应设计文档
或增加 ADR。
```

---

# 209. ADR

适合 ADR 的变化：

```text
SQLite → another storage

Tauri architecture change

Credential helper architecture change

Gateway introduction

Profile semantic change
```

不适合为：

```text
变量改名
组件拆分
```

写 ADR。

---

# 210. `unsafe`

Rust 项目默认：

```text
禁止 unsafe
```

如果平台 API 必须：

```text
unsafe
```

必须：

```text
限制在 cas-platform

最小范围

封装安全 API

注释 SAFETY rationale
```

---

# 211. `unsafe` 示例要求

必须写：

```rust
// SAFETY:
// - pointer originates from ...
// - lifetime is bounded by ...
// - API contract guarantees ...
unsafe {
    ...
}
```

不要：

```rust
// SAFETY: should be fine
```

---

# 212. FFI

平台 FFI 同样只存在：

```text
cas-platform
```

其他模块看到：

```text
safe Rust abstraction。
```

---

# 213. File Permissions

设置权限逻辑：

```text
Platform Adapter
```

不要在 Configuration Writer 里到处：

```rust
#[cfg(unix)]
chmod(...)
```

---

# 214. Process Execution

项目所有外部进程执行必须集中经过：

```text
ProcessRunner
```

或明确模块。

禁止业务代码随意：

```rust
Command::new(user_value)
```

---

# 215. User-controlled Command

V0.1 原则上禁止用户输入：

```text
arbitrary executable
```

然后 CAS 执行。

如果未来开放必须经过独立安全设计。

---

# 216. Shell

不使用：

```text
sh -c
cmd /c
powershell -Command
```

执行可由结构化参数完成的命令。

使用：

```rust
Command::new(executable)
    .args([...])
```

---

# 217. Environment

子进程环境应：

```text
最小化修改
```

不要为了便利把：

```text
整个 CAS Secret
```

写入全局环境变量。

---

# 218. Temporary File

使用系统安全临时目录或目标目录旁临时文件。

命名不能依赖用户原始字符串未经处理。

---

# 219. Cleanup

临时文件失败后：

```text
best effort cleanup
```

但不能因为 Cleanup 失败：

```text
覆盖原始业务错误。
```

记录 Warning 即可。

---

# 220. Atomic Replace

不同 OS 的行为由：

```text
cas-platform / filesystem abstraction
```

统一。

不要在高层依赖：

```text
Unix rename semantics
```

假设 Windows 完全相同。

---

# 221. File Handle

写入完成后确保：

```text
flush
close
```

再 Replace。

---

# 222. User Data Protection

任何写入用户：

```text
CODEX_HOME
```

的代码必须通过：

```text
Codex Integration
```

不能出现在 Provider、UI、Repository 等模块。

---

# 223. Code Formatting Rust

统一：

```text
rustfmt
```

不得维护手工格式风格。

CI 中检查：

```text
cargo fmt --check
```

---

# 224. Rust Lint

统一：

```text
clippy
```

CI 至少：

```text
cargo clippy
```

核心警告不允许长期忽略。

---

# 225. `allow`

禁止为了 CI 通过大量：

```rust
#[allow(...)]
```

压掉 Clippy。

确实合理的：

```text
局部 allow
+
明确原因。
```

---

# 226. TypeScript Formatting

统一使用一套：

```text
Prettier
```

或项目确定的 formatter。

不得多人手工维护不同格式。

---

# 227. TypeScript Lint

使用：

```text
ESLint
```

统一检查：

```text
unused
unsafe any
hooks
imports
```

---

# 228. Import Order

前端统一：

```text
external
internal alias
relative
style
```

可由工具自动完成。

不要人工争论排序细节。

---

# 229. Path Alias

前端可以使用：

```text
@/features
@/components
@/api
```

减少：

```text
../../../../../
```

深层相对路径。

---

# 230. Rust Import

遵循 rustfmt 默认。

无需自定义复杂分组规则。

---

# 231. Dead Code

正式 V0.1 不保留大量：

```text
unused module
future placeholder
```

如果功能未实现：

```text
不要提交空骨架
```

除非它是明确架构接口且当前已有调用价值。

---

# 232. Commented-out Code

禁止长期提交：

```text
整段注释掉的旧实现。
```

Git 已经保存历史。

---

# 233. Feature Stub

未实现功能返回：

```text
Unsupported
```

优于：

```text
空方法 success
```

避免让用户误认为已支持。

---

# 234. Error Swallowing

禁止：

```rust
let _ = important_operation();
```

除非：

```text
明确 best-effort
```

且需要：

```text
注释 / warning
```

---

# 235. Cleanup Error

适合：

```rust
if let Err(error) = cleanup().await {
    warn!(...);
}
```

因为主操作已决定结果。

---

# 236. Silent Default

禁止：

```rust
parse().unwrap_or_default()
```

用于关键配置。

例如：

```text
Codex Provider
Agent Name
Compatibility
```

解析错误必须暴露。

---

# 237. Default

Default 只适合：

```text
真实存在安全合理默认值
```

例如：

```text
optional UI filter
```

不要用 Default 隐藏缺失必填字段。

---

# 238. Date/Time

统一使用：

```text
UTC internally
```

Presentation 再转换本地时间。

不要数据库和 Domain 混用系统本地时区。

---

# 239. Size / Token

明确单位。

例如：

```text
context_window_tokens

timeout_ms

latency_ms
```

或者使用强类型。

避免字段：

```text
timeout
size
limit
```

不知道单位。

---

# 240. Duration

Rust 内部优先：

```rust
std::time::Duration
```

而不是到处整数毫秒。

序列化边界再转换。

---

# 241. Numeric Conversion

外部 API：

```text
u64
i64
usize
```

转换必须考虑：

```text
overflow
negative value
```

不要盲目 `as usize`。

---

# 242. URL

Provider URL 使用统一解析器。

业务代码不要通过：

```text
starts_with("https")
```

判断 URL 安全。

---

# 243. Normalization

Provider Endpoint、Key 等规范化：

```text
只在单一入口执行。
```

不要数据库保存时一套、Config Writer 又另一套。

---

# 244. Immutable Input

Application Request DTO 转成 Domain 后，不继续在多层修改同一个 DTO。

保持：

```text
Request
→ Validated Command
→ Domain Operation
```

---

# 245. Application Command

复杂 Use Case 可以定义：

```rust
CreateProviderCommand
UpdateAgentCommand
```

避免方法拥有十几个原始参数。

---

# 246. 参数数量

出现：

```text
> 5~6 个同层级参数
```

应考虑：

```text
Command Object / Value Object
```

尤其多个 `String` 容易传错。

---

# 247. Boolean 参数

禁止：

```rust
create_provider(true, false, true)
```

使用明确结构：

```rust
CreateProviderOptions {
    enabled: true,
    ...
}
```

---

# 248. Builder

Builder 用于：

```text
大量可选构造参数
```

不要给只有：

```text
id + name
```

的对象强行 Builder。

---

# 249. Function Return

不要返回含义不明：

```rust
(bool, bool, String)
```

建立：

```rust
ValidationResult
ApplyResult
```

等结构类型。

---

# 250. Tuple

只用于：

```text
局部、非常明确的轻量组合。
```

跨层 API 使用具名类型。

---

# 251. Event

V0.1 如果使用内部 Event：

```text
Typed Event
```

例如：

```rust
ProfileActivated
ConfigurationApplied
```

不要：

```text
String event name + JSON payload
```

在 Rust Core 内模拟消息总线。

---

# 252. Event Side Effect

Event Handler 不承担必须同步成功的核心一致性操作。

例如：

```text
Profile 激活后必须保存数据库
```

不能依赖：

```text
异步 event eventually 写
```

实现。

核心一致性直接在 Use Case 中完成。

---

# 253. Background Task

V0.1 尽量少后台常驻任务。

Provider 状态不是持续监控系统。

后台任务必须：

```text
可取消
有明确生命周期
错误可观测
```

---

# 254. Cancellation

长网络操作最好支持：

```text
用户取消
窗口关闭
```

但不要因为实现取消而让简单 CRUD 复杂化。

---

# 255. Timeout

所有外部 I/O：

```text
Provider HTTP
Process
```

必须存在合理 Timeout。

本地数据库普通查询无需额外人工 timeout。

---

# 256. Retry Safety

只有：

```text
幂等或安全重试
```

操作才自动 Retry。

不要自动重复：

```text
具有外部副作用的未来操作
```

除非明确保证幂等。

---

# 257. Idempotency

关键方法：

```text
apply_configuration
store provider configuration
resource generation
```

必须尽量具备幂等语义。

---

# 258. Delete Idempotency

是否允许：

```text
delete nonexistent resource
```

返回成功，要按 Use Case 定义。

不要各 Repository 不一致。

---

# 259. Configuration Apply

重复：

```text
Apply A
Apply A
```

必须不产生无意义文件变化。

代码层测试应围绕：

```text
deterministic render
```

设计。

---

# 260. Security-sensitive Code

以下目录/模块修改要求更谨慎：

```text
cas-secret
cas-helper
Codex auth mapper
Process execution
HTTP auth
Export
Backup
```

普通重构不得：

```text
为了减少代码
```

跨越安全边界。

---

# 261. Code Review 核心检查

每个 PR 至少考虑：

```text
职责是否正确？

有没有跨层调用？

是否引入 Provider 名称判断？

有没有破坏 Agent / Model 解耦？

有没有直接写 Codex 文件？

有没有直接读取 Secret？

有没有吞掉 Error？

有没有新增不必要依赖？

有没有破坏幂等性？

有没有改变外部数据但没考虑 Migration？
```

---

# 262. AI Agent 编码约束

AI Agent 修改 CAS 时必须：

```text
先定位所属模块

遵守已有边界

最小化修改范围

不擅自重构无关模块

不因为“最佳实践”更换技术栈

不自行引入新框架

不绕过 Repository

不绕过 Application Service

不直接覆盖 config.toml

不将 Secret 写入日志
```

---

# 263. AI Agent 遇到架构冲突

如果需求必须违反：

```text
系统结构文档
数据模型文档
安全规范
Codex 配置集成规范
```

不得静默：

```text
自己设计另一套。
```

应该：

```text
指出冲突
说明影响
等待上层架构决策
```

---

# 264. AI Agent 不扩大 Scope

例如任务：

```text
实现 Provider 列表
```

不得顺手：

```text
重写整个 Provider Domain
引入 Redux
升级 Tauri
重构数据库
```

除非这些改变是任务完成的必要条件。

---

# 265. Code Comment by AI

禁止生成：

```text
大量解释每行代码的注释。
```

注释只用于：

```text
业务语义
重要边界
非直观原因
```

---

# 266. Generated Boilerplate

AI Agent 生成代码后必须删除：

```text
未使用 import

假实现

TODO placeholder

无意义 wrapper

重复 DTO

死代码
```

不能因为生成方便保留。

---

# 267. Git Commit

推荐按：

```text
一个完整逻辑变化
```

组织 Commit。

避免：

```text
format
fix
fix2
again
final
```

类型历史。

---

# 268. Commit Message

推荐：

```text
feat(provider): add responses provider preset loader

fix(codex): preserve external provider fields during patch

refactor(storage): isolate profile transaction logic
```

是否严格 Conventional Commits 可在项目正式建立后决定。

---

# 269. 不提交生成数据

以下不进入 Git：

```text
cas.db

runtime backup

local logs

provider real credentials

generated user config

temporary files
```

---

# 270. Lockfile

应用项目：

```text
Cargo.lock
前端 package lock
```

应提交。

保证可复现构建。

---

# 271. npm 包管理器

前端固定一个：

```text
pnpm
npm
或 yarn
```

不要混用多个 lockfile。

项目初始化后写入贡献规范。

---

# 272. Node Version

固定：

```text
最低支持 Node Version
```

可通过：

```text
.nvmrc
.tool-versions
package.json engines
```

之一声明。

---

# 273. Rust Toolchain

建议：

```text
rust-toolchain.toml
```

声明项目 Toolchain。

避免不同开发者 Rust 版本差异造成：

```text
fmt
clippy
edition
```

结果不一致。

---

# 274. Rust Edition

所有 crate 使用相同 Rust Edition。

不要项目内部混用。

---

# 275. Build Warnings

正式 CI：

```text
核心项目 Warning 应视为需要修复。
```

不允许长期积累几十个：

```text
unused
deprecated
dead_code
```

Warning。

---

# 276. Deprecated API

发现依赖 API Deprecated：

```text
安排替换
```

不要无条件：

```text
allow(deprecated)
```

长期维持。

---

# 277. Dependency Pinning

保持：

```text
合理 semver
```

不必所有依赖精确锁死在 Cargo.toml。

实际可复现版本由 Lockfile 保证。

安全修复依赖需要及时更新。

---

# 278. Dependency Feature

只开启需要的 Cargo Feature。

例如：

```text
reqwest
```

不要默认启用所有不需要协议和压缩支持。

减少：

```text
binary size
attack surface
compile time
```

---

# 279. Frontend Dependency

同样避免：

```text
只是需要一个 debounce
→ 引入大型工具库
```

简单稳定功能优先本地实现或已有工具。

---

# 280. Date Library

如果前端需要日期格式化，统一使用一个方案。

不要：

```text
Date
dayjs
date-fns
moment
```

同时混用。

---

# 281. HTTP 不在 Frontend

Provider API 请求：

```text
必须从 Rust Backend 发出。
```

React 不直接：

```typescript
fetch(provider.baseUrl)
```

原因：

```text
Credential
CORS
统一安全策略
日志
TLS
```

都应该由后端控制。

---

# 282. Filesystem 不在 Frontend

React 不直接拥有：

```text
任意 filesystem permission。
```

需要打开目录、读取文件等通过：

```text
Tauri Command
```

和受控 Use Case。

---

# 283. Database 不在 Frontend

前端永远不知道：

```text
SQLite path
SQL
table name
```

---

# 284. Domain Naming

代码中的名称应与领域文档一致。

使用：

```text
Provider
Model
Agent
Profile
ManagedResource
ConfigurationSnapshot
```

不要另起：

```text
Engine
Bot
Brain
Workspace Profile
AI Source
```

造成概念漂移。

---

# 285. CAS Profile 命名

代码中：

```text
CasProfile / Profile
```

与 Codex 自带：

```text
CodexProfile
```

必须明确区分。

特别是在：

```text
cas-codex
```

中禁止两个都叫：

```text
ProfileConfig
```

导致混淆。

---

# 286. Runtime Agent 与 Domain Agent

如以后需要同时表达：

```text
CAS Agent Definition
Codex Running Agent Thread
```

必须使用不同类型。

例如：

```text
AgentDefinition
SpawnedAgentSession
```

不能都叫：

```text
Agent
```

如果当前只有 CAS Domain Agent，则保持简单。

---

# 287. Status 命名

不要定义十几个：

```text
Status
```

无上下文类型。

使用：

```text
ProviderAvailability
ModelLifecycle
CompatibilityLevel
ApplyTransactionStatus
SnapshotStatus
```

---

# 288. Mapper 命名

例如：

```text
CodexAgentConfigMapper
ProviderRowMapper
ProviderResponseMapper
```

明确目标和来源。

不要：

```text
CommonMapper
ModelMapper
```

承担十种转换。

---

# 289. Service 命名

`Service` 只用于：

```text
真正协调一组业务能力
```

例如：

```text
ConfigurationApplicationService
SecretService
```

简单 Value Object 工具不要：

```text
UrlService
KeyService
```

滥用 Service。

---

# 290. Manager 命名

除：

```text
BackupManager
ModelCatalogManager
```

这类已有明确资源生命周期语义外，避免：

```text
XxxManager
```

泛化。

优先：

```text
Compiler
Reader
Writer
Resolver
Registry
Repository
Adapter
Service
```

表达真实职责。

---

# 291. Utility 模块

确实纯技术通用：

```text
hash
path
serialization
```

可以存在小型 Utility。

但必须：

```text
无业务依赖
职责单一。
```

---

# 292. Constants

固定协议值：

```text
集中定义
```

不要：

```rust
"responses"
```

散落几十处。

但如果能通过：

```text
Enum
```

表达，优先 Enum。

---

# 293. Environment Variable

统一定义：

```text
CODEX_HOME
```

等环境变量名称。

不在多个文件复制字符串。

---

# 294. File Name Constants

如：

```text
config.toml
agents
```

由：

```text
cas-codex
```

集中管理。

---

# 295. Preset Schema Constants

属于：

```text
cas-provider
```

而不是：

```text
cas-core
```

---

# 296. Error Code Constants

推荐通过：

```text
typed enum
```

而不是：

```text
pub const ERROR_X: &str
```

散落。

序列化时再映射稳定字符串。

---

# 297. Serialization Stability

跨 IPC 或 Export 的 Enum：

```text
显式 rename
```

避免 Rust Variant 改名直接改变外部格式。

例如：

```rust
#[serde(rename = "GATEWAY_REQUIRED")]
GatewayRequired,
```

---

# 298. Internal Refactor

允许内部：

```text
Rust enum variant
```

重命名，

但外部序列化值已经发布后应保持稳定或版本化。

---

# 299. Feature Completion

一个功能不能只做到：

```text
Happy Path
```

就视为完成。

至少要处理：

```text
Not Found
Conflict
Invalid Input
External Error
Persistence Failure
```

但完整测试矩阵由测试规范定义。

---

# 300. 最终代码结构原则

整个项目最终应该保持：

```text
React
  ↓
Tauri IPC
  ↓
Application
  ↓
Domain
  ↓
Ports
  ↑
Infrastructure
```

Codex：

```text
Application
   ↓
cas-codex
   ↓
Codex Filesystem
```

Provider：

```text
Application
   ↓
cas-provider
   ↓
Provider API
```

Credential：

```text
Application
   ↓
cas-secret
   ↓
cas-platform
   ↓
OS Secret Store
```

Persistence：

```text
Application
   ↓
Repository Port
   ↑
cas-storage
   ↓
SQLite
```

---

# 301. 核心代码不变量

## 不变量一

```text
Domain 不依赖 Framework。
```

## 不变量二

```text
React 不实现业务规则。
```

## 不变量三

```text
Tauri Command 必须保持薄。
```

## 不变量四

```text
CLI 不直接访问 SQLite 或 Codex 文件。
```

## 不变量五

```text
Codex-specific 配置字段只存在于 cas-codex 边界。
```

## 不变量六

```text
Provider-specific 行为不得扩散到 Agent / Profile / UI Core。
```

## 不变量七

```text
Repository 不泄漏 SQLx 类型。
```

## 不变量八

```text
Provider Adapter 不修改业务状态。
```

## 不变量九

```text
Configuration Compiler 不执行 I/O。
```

## 不变量十

```text
Save 与 Apply 必须保持分离。
```

## 不变量十一

```text
Diagnostics 默认只读。
```

## 不变量十二

```text
生产业务错误不得使用 panic。
```

## 不变量十三

```text
Secret 不得作为普通 String 在整个系统传播。
```

## 不变量十四

```text
用户输入永远不能被直接用于 SQL、Shell 或文件命令拼接。
```

## 不变量十五

```text
跨平台差异必须集中于 Platform Layer。
```

## 不变量十六

```text
相同输入的配置编译结果必须稳定。
```

## 不变量十七

```text
未知外部数据不得通过 silent default 被伪装成合法状态。
```

## 不变量十八

```text
业务 Entity、Persistence Row、IPC DTO、External API DTO、Codex DTO 必须保持边界。
```

## 不变量十九

```text
新增 Provider 优先新增数据，而不是新增 Provider-specific 核心代码。
```

## 不变量二十

```text
不得为了未来功能提前破坏当前模块职责。
```

---

# 302. AI 开发执行检查表

任何 AI Agent 在完成代码修改前至少自行检查：

```text
[ ] 修改是否位于正确 crate / feature

[ ] 是否违反依赖方向

[ ] 是否新增了重复业务规则

[ ] 是否出现 Provider / Model 名称硬编码判断

[ ] 是否绕过 Application Layer

[ ] 是否直接修改 Codex 文件

[ ] 是否直接访问 Secret Store

[ ] 是否使用 unwrap / panic 处理外部失败

[ ] 是否存在 Secret 日志风险

[ ] 是否使用 String 代替已有 Strong Type

[ ] 是否新增魔法字符串

[ ] 是否存在无意义 clone

[ ] 是否存在跨 await 长时间持锁

[ ] 是否把外部 DTO 当 Domain Entity

[ ] 是否把 Database Row 暴露到上层

[ ] 是否新增不必要依赖

[ ] 是否改变持久化结构但遗漏 Migration

[ ] 是否破坏 deterministic configuration output

[ ] 是否扩大了原任务范围

[ ] 是否留下 TODO、死代码或假实现
```

---

# 303. 最终设计结论

CAS 的代码质量不依赖：

```text
复杂框架
大量 Design Pattern
超高抽象程度
```

而依赖以下长期稳定约束：

```text
领域边界清晰

依赖方向单向

外部系统全部隔离

业务状态只有一个 Source of Truth

Provider / Model / Agent 保持解耦

Codex-specific 逻辑集中

Secret-specific 逻辑集中

GUI 和 CLI 共用 Application Layer

错误通过稳定类型传播

配置生成保持 deterministic

高风险副作用显式发生
```

开发人员和 AI Agent 在面对实现选择时，应优先选择：

```text
更容易理解
更容易验证
更少隐藏行为
更少跨模块影响
```

的方案。

任何局部实现如果看起来更方便，但要求：

```text
UI 直接改数据库

Provider 直接改 Agent

Repository 直接操作 Codex

Config Writer 读取 Secret

Domain 引入 Tauri / SQLx

Generic Adapter 判断 provider == "deepseek"
```

都应视为架构边界正在被破坏，而不是“为了效率做的小优化”。

CAS V0.1 的代码规范最终目标是：

> 即使项目由多个开发者和多个 AI Agent 分阶段实现，也能够让每个模块只理解自己应该理解的内容，并让错误实现尽可能在编译期、类型边界、模块边界和代码审查阶段被阻止，而不是在用户的 Codex 配置被破坏后才发现问题。
