# Codex Agent Switch Codex 配置集成规范

> 文档类型：Codex Configuration Integration Specification  
> 项目暂定名称：Codex Agent Switch  
> 简称：CAS  
> 当前目标版本：V0.1  
> 文档职责：定义 CAS 如何发现、读取、解释、编译、修改、验证、备份、恢复 Codex 配置，以及 CAS 与外部 Codex 配置之间的 Ownership、冲突和兼容性边界。

---

# 0. V0.1 配置基线

V0.1 只编译 Codex 用户级 Custom Provider、用户级 `agents/*.toml` 与 Agent 引用的 Model Catalog。首个目标是 `deepseek-v4-flash`，其官方配置为 `base_url = "https://api.deepseek.com/"`、`wire_api = "responses"`；Custom Responses Provider 使用同一编译器，不存在 DeepSeek 专用写入器。

DeepSeek V4 Flash Model Definition 必须携带官方 Catalog 所需字段与 `minimal_client_version = 0.144.0` 基线；实际 Apply 仍以 Codex Feature Probe 为主，版本表为辅。

V0.1 只编译 Base Binding，不实现 CAS Profile Compilation。正式平台为 Windows，`auth.command` 必须由安装环境注入稳定的 `cas-helper.exe` 绝对路径；Preset 不保存 executable 或 Secret。DeepSeek 官方明文 `experimental_bearer_token` 示例只作为协议证据，CAS 明确不生成该字段。

Apply 的 P0 前置门禁包括真实 per-agent provider/model、生效的 command-backed credential、非破坏式多文件 patch / conflict / restore，以及安装升级后的 helper 路径稳定性。

官方参考：<https://api-docs.deepseek.com/quick_start/agent_integrations/codex/>

---

# 1. 文档目标

CAS 不直接参与 Codex Agent Runtime。

CAS 对 Codex 的核心职责是：

```text
CAS Internal State
        ↓
Configuration Compiler
        ↓
Configuration Plan
        ↓
Ownership / Conflict Validation
        ↓
Backup
        ↓
Configuration Writer
        ↓
Codex Configuration
        ↓
Validation
```

本规范解决以下问题：

```text
Codex 配置在哪里？

哪些配置 CAS 可以修改？

哪些配置 CAS 只能读取？

Provider 应写在哪里？

Agent 应写在哪里？

Model Catalog 如何关联？

怎样避免覆盖用户已有配置？

怎样识别外部修改？

怎样实现原子写入？

Apply 中途失败怎么办？

CAS 删除 Provider / Agent 时如何处理文件？

Codex 升级导致字段变化时怎么办？
```

---

# 2. 配置集成基本原则

CAS 与 Codex 集成必须长期遵循以下原则。

## 2.1 非破坏式修改

CAS 不拥有完整 Codex 配置。

必须：

```text
读取原配置
    ↓
保留未知内容
    ↓
只修改 CAS-owned 部分
```

禁止：

```text
CAS State
    ↓
重新生成整个 config.toml
    ↓
覆盖用户 config.toml
```

---

## 2.2 CAS State 与 Codex State 分离

CAS 内部状态是：

```text
Desired State
```

Codex 当前文件状态是：

```text
Observed / Applied State
```

二者不得混淆。

关系：

```text
Desired State
     │
     │ Compile
     ▼
Configuration Plan
     │
     │ Apply
     ▼
Codex State
```

---

## 2.3 Read Before Write

任何 Codex 配置写入操作都必须：

```text
重新读取当前磁盘状态
```

不得直接根据 CAS 启动时缓存的配置写入。

即：

```text
错误：

CAS 启动
↓
读取 config.toml
↓
用户外部修改 config.toml
↓
CAS 使用旧对象写回


正确：

CAS 启动
↓
读取

...

Apply
↓
重新读取最新文件
↓
冲突检测
↓
再写入
```

---

## 2.4 Backup Before Mutation

任何会修改 Codex 文件系统状态的 Apply 必须：

```text
先 Backup
再 Write
```

而不是：

```text
Write
失败后再想办法备份
```

---

## 2.5 Unknown Means Preserve

对于 CAS 不认识的：

```text
TOML Key
Table
Agent Field
Provider Field
Future Codex Field
```

默认规则：

```text
Preserve
```

不得因为：

```text
CAS 当前版本不知道它是什么
```

就将其删除。

---

# 3. 当前 Codex 配置结构

当前 Codex 用户级配置位于：

```text
$CODEX_HOME/config.toml
```

其中：

```text
CODEX_HOME
```

默认：

```text
~/.codex
```

Codex 同时支持项目级：

```text
<project>/.codex/config.toml
```

以及用户级 Profile：

```text
$CODEX_HOME/<profile-name>.config.toml
```

Codex 当前配置优先级从高到低为：

```text
CLI flags / --config
        ↓
Project .codex/config.toml
        ↓
Selected Codex Profile
        ↓
User config.toml
        ↓
System config
        ↓
Built-in defaults
```

项目级配置只在项目被信任时加载。

---

# 4. CAS 使用的 Codex 配置范围

V0.1 将 Codex 配置分成四类。

```text
Codex Configuration
│
├── User Global Config
│   └── $CODEX_HOME/config.toml
│
├── User Agent Config
│   └── $CODEX_HOME/agents/*.toml
│
├── CAS Model Catalog
│   └── CAS-managed model catalog files
│
└── External Configuration
    ├── project .codex/
    ├── Codex profile config
    ├── system config
    └── requirements.toml
```

CAS 对这些区域具有不同权限。

---

# 5. User Global Config

文件：

```text
$CODEX_HOME/config.toml
```

CAS V0.1 主要在这里管理：

```text
model_providers.<cas-provider-id>
```

必要情况下可以管理有限的：

```text
agents
```

全局 Multi-Agent 配置。

但默认不修改：

```text
model
model_provider
approval_policy
sandbox_mode
mcp_servers
projects
notifications
telemetry
TUI
memories
hooks
其他用户设置
```

特别是：

```text
model
model_provider
```

CAS 不应为了配置 Subagent 而修改 Primary Agent 的默认模型。

---

# 6. Provider 必须配置在用户级

Codex 当前明确禁止项目级：

```text
.codex/config.toml
```

覆盖：

```text
model_provider
model_providers
```

等机器级 Provider / Auth 配置。

因此 CAS 管理的第三方 Provider 必须注册于：

```text
$CODEX_HOME/config.toml
```

而不是：

```text
<project>/.codex/config.toml
```



---

# 7. CAS 不管理 Primary Model

CAS V0.1 的核心职责是：

```text
Subagent Model Management
```

因此正常 Apply 不修改：

```toml
model = "..."
model_provider = "..."
```

这两个用户级顶层字段。

例如用户原有：

```toml
model = "gpt-5.6"
```

Apply 前：

```text
Primary → GPT-5.6
```

CAS 为 Executor 配置 DeepSeek 后：

```text
Primary → GPT-5.6

Executor → DeepSeek V4 Flash
```

Primary 配置必须保持不变。

---

# 8. Custom Agent 配置目录

Codex 当前支持用户级 Custom Agent：

```text
$CODEX_HOME/agents/*.toml
```

以及项目级：

```text
<project>/.codex/agents/*.toml
```

每个 standalone Agent 文件定义一个 Custom Agent。

当前最低必需字段：

```text
name
description
developer_instructions
```

Custom Agent 文件本身作为 spawned session 的配置层，可以覆盖普通 Codex Session 支持的配置，例如：

```text
model
model_reasoning_effort
sandbox_mode
mcp_servers
skills.config
```



---

# 9. CAS V0.1 Agent Scope

V0.1 默认只管理：

```text
$CODEX_HOME/agents/
```

下的用户级 Agent。

不主动修改：

```text
<project>/.codex/agents/
```

原因：

```text
CAS V0.1 定位
=
用户全局 Agent Team 管理器
```

而不是：

```text
项目仓库配置管理器
```

---

# 10. 项目级 Agent

CAS 可以检测：

```text
<project>/.codex/agents/*.toml
```

但在 V0.1 中默认：

```text
Read Only
```

不得自动：

```text
修改
删除
迁移
接管
```

项目级 Agent 管理可以在后续版本单独扩展。

---

# 11. Agent 文件命名规则

CAS 自己生成的 Agent 文件推荐统一：

```text
cas-<agent-key>.toml
```

例如：

```text
cas-executor.toml
cas-reviewer.toml
cas-tester.toml
```

文件名用于：

```text
CAS Resource Identification
```

但不用于决定 Codex Agent 身份。

Codex 当前以：

```text
name
```

字段识别 Custom Agent，文件名仅是约定。

---

# 12. Agent Name 是真实身份

例如：

```toml
name = "executor"
```

则：

```text
executor
```

才是 Codex 中真正的 Agent Role Identity。

因此 CAS 在生成 Agent 前必须扫描：

```text
所有可见 Custom Agent
```

中的：

```text
name
```

而不能只检查：

```text
executor.toml
```

是否存在。

---

# 13. Agent Name 冲突

例如：

```text
用户已有：
my-agent.toml

name = "executor"
```

CAS 准备创建：

```text
cas-executor.toml

name = "executor"
```

这是：

```text
Agent Identity Conflict
```

CAS 不得直接创建。

必须中止该 Agent 的 Apply。

---

# 14. Built-in Agent 冲突

Codex 当前内建：

```text
default
worker
explorer
```

如果用户定义同名 Custom Agent：

```text
explorer
```

Custom Agent 会优先于 built-in Agent。

因此 CAS 创建：

```text
explorer
```

必须被视为：

```text
有意覆盖 Built-in Role
```

而不是普通无影响操作。

CAS 应明确知道：

```text
default
worker
explorer
```

属于 reserved built-in agent identities。

---

# 15. CAS Agent 文件基本映射

CAS Agent：

```text
Agent
+
Model Binding
+
Provider Binding
```

编译为：

```toml
name = "executor"

description = """
Implementation-focused worker.
"""

model = "deepseek-v4-flash"
model_provider = "cas_deepseek"

model_reasoning_effort = "high"

sandbox_mode = "workspace-write"

developer_instructions = """
...
"""
```

其中：

```text
name
description
developer_instructions
```

来源于 Agent。

```text
model
model_provider
```

来自当前 Model Binding。

```text
model_reasoning_effort
sandbox_mode
```

来自有效 Agent 配置。

---

# 16. Agent Config 必须自包含模型绑定

为了保证：

```text
Primary Provider
```

与：

```text
Subagent Provider
```

彼此独立，CAS Agent 文件必须明确写入：

```toml
model = "..."
model_provider = "..."
```

不能依赖 Parent Session：

```text
model_provider
```

继承。

Custom Agent 本身是 spawned-session configuration layer；显式模型配置优先于默认继承规则。

---

# 17. 不依赖 `[agents].default_subagent_model`

Codex 支持：

```toml
[agents]
default_subagent_model = "..."
```

但 CAS 不使用它实现不同 Agent 的模型绑定。

因为：

```text
CAS
需要：

Executor → Model A
Reviewer → Model B
Explorer → Model C
```

而：

```text
default_subagent_model
```

是全局默认。

因此 CAS Agent Binding 应通过：

```text
每个 Agent 自己的 config layer
```

实现。

---

# 18. `[agents]` 全局设置

Codex 当前全局 Multi-Agent 配置包括：

```text
agents.enabled
agents.max_concurrent_threads_per_session
agents.default_subagent_model
agents.default_subagent_reasoning_effort
agents.interrupt_message
```



CAS V0.1 默认：

```text
只读取这些配置
```

不主动覆盖用户设置。

---

# 19. `agents.enabled`

Codex 当前：

```text
agents.enabled
```

默认：

```text
true
```

因此 CAS 不需要为了启用 Agent 无条件写入：

```toml
[agents]
enabled = true
```



原则：

> 如果 Codex 默认已经满足需求，不写冗余配置。

---

# 20. Multi-Agent 被显式关闭

如果用户已有：

```toml
[agents]
enabled = false
```

CAS 不得静默修改为：

```toml
enabled = true
```

应判断：

```text
Multi-Agent explicitly disabled
```

并要求用户显式决定是否启用。

---

# 21. CAS 不覆盖并发限制

如果用户已有：

```toml
[agents]
max_concurrent_threads_per_session = 8
```

CAS 不得因为自己的默认值是：

```text
4
```

而修改它。

V0.1 默认不拥有：

```text
max_concurrent_threads_per_session
```

---

# 22. Runtime Override 注意事项

Custom Agent 文件中的 Sandbox 不是绝对权限边界。

Codex 当前会在 Spawn Child 时重新应用 Parent Turn 的实时权限和 Sandbox Override，例如：

```text
/permissions
--yolo
```

这些运行时 Override 可能覆盖 Custom Agent 文件的默认 Sandbox。

因此 CAS 只能描述：

```text
Agent Default Sandbox Policy
```

不能向用户保证：

```text
Agent 永远以该 Sandbox 运行
```

---

# 23. Provider 配置

CAS Provider 编译到：

```toml
[model_providers.<provider-id>]
```

Codex 当前 Custom Provider 支持：

```text
name
base_url
wire_api
env_key
auth
http_headers
env_http_headers
query_params
request_max_retries
stream_max_retries
stream_idle_timeout_ms
supports_standalone_web_search
supports_websockets
...
```

当前 `wire_api` 唯一支持值为：

```text
responses
```



---

# 24. CAS Provider ID

CAS 生成的 Provider ID 必须拥有独立命名空间。

推荐：

```text
cas_<provider-key>
```

例如：

```text
cas_deepseek
cas_openrouter
cas_company_proxy
```

不得直接创建：

```text
deepseek
openrouter
```

避免与用户已有配置冲突。

---

# 25. Codex Reserved Provider ID

Codex 当前保留：

```text
openai
ollama
lmstudio
```

Custom Provider 不允许覆盖这些 ID。

CAS Provider ID 生成器必须保证：

```text
永远不会生成这些 reserved IDs。
```

---

# 26. Provider Ownership

以下：

```toml
[model_providers.cas_deepseek]
```

可以归 CAS 所有。

而：

```toml
[model_providers.deepseek]
```

如果安装 CAS 前已经存在，则默认：

```text
External-owned
```

CAS 不得因为它与自己的 Provider 语义相同就接管。

---

# 27. 不复用 External Provider 作为默认行为

假设用户已有：

```toml
[model_providers.deepseek]
...
```

然后在 CAS 中新增：

```text
DeepSeek
```

V0.1 默认应创建：

```toml
[model_providers.cas_deepseek]
```

而不是直接修改：

```text
model_providers.deepseek
```

除非未来用户执行：

```text
Manage with CAS / Import
```

---

# 28. Provider Config 示例

逻辑示例：

```toml
[model_providers.cas_deepseek]
name = "DeepSeek"
base_url = "https://api.deepseek.com/"
wire_api = "responses"

[model_providers.cas_deepseek.auth]
command = "C:/Program Files/Codex Agent Switch/bin/cas-helper.exe"
args = ["token", "<credential-id>"]
timeout_ms = 5000
refresh_interval_ms = 300000
```

此配置表达：

```text
Codex
   ↓
CAS Provider
   ↓
cas-helper
   ↓
System Secret Store
```

---

# 29. Provider Credential 集成

Codex 当前支持 Command-backed Bearer Token。

配置：

```toml
[model_providers.<id>.auth]
command = "..."
args = [...]
```

Codex 需要 Token 时调用该命令，并要求 Token 输出到：

```text
stdout
```

Auth Command：

```text
没有 stdin
空 token 视为错误
```

同时不得和：

```text
env_key
experimental_bearer_token
requires_openai_auth
```

混用。

---

# 30. CAS 默认 Credential Strategy

V0.1 默认采用：

```text
Command-backed Authentication
```

即：

```text
Codex
   ↓
cas-helper
   ↓
OS Credential Store
```

而不是：

```text
API Key → config.toml
```

---

# 31. 禁止写入明文 Token

CAS 不得生成：

```toml
experimental_bearer_token = "sk-xxxx"
```

官方当前也明确将 direct bearer token 标记为 discouraged。

---

# 32. `env_key` 支持

V0.1 可以保留：

```text
Environment Variable Credential
```

高级模式。

例如：

```toml
env_key = "DEEPSEEK_API_KEY"
```

但：

```text
不是 CAS 默认 Credential Strategy。
```

---

# 33. Auth Strategy 必须互斥

一个 CAS-managed Provider 最终只能生成一种认证方案。

例如：

```text
COMMAND
```

则不得同时生成：

```text
env_key
experimental_bearer_token
requires_openai_auth
```

---

# 34. Static Header 与 Secret Header

非敏感 Header：

```toml
http_headers = {
    "X-App" = "CAS"
}
```

可以写入配置。

敏感 Header 必须使用：

```text
env_http_headers
```

或者未来由 Gateway / Credential Helper 解决。

不得：

```toml
http_headers = {
    "X-API-Key" = "secret"
}
```

---

# 35. Model Catalog

Codex 当前支持：

```toml
model_catalog_json = "/path/to/catalog.json"
```

并在启动时加载该 Model Catalog。

但该字段属于：

```text
Session Config
```

而不是 Provider Definition 内部字段。

---

# 36. Model Catalog 使用原则

CAS 必须区分：

```text
CAS Model Metadata
```

和：

```text
Codex Runtime Model Catalog
```

前者是 CAS 自己的模型能力数据。

后者是：

```text
Codex 运行时需要加载的 JSON
```

两者不得视为同一个文件。

V0.1 生成给第三方 Responses Provider 的 Codex Runtime Model Catalog 时，必须使用 `multi_agent_version = "v1"`。V2 的委派正文当前通过 `agent_message.encrypted_content` 传递，第三方 Provider 无法读取；不得仅因 Provider 支持 Responses API 就声明 V2 兼容。

---

# 37. Agent-specific Model Catalog

Custom Agent 是独立配置层，因此 CAS 可以在必要时生成：

```toml
model_catalog_json = "/.../cas/catalogs/deepseek.json"
```

到 Agent Config。

这样：

```text
Executor
```

可以使用 Provider A 的 Catalog；

```text
Reviewer
```

可以使用 Provider B 的 Catalog。

而不修改 Primary Session 的：

```text
model_catalog_json
```

---

# 38. Catalog 不写入全局配置作为默认方案

CAS 不应为了某个 Subagent 修改：

```text
$CODEX_HOME/config.toml
```

顶层：

```toml
model_catalog_json = "..."
```

因为这会影响：

```text
Primary Agent
```

及其他 Session。

V0.1 优先：

```text
Agent-specific catalog
```

---

# 39. Catalog 文件目录

CAS-generated Catalog 建议存放于：

```text
$CODEX_HOME/cas/model-catalogs/
```

例如：

```text
$CODEX_HOME/
│
├── config.toml
├── agents/
│   └── cas-executor.toml
│
└── cas/
    └── model-catalogs/
        └── deepseek.json
```

避免和用户自有：

```text
model-catalogs/
```

混用。

---

# 40. CAS Resource Namespace

所有 CAS 独占文件应位于明确 namespace：

```text
$CODEX_HOME/cas/
```

例如：

```text
cas/
├── model-catalogs/
├── state/
├── generated/
└── backups/
```

Custom Agent 因 Codex discovery 约束需要存在：

```text
$CODEX_HOME/agents/
```

因此 Agent 使用：

```text
cas-*.toml
```

进行文件级识别。

---

# 41. CAS 不使用 Codex Profile 实现 CAS Profile（P1）

第 41—44 节定义后续 CAS Profile 与 Codex Profile 的边界；V0.1 只使用 Base Binding，不执行 Profile Compilation。

这是 V0.1 的重要设计决策。

CAS 自己的：

```text
Profile
```

例如：

```text
Budget
Balanced
Quality
```

不直接映射为：

```text
$CODEX_HOME/<profile>.config.toml
```

---

# 42. 原因

Codex Profile 当前通过：

```text
--profile profile-name
```

选择，并且 Profile 文件属于独立配置层。当前配置优先级中，它位于项目配置之下、用户 config 之上。

如果 CAS Profile 直接使用 Codex Profile：

```text
用户还必须确保每次启动 Codex
都选择正确 --profile
```

这违背：

```text
CAS Apply 后即可使用
```

的产品目标。

---

# 43. CAS Profile 的正确语义

CAS：

```text
Balanced
```

是：

```text
Desired Agent Binding Set
```

激活并 Apply 后：

```text
CAS Compiler
   ↓
更新 CAS-owned Agent Files
   ↓
Codex 默认看到新 Agent Binding
```

不要求用户改变 Codex 启动方式。

---

# 44. 已有 Codex Profile

用户原本存在：

```text
~/.codex/deep-review.config.toml
```

CAS：

```text
只检测
不修改
```

V0.1 不尝试让所有 Codex Profile 都同步 CAS Agent Team。

---

# 45. 项目级配置处理

CAS V0.1 不修改：

```text
<repo>/.codex/config.toml
```

但在 Diagnostics 中可以识别它可能对运行结果产生的影响。

例如：

```text
Project Config
    ↓
sandbox_mode
```

优先级高于用户级配置。

---

# 46. Project Config Provider 限制

项目配置即使包含：

```text
model_provider
model_providers
```

Codex 当前也会忽略这些字段，并输出启动 Warning。

CAS 不应根据项目文件中的这些字段判断真实 Provider 状态。

---

# 47. Requirements 配置

组织管理环境可能存在：

```text
requirements.toml
```

它属于：

```text
Admin-enforced configuration
```

用户不能覆盖其中的受限设置。

例如管理员可以限制：

```text
sandbox_mode
permission profiles
multi_agent
```



---

# 48. CAS 不修改 Requirements

CAS 对：

```text
requirements.toml
```

永远：

```text
Read Only
```

不得：

```text
修改
删除
绕过
替换
```

---

# 49. Multi-Agent 被管理员禁用

如果：

```toml
[features]
multi_agent = false
```

被 Managed Requirements 强制指定，则 CAS 必须报告：

```text
MULTI_AGENT_ADMIN_DISABLED
```

CAS 不允许尝试通过修改：

```text
config.toml
```

绕过管理员限制。

---

# 50. Sandbox 管理限制

如果 Agent 请求：

```text
workspace-write
```

但 Requirements 只允许：

```text
read-only
```

CAS 应认为：

```text
Requested configuration cannot become effective
```

而不是生成一个看似成功、实际上永远不会生效的配置。

---

# 51. Configuration Source Classification

CAS 扫描 Codex 配置后，每项应被内部分类为：

```text
CAS_OWNED

EXTERNAL_USER

PROJECT

SYSTEM

MANAGED_REQUIREMENT

BUILT_IN

UNKNOWN
```

该分类用于：

```text
Ownership
Conflict Detection
Diagnostics
Apply
```

---

# 52. CAS-owned Provider 判定

Provider 满足以下条件之一时才可以认定为 CAS-owned：

```text
1. CAS ManagedResource 中已有明确记录

或

2. Provider ID 使用 CAS namespace，
   并能够通过 CAS metadata/fingerprint 确认
```

仅：

```text
ID 以 cas_ 开头
```

不足以无条件证明 Ownership。

---

# 53. CAS-owned Agent 判定

Agent 文件满足：

```text
ManagedResource 明确记录
+
文件路径匹配
+
Agent name 匹配
```

才视为：

```text
CAS-owned
```

---

# 54. 不依赖文件注释作为唯一 Ownership

CAS 可以生成：

```toml
# Managed by Codex Agent Switch
```

但：

```text
comment
```

只能作为辅助。

不得作为唯一 Ownership 依据。

原因：

```text
用户可能复制文件
格式化工具可能删除注释
```

---

# 55. Managed Fingerprint

每个 CAS-owned 资源 Apply 成功后记录：

```text
semanticHash / fragmentHash
```

可另外记录 `contentHash` 作为字节级诊断信息，但不得用于 Ownership 或 Conflict 判定。

例如语义指纹算法：

```text
SHA-256
```

用于：

```text
External Modification Detection
```

---

# 56. Shared File Fingerprint

对于：

```text
config.toml
```

不能只记录：

```text
整个文件 hash
```

然后把任何变化都视为冲突。

因为用户可能只修改：

```text
MCP
TUI
其他无关配置
```

因此必须同时维护：

```text
Whole File Fingerprint
+
CAS-owned Fragment Fingerprints
```

---

# 57. Fragment Fingerprint

例如 CAS 管理：

```toml
[model_providers.cas_deepseek]
```

则记录：

```text
Logical Path:
model_providers.cas_deepseek

Canonical Fragment Hash:
abc...
```

Apply 时：

```text
当前 Fragment Hash
       ↓
是否与 Last Applied Hash 相同？
```

---

# 58. 无关外部修改

例如用户修改：

```toml
[mcp_servers.xxx]
```

但：

```text
CAS Provider Fragment
```

完全没变。

此时：

```text
不是冲突
```

CAS 应重新基于最新 AST Patch。

---

# 59. CAS-owned Fragment 被外部修改

例如 CAS 上次写入：

```toml
[model_providers.cas_deepseek]
base_url = "A"
```

用户手工改成：

```toml
base_url = "B"
```

则：

```text
CAS fragment externally modified
```

Apply 必须产生：

```text
Conflict
```

不得覆盖 B。

---

# 60. Agent 文件外部修改

CAS-owned：

```text
cas-executor.toml
```

Hash 发生变化时：

```text
External Modification
```

默认停止更新该 Agent。

用户需要显式选择：

```text
Keep External
Use CAS Version
Import Changes
```

具体 UI 不由本文档规定。

---

# 61. Configuration Reader

Reader 必须能够：

```text
定位 CODEX_HOME
读取 config.toml
读取 agents/*.toml
读取相关 catalog
读取 requirements 约束信息
读取 Codex version
```

Reader 不能：

```text
写文件
自动修复
删除损坏配置
```

---

# 62. Parser 原则

对于：

```text
config.toml
```

必须使用：

```text
Round-trip preserving TOML parser/editor
```

Rust 推荐：

```text
toml_edit
```

目的：

```text
尽量保留：

Comments
Ordering
Formatting
Unknown Fields
```

不得使用简单：

```text
Deserialize
↓
修改 Struct
↓
Serialize entire document
```

作为 shared config 的主要更新方案。

---

# 63. Strongly Typed + AST 双模型

推荐同时维护：

```text
TOML AST
```

和：

```text
Known Codex View
```

结构：

```text
Raw TOML AST
      │
      ├── Preserve Unknown
      │
      ▼
Known Config Projection
```

Known Projection 用于：

```text
业务判断
Validation
Compatibility
```

AST 用于：

```text
Patch
Round-trip Write
```

---

# 64. Parse Error

如果：

```text
config.toml
```

自身已经无法解析：

```text
CAS 不得尝试修复并覆盖。
```

返回：

```text
CODEX_CONFIG_PARSE_ERROR
```

允许用户：

```text
查看错误
打开文件
Restore
```

---

# 65. Agent Parse Error

External Agent 文件解析失败：

```text
记录诊断
不修改该文件
```

CAS-owned Agent 文件解析失败：

```text
视为资源异常
```

Apply 前必须停止涉及该 Agent 的修改。

---

# 66. Configuration Compiler

Compiler 输入：

```text
Desired CAS State

+

Observed Codex State

+

Ownership State

+

Codex Capabilities

+

Managed Requirements
```

输出：

```text
ConfigurationPlan
```

Compiler 本身：

```text
Pure / No filesystem mutation
```

---

# 67. Configuration Plan

Plan 至少包含：

```text
Create
Update
Delete
NoOp
Conflict
Blocked
```

每个 Operation 包含：

```text
Resource
Logical Owner
Target
Before Fingerprint
After Representation
Reason
```

---

# 68. 示例 Plan

```text
UPDATE
config.toml
model_providers.cas_deepseek

CREATE
agents/cas-executor.toml

CREATE
cas/model-catalogs/deepseek.json

NO_OP
agents/cas-reviewer.toml
```

---

# 69. Apply 前 Plan 必须稳定

Apply 开始时必须基于：

```text
当前磁盘重新读取结果
```

重新生成 Configuration Plan。

不能使用：

```text
10 分钟前 Review 页面计算出的 Plan
```

直接写入。

Review Plan 只能作为：

```text
Preview
```

Apply 时必须再次编译。

---

# 70. Apply Pipeline

标准 Apply：

```text
1. Acquire CAS Apply Lock

2. Detect CODEX_HOME

3. Read latest Codex state

4. Parse

5. Read ownership metadata

6. Detect external modifications

7. Read managed requirements

8. Validate desired state

9. Compile configuration plan

10. Reject conflicts / blocked operations

11. Create backup

12. Prepare temporary files

13. Validate generated content

14. Write CAS-exclusive resources

15. Patch shared config

16. Validate resulting state

17. Commit ownership metadata

18. Release lock

19. Mark state Applied
```

---

# 71. Apply Lock

CAS 必须防止：

```text
两个 CAS Apply
```

同时发生。

需要跨进程：

```text
Apply Lock
```

而不只是进程内 Mutex。

---

# 72. Lock Scope

建议 Lock 针对：

```text
CODEX_HOME
```

而不是单文件。

因为一次 Apply 可能同时修改：

```text
config.toml
agents/*.toml
catalog
CAS metadata
```

---

# 73. 不锁 Codex 本身

CAS 不要求：

```text
Codex 必须退出
```

才能写配置。

但如果当前 Codex 版本只在：

```text
Startup
```

读取部分配置，则 Apply 后应视为：

```text
配置已写入
但当前既有 Session 未必重新加载
```

CAS 不得假装可以控制已经启动的 Codex Session。

---

# 74. Backup Scope

Backup 至少覆盖：

```text
所有即将被修改或删除的文件
```

例如：

```text
config.toml
agents/cas-executor.toml
cas/model-catalogs/deepseek.json
```

---

# 75. 不存在文件也要记录

如果 Apply 将：

```text
CREATE agents/cas-executor.toml
```

Backup Manifest 应记录：

```text
existsBefore = false
```

这样 Restore 才知道：

```text
应该删除这个新增文件
```

而不是找不到旧内容。

---

# 76. Backup 时机

Backup 必须发生于：

```text
Conflict Validation
```

之后、

```text
任何实际 Mutation
```

之前。

避免每次用户点击 Apply 都制造无效备份。

Backup 不能替代写入前置条件。每个目标执行 `atomic replace` 前必须重新读盘：

```text
Shared file：完整输入字节未变化；否则放弃当前 render，重新 Parse / Plan
Exclusive file：当前 semanticHash 与 Plan precondition 一致
Mismatch：立即中止后续 Mutation，并按 Journal 进入 Rollback / Recovery
```

这条 compare-and-abort 检查用于封闭“Conflict Validation 完成后、真正替换前”窗口；CAS 不宣称能与不合作的外部写入者实现跨文件原子事务。

---

# 77. 临时文件写入

禁止直接：

```text
truncate target
↓
write
```

推荐：

```text
render new content
↓
write temporary file
↓
flush
↓
validate temp
↓
atomic replace
```

---

# 78. Shared `config.toml` 更新

对于：

```text
config.toml
```

流程：

```text
Parse current AST
        ↓
Patch CAS-owned nodes
        ↓
Render
        ↓
Parse rendered result again
        ↓
Temp file
        ↓
Atomic replace
```

---

# 79. Agent 文件更新

CAS-owned Agent 是独占资源，可以：

```text
完整重新生成文件
```

因为：

```text
CAS 拥有完整文件
```

但前提：

```text
未检测到外部修改冲突。
```

---

# 80. Catalog 更新

CAS-owned Catalog 同样属于：

```text
exclusive resource
```

允许完整重新生成。

必须：

```text
write temp
validate JSON
atomic replace
```

---

# 81. Mutation Ordering

一次 Apply 包含：

```text
Provider
Catalog
Agent
```

时，不使用单一固定顺序，而按引用依赖生成 Mutation DAG：

```text
CREATE：Shared config.toml Provider → Catalog → Agent Config
DELETE：Agent Config → unused Catalog → unused Provider
UPDATE：依赖项先于引用方；删除引用先于删除目标
```

原因：

任何时刻优先避免出现：

```text
Agent 指向尚不存在的 Provider / Catalog
```

但真正的一致性仍依赖 Backup / Recovery，而不是写入顺序本身。

---

# 82. Multi-file Atomicity

普通文件系统无法简单提供：

```text
多个文件的一次原子事务
```

因此 CAS 必须使用：

```text
Application-level Transaction
```

模型：

```text
Backup
   ↓
Write sequence
   ↓
Validate
   ↓
Success

或者

Failure
   ↓
Rollback from backup
```

---

# 83. Apply Failure

如果中途任何步骤失败：

```text
停止继续写入
```

然后：

```text
Rollback already-mutated resources
```

---

# 84. Rollback 成功

返回：

```text
APPLY_FAILED_ROLLED_BACK
```

语义：

```text
新配置没有生效
原配置已恢复
```

不得仅返回：

```text
Failed
```

---

# 85. Rollback 失败

如果：

```text
Apply failed
+
Rollback also failed
```

返回：

```text
RECOVERY_REQUIRED
```

并保留：

```text
Backup Snapshot
Failure Manifest
Observed Current State
```

不得将状态标记为 Applied。

---

# 86. Apply Success

只有当：

```text
所有目标文件写入成功
+
重新读取成功
+
解析成功
+
CAS-owned Projection 与 Desired State 一致
```

才能：

```text
mark Applied
```

---

# 87. Post-write Validation

写入后重新读取：

```text
config.toml
Agent Files
Catalog
```

然后计算：

```text
Actual Projection
```

与：

```text
Expected Projection
```

比较。

如果不一致：

```text
Apply 不成功
```

---

# 88. Codex Syntax Validation

V0.1 至少执行：

```text
TOML parse validation
JSON parse validation
CAS schema validation
```

如果 Codex 提供稳定、无副作用的官方配置验证命令，可以作为额外验证层。

但不得依赖：

```text
启动一个完整 Codex interactive session
```

作为唯一验证方式。

---

# 89. Configuration Deletion

删除 CAS Provider 不等于立刻删除：

```text
model_providers.cas_xxx
```

必须先确定：

```text
没有 CAS-managed Agent
继续引用它。
```

---

# 90. 删除 Provider Operation

标准：

```text
Remove Agent Bindings
        ↓
Remove / Update Agent Config
        ↓
Confirm no CAS references
        ↓
Delete provider fragment
```

禁止先删除 Provider。

---

# 91. 删除 Agent

CAS 删除 Agent：

```text
删除 CAS Domain Agent
```

Apply 时转换为：

```text
DELETE
$CODEX_HOME/agents/cas-<key>.toml
```

前提：

```text
该文件仍然是 CAS-owned
且没有 external modification conflict。
```

---

# 92. External Agent 永不自动删除

即使 External Agent：

```text
name = "executor"
```

导致 CAS Agent 无法创建，CAS 也不能：

```text
为了消除冲突
自动删除 External Agent。
```

---

# 93. Provider Rename

CAS Provider 的：

```text
Display Name
```

修改不需要改变：

```text
provider id
```

例如：

```text
cas_deepseek
```

应保持稳定。

否则所有 Agent：

```text
model_provider
```

都要同步改变。

---

# 94. Agent Rename

Agent：

```text
key / runtime name
```

变化属于：

```text
Identity Change
```

而不是普通字段修改。

Apply 应表现为：

```text
DELETE old Agent resource
+
CREATE new Agent resource
```

而不是简单更新文件显示名称。

---

# 95. Base URL 更新

Provider：

```text
base_url
```

更新只 Patch：

```text
model_providers.cas_xxx.base_url
```

不得重建整个：

```text
model_providers
```

表。

---

# 96. Unknown Provider Fields

假设未来 Codex 或用户在 CAS-owned Provider 下加入：

```toml
future_field = true
```

如果 CAS 不认识该字段：

默认：

```text
Preserve
```

而不是因为 CAS 重写 Provider Fragment 就删除。

---

# 97. CAS-exclusive Fragment 也应考虑 Forward Compatibility

虽然：

```text
model_providers.cas_xxx
```

属于 CAS-owned，

仍建议：

```text
Known-field patch
```

而不是每次整个 table 重新序列化。

这样未来：

```text
Codex 自动加入字段
或新版本 CAS 加入字段
```

更安全。

---

# 98. Agent 文件的 Unknown Fields

CAS Agent 文件属于 CAS Exclusive Resource。

但如果外部工具向其中添加 Unknown Field：

```text
hash 已变化
```

应首先触发：

```text
External Modification Conflict
```

因此 CAS 不需要尝试自动 merge 该文件。

---

# 99. AGENTS.md 边界

CAS V0.1 不修改：

```text
AGENTS.md
```

原因：

```text
AGENTS.md
属于项目/用户 Prompt Guidance

CAS
负责 Agent Configuration
```

即使某个 Agent Template 建议用户在 AGENTS.md 中指导 Parent Delegation，CAS 也不能自动植入。

---

# 100. MCP 配置边界

CAS 不因为生成 Agent 而修改用户全局：

```text
[mcp_servers]
```

如果某个 Custom Agent 本身配置专用 MCP：

```text
允许写入 CAS-owned Agent TOML
```

但不得修改全局 MCP。

---

# 101. Skill 配置边界

同理：

```text
skills.config
```

如果属于特定 CAS Agent，可以存在：

```text
CAS Agent File
```

但 CAS V0.1 不管理用户全局 Skill 设置。

---

# 102. Codex 配置优先级诊断

由于 Codex 存在多层优先级：

```text
CLI
Project
Profile
User
System
Default
```

CAS 不应仅查看：

```text
config.toml
```

然后宣称：

```text
“当前运行值一定是 X”
```



---

# 103. Effective Configuration

CAS 应区分：

```text
Configured Value
```

与：

```text
Potential Effective Value
```

例如用户级：

```toml
sandbox_mode = "workspace-write"
```

项目级：

```toml
sandbox_mode = "read-only"
```

则在该项目：

```text
effective = read-only
```

---

# 104. 无工作目录时

CAS Desktop 未绑定具体项目时：

```text
无法完整计算 project-specific effective config
```

此时只能展示：

```text
Global Codex Configuration
```

不能虚构某个项目运行时 Effective State。

---

# 105. Runtime CLI Override

例如用户启动：

```text
codex --model ...
```

CLI Override 优先级最高。

CAS 无法通过静态文件知道：

```text
当前任意 Session
```

是否存在此 Override。

因此 CAS Apply 的语义永远是：

> 更新持久化配置。

而不是：

> 强制改变所有运行中的 Codex Session。

---

# 106. Codex Version

CAS 每次启动或 Diagnostics 应检测：

```text
Codex Version
```

用于：

```text
Capability Gate
```

---

# 107. Version Capability Registry

CAS 内部应维护：

```text
CodexVersionCapability
```

例如：

```text
supportsCustomAgents
supportsModelProviderAuthCommand
supportsMultiAgent
supportsModelCatalog
supportsXxx
```

业务层不得大量：

```text
if version >= ...
```

散落。

---

# 108. Feature Detection 优先

如果某能力可以：

```text
Feature Probe
```

确认，优先 Feature Detection。

Version Check 作为：

```text
Compatibility fallback
```

---

# 109. Unsupported Version

如果 Codex 版本不支持 CAS V0.1 必需能力：

```text
CAS 仍允许查看配置
```

但：

```text
Apply Agent Configuration
```

必须阻止。

返回：

```text
CODEX_VERSION_UNSUPPORTED
```

---

# 110. Future Codex Format

官方当前明确说明 Custom Agent 配置格式未来可能继续演进。

因此：

```text
Agent Config Mapper
```

必须集中在：

```text
cas-codex
```

模块。

不得让：

```text
model_provider
developer_instructions
sandbox_mode
```

这样的 Codex 字段散布在整个应用。

---

# 111. Configuration Adapter Versioning

建议结构：

```text
CodexIntegration
│
├── CurrentConfigAdapter
└── CompatibilityAdapters
```

如果未来出现破坏性变化：

```text
Domain
不变

Configuration Adapter
变化
```

---

# 112. CODEX_HOME 检测

默认：

```text
CODEX_HOME env
```

如果存在合法值：

```text
优先使用
```

否则：

```text
~/.codex
```

Codex 官方当前本地状态默认位于 `CODEX_HOME`，其默认值为 `~/.codex`。

---

# 113. 路径规范化

CAS 内部路径比较必须进行：

```text
canonicalization / normalization
```

考虑：

```text
Windows separator
case behavior
symlink
relative paths
home expansion
```

不得通过原始字符串直接判断：

```text
是否同一个文件。
```

---

# 114. Symlink

如果：

```text
config.toml
```

或 Agent 文件是 Symbolic Link：

CAS 必须检测。

V0.1 推荐：

```text
允许读取
写入前 Warning / Policy Check
```

不得意外：

```text
替换 symlink 本身
```

导致用户配置结构改变。

---

# 115. 文件权限

Apply 前必须检查：

```text
directory writable
target writable
temporary file creatable
```

不可写：

```text
CONFIG_NOT_WRITABLE
```

必须在 Backup / Mutation 前尽可能提前发现。

---

# 116. Encoding

CAS-generated：

```text
TOML
JSON
Metadata
```

统一：

```text
UTF-8
```

读取已有文件时应避免无理由改变：

```text
BOM
Line ending
```

如 round-trip 工具可保持，则优先保持。

---

# 117. Line Ending

对于 shared：

```text
config.toml
```

优先保持原文件：

```text
LF / CRLF
```

避免 Apply 产生：

```text
整文件 diff
```

---

# 118. Formatting

CAS 不主动：

```text
格式化整个 config.toml
```

只对：

```text
新创建节点
```

采用 CAS 默认格式。

---

# 119. Comments

用户已有 Comment：

```text
必须尽可能保留。
```

CAS 新建 Provider 可以增加：

```text
简短 Managed comment
```

但不依赖它作为 Ownership。

---

# 120. Config Compiler 输入输出边界

Compiler 不直接接收：

```text
React Form
SQLite Row
Raw UI State
```

而接收：

```text
Validated Domain State
```

输出：

```text
ConfigurationPlan
```

避免把 Codex Patch 逻辑和 UI/Storage 耦合。

---

# 121. Provider Compilation

逻辑：

```text
Provider
   ↓
Resolve Authentication
   ↓
Resolve Protocol
   ↓
Resolve Codex Compatibility
   ↓
Provider Config Mapper
   ↓
model_providers.cas_xxx
```

如果无法映射：

```text
BLOCKED
```

不得生成部分 Provider 配置。

---

# 122. Model Compilation

Model 本身不一定生成：

```text
config.toml
```

它主要参与：

```text
Agent model
Agent model_provider
Agent model_catalog_json
```

映射。

---

# 123. Agent Compilation

逻辑：

```text
Agent
+
Effective Binding
+
Model
+
Provider
+
Catalog
+
Policy
        ↓
AgentConfigMapper
        ↓
cas-<agent>.toml
```

---

# 124. Profile Compilation（P1）

CAS Profile：

```text
Profile
        ↓
Resolve bindings
        ↓
Compile every affected Agent
```

Profile 自身不生成：

```text
Codex profile file
```

---

# 125. Configuration No-op

如果 Desired State 与当前 CAS-owned Codex Projection 完全一致：

```text
ConfigurationPlan = NoOp
```

不得：

```text
重新写文件
创建 Backup
更新时间戳
```

仅因为用户点击 Apply。

---

# 126. Idempotency

必须保证：

```text
Apply(State A)
Apply(State A)
Apply(State A)
```

最终文件内容与：

```text
Apply(State A)
```

一致。

---

# 127. Stable Rendering

CAS Exclusive File 生成必须：

```text
Deterministic
```

相同 Domain State：

```text
输出相同内容。
```

不能因为：

```text
HashMap 顺序
时间戳
随机字段
```

产生无意义 diff。

---

# 128. 禁止在生成文件中加入变化时间戳

例如不要：

```toml
# Generated at 2026-08-07 18:32:12
```

因为每次 Apply 都会导致：

```text
Content Hash 改变。
```

Generation Timestamp 存数据库/metadata，不进入配置正文。

---

# 129. Apply State Fingerprint

成功 Apply 后生成：

```text
Applied State Fingerprint
```

用于判断：

```text
CAS Desired State
是否发生变化。
```

不要单纯根据：

```text
updatedAt
```

判断 Pending Changes。

---

# 130. Pending Changes

推荐：

```text
Desired Projection Fingerprint
!=
Last Applied Desired Fingerprint
```

表示：

```text
Pending
```

同时还需要：

```text
Observed State Verification
```

判断磁盘是否被外部修改。

---

# 131. Drift

存在：

```text
Desired == Last Applied Desired
```

但：

```text
Observed != Last Applied Observed
```

时：

```text
Configuration Drift
```

不是：

```text
Pending User Changes
```

二者必须区分。

---

# 132. Drift 类型

至少：

```text
CAS_RESOURCE_MODIFIED

CAS_RESOURCE_MISSING

CAS_PROVIDER_FRAGMENT_MODIFIED

CAS_PROVIDER_FRAGMENT_MISSING

EXTERNAL_RESOURCE_CONFLICT
```

---

# 133. CAS Resource Missing

例如：

```text
cas-executor.toml
```

被用户删除。

此时：

```text
Drift
```

用户可以选择：

```text
Reapply CAS configuration
```

不应自动重建。

---

# 134. External Resource Preserve

CAS Apply 必须证明：

```text
非 CAS-owned 节点没有被计划删除。
```

Configuration Plan 可执行前增加：

```text
Ownership Safety Validation
```

---

# 135. Protected Config Categories

V0.1 明确禁止自动修改：

```text
mcp_servers
projects
hooks
rules
memories
permissions
approval_policy
sandbox global config
notifications
telemetry
desktop settings
auth.json
project .codex
system config
requirements.toml
```

除非未来某个独立功能规范明确扩展 Ownership。

---

# 136. `auth.json`

CAS 不读取或修改 Codex：

```text
auth.json
```

用于实现第三方 Provider Credential。

CAS 使用：

```text
自己的 Secret Store
+
cas-helper
```

与 Codex 官方登录状态分离。

---

# 137. Primary OpenAI Authentication

CAS 不改变：

```text
用户现有 ChatGPT OAuth
OpenAI Authentication
```

这是重要边界。

配置 Subagent Provider 不得导致：

```text
Primary Session
```

失去当前认证模式。

---

# 138. `requires_openai_auth`

CAS Custom Provider 默认：

```text
false
```

不得为了复用用户 ChatGPT 登录擅自：

```text
requires_openai_auth = true
```

除非 Provider 接入规范明确要求。

---

# 139. Provider Connectivity 与 Apply 分离

以下情况：

```text
Provider API 暂时不可达
```

不一定意味着：

```text
不能写入配置。
```

因此：

```text
Connectivity Check
```

与：

```text
Configuration Validity
```

分离。

---

# 140. Hard Block 与 Warning

Apply Hard Block：

```text
TOML invalid
Provider mapping impossible
Agent identity conflict
CAS-owned resource conflict
Unsupported Codex capability
Admin requirement conflict
Credential reference invalid
Target unwritable
```

Warning：

```text
Provider currently unreachable
Model compatibility unknown
Codex restart/session restart may be required
```

---

# 141. Provider 删除后的 Credential

删除：

```text
Codex Provider Fragment
```

和删除：

```text
Secret
```

不是同一个配置事务。

Credential 生命周期由：

```text
Secret Management Specification
```

决定。

本规范只要求：

```text
不会留下 Codex Provider 引用不存在 Credential Helper Target。
```

---

# 142. Backup 与 CAS Database 一致性

Apply 成功前：

```text
不能先把 ManagedResource
标记为新版本已 Applied。
```

推荐：

```text
File Transaction
     ↓
Validation
     ↓
Persistence Commit
```

如果 CAS Persistence Commit 失败：

```text
必须进入 Recovery 状态
```

而不是假装成功。

---

# 143. Crash Recovery

如果进程在：

```text
Backup
↓
Write
↓
Crash
```

之间退出，下一次 CAS 启动必须检测：

```text
unfinished apply transaction
```

---

# 144. Apply Journal

建议每次 Apply 建立：

```text
Apply Journal
```

至少记录：

```text
transactionId
startedAt
snapshotId
planned resources
completed operations
status
```

状态：

```text
PREPARED
WRITING
VALIDATING
COMMITTED
ROLLING_BACK
ROLLED_BACK
RECOVERY_REQUIRED
```

---

# 145. Journal 存放

Journal 应存：

```text
CAS 自己的数据目录
```

而不是：

```text
config.toml
```

---

# 146. Startup Recovery

CAS 启动发现：

```text
WRITING
VALIDATING
ROLLING_BACK
```

未完成事务：

不得立即允许新的 Apply。

必须先：

```text
Inspect Current State
        ↓
Compare Snapshot / Journal
        ↓
Recover or mark Recovery Required
```

---

# 147. Restore 与 Apply 不同

Restore：

```text
以 Snapshot 为目标
```

Apply：

```text
以 Current CAS Desired State 为目标
```

二者必须使用不同 Application Use Case。

---

# 148. Restore 不修改 CAS Desired State

默认 Restore：

```text
只恢复 Codex 文件状态。
```

不得自动把 CAS Domain State 回滚到历史版本。

否则会将：

```text
配置恢复
```

和：

```text
业务数据版本回滚
```

混为一体。

如果未来需要完整 CAS State Snapshot，应单独设计。

---

# 149. Restore 后状态

Restore 完成后：

```text
Observed Codex State
```

可能与：

```text
Current Desired CAS State
```

不同。

因此状态应是：

```text
Changes Pending / Drift
```

而不是强行：

```text
Applied
```

---

# 150. Diagnostics Integration

配置集成层必须向 Diagnostics 暴露：

```text
CodexHomeStatus
ConfigParseStatus
ProviderFragmentStatus
AgentResourceStatus
CatalogStatus
OwnershipStatus
DriftStatus
RequirementsStatus
VersionCapabilityStatus
```

Diagnostics 不直接重新实现配置解析。

---

# 151. Logging

配置操作日志不得记录：

```text
API Key
Bearer Token
Credential stdout
Secret Header
```

允许：

```text
Provider ID
Agent ID
File Path
Operation Type
Error Code
Fingerprint
```

---

# 152. Diff Logging

默认不记录完整：

```text
config.toml diff
```

到普通日志。

因为未知外部配置未来可能包含：

```text
敏感内容。
```

Debug Diff 需要经过：

```text
Redaction
```

---

# 153. Error Classification

配置集成至少需要统一错误类别：

```text
EnvironmentError
ParseError
CompatibilityError
OwnershipError
ConflictError
ValidationError
BackupError
WriteError
RollbackError
CredentialIntegrationError
ManagedRequirementError
```

---

# 154. 典型错误码

推荐：

```text
CODEX_NOT_FOUND

CODEX_HOME_NOT_ACCESSIBLE

CODEX_VERSION_UNSUPPORTED

CODEX_CONFIG_PARSE_ERROR

CODEX_CONFIG_NOT_WRITABLE

AGENT_CONFIG_PARSE_ERROR

AGENT_NAME_CONFLICT

PROVIDER_ID_CONFLICT

CAS_RESOURCE_EXTERNALLY_MODIFIED

CAS_RESOURCE_MISSING

MODEL_CATALOG_INVALID

PROVIDER_MAPPING_UNSUPPORTED

MULTI_AGENT_ADMIN_DISABLED

SANDBOX_DISALLOWED_BY_ADMIN

APPLY_CONFLICT

APPLY_FAILED_ROLLED_BACK

RECOVERY_REQUIRED
```

具体错误响应格式由错误处理规范负责。

---

# 155. V0.1 标准目录示例

最终可能形成：

```text
~/.codex/
│
├── config.toml
│
├── auth.json                    # External / Codex-owned
│
├── agents/
│   ├── my-own-agent.toml        # External
│   ├── cas-executor.toml        # CAS-owned
│   └── cas-reviewer.toml        # CAS-owned
│
├── deep-review.config.toml      # External Codex Profile
│
└── cas/
    ├── model-catalogs/
    │   ├── deepseek.json
    │   └── provider-x.json
    │
    └── ...
```

CAS 自身数据库和 Secret Store 不要求必须放：

```text
CODEX_HOME
```

其路径由 Persistence / Platform 规范定义。

---

# 156. 示例：Apply 前

```toml
model = "gpt-5.6"

[mcp_servers.my_server]
url = "https://example.com/mcp"

[projects."D:/code/project"]
trust_level = "trusted"
```

CAS Desired：

```text
Executor
→ DeepSeek V4 Flash
```

---

# 157. 示例：Apply 后 `config.toml`

```toml
model = "gpt-5.6"

[mcp_servers.my_server]
url = "https://example.com/mcp"

[projects."D:/code/project"]
trust_level = "trusted"

[model_providers.cas_deepseek]
name = "DeepSeek"
base_url = "https://api.deepseek.com/"
wire_api = "responses"

[model_providers.cas_deepseek.auth]
command = "C:/Program Files/Codex Agent Switch/bin/cas-helper.exe"
args = ["token", "<credential-id>"]
timeout_ms = 5000
refresh_interval_ms = 300000
```

注意：

```text
原有 model
MCP
project
```

均保留。

---

# 158. 示例：Agent 文件

```toml
name = "executor"

description = """
Implementation-focused worker used after architecture
and implementation boundaries have been established.
"""

model = "deepseek-v4-flash"
model_provider = "cas_deepseek"

model_reasoning_effort = "high"

model_catalog_json = "/resolved/path/.codex/cas/model-catalogs/deepseek.json"

sandbox_mode = "workspace-write"

developer_instructions = """
You are an implementation executor working under a primary Codex agent.

Follow the delegated task and established architecture.

Implement the requested change, run relevant verification, and report
the resulting changes and unresolved issues.

Do not redesign unrelated architecture or expand task scope.
"""
```

---

# 159. 示例：模型切换

当前：

```text
executor
→ DeepSeek V4 Flash
```

用户改为：

```text
executor
→ Provider X / Model X
```

Configuration Plan：

```text
ENSURE provider cas_provider_x

ENSURE required catalog

UPDATE agents/cas-executor.toml
    model_provider:
        cas_deepseek
        →
        cas_provider_x

    model:
        deepseek-v4-flash
        →
        model-x

REMOVE cas_deepseek provider
    only if no remaining CAS references
```

Agent Identity：

```text
executor
```

不变。

---

# 160. 示例：外部配置修改

Last Applied：

```toml
[model_providers.cas_deepseek]
base_url = "https://api.deepseek.com/"
```

Observed：

```toml
[model_providers.cas_deepseek]
base_url = "https://proxy.example.com/"
```

Desired：

```toml
base_url = "https://api.deepseek.com/"
```

结果：

```text
CONFLICT
```

而不是：

```text
自动恢复成 api.deepseek.com
```

---

# 161. 示例：无关修改

Last Applied：

```toml
[model_providers.cas_deepseek]
...
```

之后用户加入：

```toml
[mcp_servers.foo]
...
```

CAS 再次 Apply：

```text
No conflict
```

必须：

```text
读取最新 config.toml
保留新 MCP
Patch CAS fragment
```

---

# 162. 示例：已有同名 Agent

External：

```text
~/.codex/agents/custom.toml

name = "executor"
```

CAS Desired：

```text
Create executor
```

结果：

```text
AGENT_NAME_CONFLICT
```

CAS 不创建：

```text
cas-executor.toml
```

---

# 163. 示例：Built-in `explorer`

如果用户选择让 CAS 创建：

```text
explorer
```

必须明确理解：

```text
CAS Custom Explorer
```

将覆盖 Codex built-in explorer 的 Role Resolution。Codex 当前明确规定同名 Custom Agent 优先。

因此此操作不能被当成普通无影响 Agent Creation。

---

# 164. V0.1 Apply 支持矩阵

V0.1 必须支持：

```text
CREATE CAS Provider

UPDATE CAS Provider

DELETE unused CAS Provider

CREATE CAS Agent

UPDATE CAS Agent

DELETE CAS Agent

CREATE CAS Catalog

UPDATE CAS Catalog

DELETE unused CAS Catalog

Patch shared config.toml

Detect external modification

Backup

Rollback

Restore
```

---

# 165. V0.1 不支持自动修改

明确不支持：

```text
project .codex/config.toml

project .codex/agents/

Codex profile files

system config

requirements.toml

auth.json

AGENTS.md

global MCP configuration

global hooks

global permission configuration

Primary model selection

Codex login method
```

---

# 166. 配置集成核心不变量

## 不变量一

```text
CAS 不拥有完整 config.toml。
```

---

## 不变量二

```text
任何 Unknown External Configuration 默认 Preserve。
```

---

## 不变量三

```text
Provider 配置只进入合法的 machine/user-level Provider 层。
```

---

## 不变量四

```text
Subagent 模型绑定不得修改 Primary Model。
```

---

## 不变量五

```text
CAS-owned Agent 与 External Agent 必须能够明确区分。
```

---

## 不变量六

```text
Agent Runtime Identity 由 name 决定，
不能只根据 filename 判断。
```

---

## 不变量七

```text
任何 Mutation 前必须完成 Backup。
```

---

## 不变量八

```text
Apply 必须基于最新磁盘状态重新编译 Plan。
```

---

## 不变量九

```text
CAS-owned 内容被外部修改后不得静默覆盖。
```

---

## 不变量十

```text
无关 External Modification 不应阻止 Apply。
```

---

## 不变量十一

```text
Apply 必须幂等。
```

---

## 不变量十二

```text
Apply 失败不得产生虚假的 Applied 状态。
```

---

## 不变量十三

```text
管理员 Requirements 不得被 CAS 绕过。
```

---

## 不变量十四

```text
Secret 不进入普通 Codex TOML 明文。
```

---

## 不变量十五

```text
CAS Profile 不依赖 Codex --profile 才能生效。
```

---

## 不变量十六

```text
CAS Desktop 退出后，
生成的 Codex 配置仍应独立存在。
```

---

# 167. 最终配置模型

CAS V0.1 与 Codex 的最终关系应保持：

```text
                       CAS
                        │
                        │ Desired State
                        ▼
              Configuration Compiler
                        │
                        ▼
                Configuration Plan
                        │
             ┌──────────┴──────────┐
             │                     │
             ▼                     ▼
      Ownership Check       Requirements Check
             │                     │
             └──────────┬──────────┘
                        ▼
                      Backup
                        │
                        ▼
             Configuration Writer
                        │
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
     config.toml     agents/*.toml   Catalog
          │             │             │
          └─────────────┼─────────────┘
                        ▼
                Post-write Validate
                        │
                        ▼
                   Codex Runtime
```

其中：

```text
config.toml
```

是：

```text
Shared Resource
```

只能进行：

```text
Non-destructive Patch
```

而：

```text
cas-*.toml
CAS model catalogs
```

是：

```text
CAS Exclusive Resource
```

可以完整生成，但必须接受：

```text
External Modification Detection
```

保护。

最终必须确保：

> CAS 永远是 Codex 配置的管理参与者，而不是 Codex 配置的独占拥有者。

> CAS 能够创建和维护自己的 Provider、Agent 和 Model Catalog，但不能因为自己的存在破坏用户已经建立的 Codex 环境。

> 一次 Apply 的安全性优先级高于“尽可能自动修复一切”。

> 当 CAS 无法证明某个写操作安全时，正确行为是停止 Apply 并报告冲突，而不是猜测用户意图后继续覆盖。
