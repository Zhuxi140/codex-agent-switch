# Codex Agent Switch 数据模型文档

> 文档类型：Data Model Specification  
> 项目暂定名称：Codex Agent Switch  
> 简称：CAS  
> 文档职责：定义系统核心数据实体、字段语义、实体关系、约束、生命周期及持久化边界。

---

# 0. V0.1 数据模型基线

V0.1 的持久化核心是 `Provider → Model → AgentModelBinding → Agent`，只实现 Base Binding。Profile / ProfileAgentBinding 的长期模型保留为 P1，不进入 V0.1 Schema、IPC 或运行状态。`deepseek-v4-flash` 是 Preset 实例数据，不是领域子类型；Custom Responses Provider 使用相同实体与约束。

Capability 只保留业务所需三态及统一 Evidence Source；Discovery 与 Runtime Probe 的历史记录延后。Ownership 以 `semanticHash / fragmentHash` 判定，字节级 `contentHash` 只用于诊断。

---

# 1. 文档目标

Codex Agent Switch 的核心数据模型围绕以下关系建立：

```text
Agent
  ↓
Model Binding
  ↓
Model
  ↓
Provider
  ↓
Protocol
```

同时由以下辅助模型提供支撑：

```text
Profile
Capability
Credential Reference
Provider Preset
Model Metadata
Application Setting
Configuration Snapshot
Managed Resource
```

数据模型必须保证：

- Agent 与具体模型解耦；
- Model 与具体 Agent 解耦；
- Model 与 Provider 明确关联；
- Provider 与认证凭据解耦；
- Provider 与具体通信协议解耦；
- Profile 只描述 Agent 与 Model 的组合关系；
- Secret 不以明文进入业务数据库；
- Codex 配置文件不是 CAS 业务数据源；
- CAS 可以识别自己管理的 Codex 资源；
- 模型与 Provider 能够在未来平滑扩展。

---

# 2. 核心实体关系

整体关系：

```text
Provider
   │
   │ 1:N
   ▼
Model
   │
   │ N:M
   ▼
AgentModelBinding
   ▲
   │
   │ N:1
Agent


Profile
   │
   │ 1:N
   ▼
ProfileAgentBinding
   │
   ├── Agent
   └── Model
```

认证关系：

```text
Provider
   │
   │ 0..1
   ▼
CredentialReference
   │
   ▼
Operating System Secret Store
```

能力关系：

```text
Provider ──┐
           ├── Capability
Model ─────┘
```

Codex 管理关系：

```text
CAS Domain State
      │
      ▼
ManagedResource
      │
      ▼
Codex Configuration Resource
```

---

# 3. 标识符规范

所有 CAS 自有实体必须使用稳定 ID。

推荐：

```text
UUID
```

或者：

```text
UUIDv7
```

不得使用：

```text
Provider 名称
Model 名称
Agent 名称
文件路径
数组索引
```

作为内部主标识符。

例如：

```text
provider.id = 018f...
model.id    = 018f...
agent.id    = 018f...
```

而：

```text
deepseek
deepseek-v4-flash
executor
```

属于业务标识，不属于内部主键。

---

# 4. 通用时间字段

主要持久化实体统一具备：

```text
createdAt
updatedAt
```

语义：

```text
createdAt
实体首次进入 CAS 持久化系统的时间。

updatedAt
实体最后一次业务状态发生变化的时间。
```

如果实体支持软删除，再增加：

```text
deletedAt
```

V0.1 不强制所有实体使用软删除。

---

# 5. Provider

Provider 表示一个模型服务来源。

示例：

```text
DeepSeek
OpenRouter
Custom Responses Provider
Local Provider
```

## 5.1 Provider 字段

```text
Provider
├── id
├── key
├── name
├── type
├── baseUrl
├── protocol
├── authType
├── credentialRef
├── enabled
├── source
├── presetId
├── customHeaders
├── capabilities
├── metadata
├── createdAt
└── updatedAt
```

---

## 5.2 `id`

类型：

```text
ProviderId
```

系统内部唯一标识。

不可修改。

---

## 5.3 `key`

Provider 的稳定业务标识。

例如：

```text
deepseek
openrouter
my-company-proxy
```

要求：

```text
全局唯一
```

推荐格式：

```text
[a-z0-9][a-z0-9-_]*
```

`key` 用于：

- 内部引用；
- CLI 引用；
- 配置生成；
- CAS-owned Codex Provider 命名。

不得自动跟随 `name` 修改。

---

## 5.4 `name`

用户可见名称。

例如：

```text
DeepSeek
My OpenRouter
Company AI Gateway
```

允许修改。

不要求唯一。

---

# 6. Provider Type

`type` 描述 Provider 的来源类别。

推荐：

```text
BUILT_IN
PRESET
CUSTOM
LOCAL
GATEWAY
```

语义：

### BUILT_IN

CAS 内建、具有明确适配逻辑的 Provider。

### PRESET

由 CAS Provider Preset 创建。

### CUSTOM

用户手动创建的兼容 Provider。

### LOCAL

本地运行的模型服务。

### GATEWAY

通过 CAS 或其他兼容 Gateway 访问。

V0.1 可以只实际支持：

```text
PRESET
CUSTOM
```

但数据模型保留完整枚举空间。

---

# 7. Provider Protocol

`protocol` 表示 Provider 对外通信协议。

定义为独立值对象：

```text
ProtocolType
```

推荐枚举：

```text
RESPONSES
CHAT_COMPLETIONS
ANTHROPIC_MESSAGES
GEMINI
CUSTOM
```

V0.1 实际支持：

```text
RESPONSES
```

其余值可以存在于数据模型，但不得在当前版本中被标记为：

```text
可直接用于 Codex
```

---

# 8. Provider Authentication

`authType` 描述 Provider 使用的认证形式。

推荐：

```text
NONE
BEARER_TOKEN
API_KEY_HEADER
CUSTOM_HEADER
COMMAND
```

其中 V0.1 核心：

```text
BEARER_TOKEN
```

具体密钥不得保存在 Provider 实体中。

Provider 只保存：

```text
credentialRef
```

---

# 9. CredentialReference

CredentialReference 表示：

> CAS 业务实体与系统 Secret Store 中凭据之间的引用。

结构：

```text
CredentialReference
├── id
├── providerId
├── key
├── secretType
├── storageBackend
├── createdAt
└── updatedAt
```

禁止包含：

```text
API Key 明文
Access Token 明文
Refresh Token 明文
密码
```

---

## 9.1 Credential Reference 示例

```text
Provider
DeepSeek

credentialRef
credential://provider/018f...
```

真正 Secret：

```text
Windows Credential Manager
macOS Keychain
Linux Secret Service
```

---

# 10. Credential Secret Type

推荐：

```text
API_KEY
BEARER_TOKEN
ACCESS_TOKEN
CUSTOM_SECRET
```

此字段描述语义，不包含秘密内容。

---

# 11. Storage Backend

用于表示 Secret 实际保存位置。

```text
WINDOWS_CREDENTIAL_MANAGER
MACOS_KEYCHAIN
LINUX_SECRET_SERVICE
CUSTOM
```

该字段主要用于：

- 诊断；
- 迁移；
- Secret 生命周期管理。

---

# 12. Provider Custom Headers

部分兼容 Provider 可能要求额外 HTTP Header。

因此 Provider 可包含：

```text
customHeaders
```

但必须区分：

```text
非敏感 Header
```

和：

```text
敏感 Header
```

普通 Header 可以直接保存：

```text
X-App-Name
X-Client-Version
```

敏感值不得明文存储。

应保存：

```text
HeaderCredentialBinding
```

例如：

```text
X-API-Key
    ↓
credentialRef
```

---

# 13. Provider Source

`source` 表示 Provider 定义来源。

推荐：

```text
BUILT_IN
PRESET
USER
IMPORTED
```

该字段与 Provider Type 不完全相同。

例如：

```text
type   = CUSTOM
source = USER
```

或者：

```text
type   = PRESET
source = BUILT_IN
```

---

# 14. Provider Metadata

`metadata` 用于保存非核心扩展信息。

原则：

> 只有不影响核心业务语义的数据才能进入 metadata。

不得将核心字段长期塞进：

```json
{
  "metadata": {
    "everything": "..."
  }
}
```

如果某字段开始参与业务判断，应提升为正式数据字段。

---

# 15. Model

Model 表示一个可以通过某个 Provider 调用的模型。

结构：

```text
Model
├── id
├── providerId
├── modelId
├── displayName
├── enabled
├── source
├── contextWindow
├── maxOutputTokens
├── reasoning
├── capabilities
├── compatibility
├── metadataSource
├── metadata
├── createdAt
└── updatedAt
```

---

# 16. `modelId`

表示发送给 Provider API 的实际模型标识。

例如：

```text
deepseek-v4-flash
```

或者：

```text
provider/model-name
```

具体取决于 Provider。

约束：

```text
(providerId, modelId)
```

组合唯一。

不同 Provider 可以拥有相同 `modelId`。

---

# 17. `displayName`

用户可见名称。

例如：

```text
DeepSeek V4 Flash
```

与：

```text
modelId
```

分离。

这样 Provider 修改展示名称不会影响运行配置。

---

# 18. Model Source

推荐：

```text
DISCOVERED
PRESET
USER
CATALOG
IMPORTED
```

语义：

### DISCOVERED

通过 Provider Model Discovery 获取。

### PRESET

CAS 内建 Provider Preset 自带。

### USER

用户手动添加。

### CATALOG

来自模型 Catalog。

### IMPORTED

从已有 Codex 配置导入。

---

# 19. Context Window

字段：

```text
contextWindow
```

表示模型最大上下文窗口。

单位：

```text
token
```

允许：

```text
null
```

表示：

```text
unknown
```

禁止使用：

```text
0
```

代表未知。

---

# 20. Reasoning Configuration

模型推理能力使用独立值对象：

```text
ReasoningCapability
```

结构：

```text
ReasoningCapability
├── supported
├── supportedEfforts
└── defaultEffort
```

例如：

```text
supported = true

supportedEfforts:
- low
- medium
- high

defaultEffort = high
```

如果：

```text
supported = false
```

则：

```text
supportedEfforts
defaultEffort
```

必须为空。

---

# 21. Capability

Capability 表示 Provider 或 Model 声明的能力。

推荐统一枚举：

```text
RESPONSES_API
STREAMING
TOOL_CALLING
PARALLEL_TOOL_CALLING
STRUCTURED_OUTPUT
REASONING
MULTI_AGENT
CODEX_MULTI_AGENT
LARGE_CONTEXT
IMAGE_INPUT
FILE_INPUT
WEB_SEARCH
APPLY_PATCH
SHELL_TOOL
```

能力集合：

```text
CapabilitySet
```

---

# 22. Provider Capability 与 Model Capability

Provider 和 Model 都可以声明 Capability。

最终有效能力采用：

```text
Effective Capability
```

计算。

原则：

```text
EffectiveCapability
=
ProviderCapability
∩
ModelCapability
∩
CASCompatibility
```

例如：

Provider 支持：

```text
RESPONSES_API
TOOL_CALLING
PARALLEL_TOOL_CALLING
```

但模型只支持：

```text
RESPONSES_API
TOOL_CALLING
```

最终：

```text
PARALLEL_TOOL_CALLING = false
```

---

# 23. Capability Source

为了区分能力来自哪里，可为能力声明附带：

```text
CapabilitySource
```

推荐：

```text
OFFICIAL_PROVIDER
OFFICIAL_CODEX
CAS_BUILT_IN
RUNTIME_PROBE
USER_OVERRIDE
UNKNOWN
```

优先级策略不属于本数据模型文档。

但数据结构必须允许保留来源。

---

# 24. Model Compatibility

Model 需要单独保存与 Codex 的兼容信息。

定义：

```text
ModelCompatibility
```

结构：

```text
ModelCompatibility
├── codexCompatible
├── compatibilityLevel
├── minimumCodexVersion
├── multiAgentVersion
├── issues
└── verifiedAt
```

---

# 25. Compatibility Level

推荐：

```text
NATIVE
COMPATIBLE
GATEWAY_REQUIRED
UNSUPPORTED
UNKNOWN
```

语义：

### NATIVE

Provider / Model 明确原生支持 Codex 所需能力。

### COMPATIBLE

通过兼容 Responses API 等方式可以正常使用。

### GATEWAY_REQUIRED

必须经过协议转换层。

### UNSUPPORTED

当前 CAS 明确不能用于 Codex Agent。

### UNKNOWN

尚未验证。

---

# 26. Multi-Agent Version

独立字段：

```text
multiAgentVersion
```

推荐：

```text
NONE
V1
V2
UNKNOWN
```

不得将：

```text
multiAgentVersion
```

作为普通字符串散落在 metadata 中。

---

# 27. Model Metadata Source

模型元数据可能来自：

```text
Provider API
官方 Catalog
CAS 内建 Catalog
用户自定义
运行时探测
```

定义：

```text
ModelMetadataSource
```

例如：

```text
OFFICIAL_PROVIDER
CAS_BUILT_IN
REMOTE_CATALOG
USER
DISCOVERY
```

---

# 28. Agent

Agent 表示一个 Codex Agent Role。

结构：

```text
Agent
├── id
├── key
├── displayName
├── description
├── instruction
├── type
├── enabled
├── sandboxPolicy
├── reasoningPolicy
├── source
├── managed
├── createdAt
└── updatedAt
```

---

# 29. Agent Key

示例：

```text
executor
explorer
reviewer
tester
database_expert
java_worker
```

必须在 CAS Agent 命名空间内唯一。

推荐：

```text
[a-z][a-z0-9_-]*
```

禁止使用 Provider 或 Model 名称表达角色。

---

# 30. Agent Name

用于 UI 展示。

例如：

```text
Executor
Code Reviewer
Database Expert
```

允许修改。

---

# 31. Agent Description

`description` 用于表达：

> 什么时候应该调用这个 Agent。

例如：

```text
Implementation-focused worker responsible for executing
already-decided engineering tasks.
```

它与：

```text
instruction
```

职责不同。

`description` 面向：

```text
Parent Agent
```

`instruction` 面向：

```text
Spawned Agent 本身
```

---

# 32. Agent Instruction

`instruction` 表示该 Agent 的执行职责与行为约束。

必须与：

```text
Model
Provider
```

解耦。

禁止出现：

```text
You are DeepSeek...
```

推荐：

```text
You are an implementation executor...
```

---

# 33. Agent Type

推荐：

```text
BUILT_IN_ROLE
PRESET
CUSTOM
IMPORTED
```

例如：

```text
executor → PRESET
database_expert → CUSTOM
```

---

# 34. Agent Source

推荐：

```text
CAS
USER
IMPORTED
```

用于判断配置来源。

不用于判断 Ownership。

---

# 35. Sandbox Policy

定义独立值对象：

```text
SandboxPolicy
```

推荐初始值：

```text
READ_ONLY
WORKSPACE_WRITE
DANGER_FULL_ACCESS
INHERIT
```

Agent 只保存策略，不直接保存 Codex 未来可能变化的底层字段结构。

Codex-specific 映射由集成层处理。

---

# 36. Reasoning Policy

Agent 可以表达：

```text
INHERIT
LOW
MEDIUM
HIGH
MODEL_DEFAULT
```

此值表示：

> Agent 对推理强度的偏好。

最终是否有效需要结合 Model Capability 判断。

---

# 37. AgentModelBinding

Agent 与 Model 不直接把 Model 字段嵌入 Agent。

使用显式绑定实体：

```text
AgentModelBinding
├── id
├── agentId
├── modelId
├── enabled
├── priority
├── source
├── createdAt
└── updatedAt
```

V0.1 每个 Agent 默认只有一个：

```text
Active Binding
```

但独立建模有利于未来支持：

```text
fallback
routing
priority
```

---

# 38. AgentModelBinding 约束

同一个 Agent：

```text
只能存在一个 active primary binding
```

V0.1 可以约束：

```text
agentId UNIQUE
```

未来如果支持多模型路由，再取消该限制。

---

# 39. Binding 不保存 Provider

Binding：

```text
Agent → Model
```

而不是：

```text
Agent → Provider + Model
```

Provider 通过 Model 得到：

```text
Agent
  ↓
Model
  ↓
Provider
```

这样切 Provider 提供的模型时，数据关系清晰。

---

# 40. Profile（P1）

第 40—44 节定义后续 Profile 语义，不进入 V0.1 持久化与运行范围。

Profile 表示一组可以整体激活的 Agent Model Binding。

结构：

```text
Profile
├── id
├── key
├── name
├── description
├── isActive
├── source
├── createdAt
└── updatedAt
```

---

# 41. Profile 不复制 Agent

Profile 不应该保存：

```text
完整 Agent
完整 Provider
完整 Model
```

Profile 只是绑定组合。

---

# 42. ProfileAgentBinding

定义：

```text
ProfileAgentBinding
├── id
├── profileId
├── agentId
├── modelId
├── enabled
├── reasoningOverride
├── sandboxOverride
├── createdAt
└── updatedAt
```

核心关系：

```text
Profile
   ↓
Agent
   ↓
Model
```

---

# 43. Profile Override

Profile 可以针对某个 Agent 提供有限覆盖：

```text
reasoningOverride
sandboxOverride
```

但不得覆盖：

```text
Agent instruction
Agent description
Provider endpoint
API Key
Model capability
```

这些属于其他实体职责。

---

# 44. Active Profile

系统任一时刻最多存在：

```text
1 个 Active Profile
```

如果没有 Active Profile，则系统可以使用：

```text
Direct Agent Binding
```

Profile 功能进入 P1 后，如需默认 Profile，应按实际使用 Lazy Create：

```text
default
```

使状态始终明确。

---

# 45. ProviderPreset

ProviderPreset 用于提供开箱即用 Provider 模板。

结构：

```text
ProviderPreset
├── id
├── key
├── name
├── providerType
├── defaultBaseUrl
├── protocol
├── authType
├── modelDefinitions
├── capabilityHints
├── documentation
├── version
└── source
```

Preset 不代表：

```text
用户已经配置的 Provider
```

它只是：

```text
创建 Provider 的模板
```

---

# 46. ProviderPreset 与 Provider

关系：

```text
ProviderPreset
     │
     │ instantiate
     ▼
Provider
```

Provider 创建后允许用户修改：

```text
name
baseUrl
credentials
models
```

Preset 更新不得无条件覆盖用户 Provider。

---

# 47. AgentPreset

AgentPreset 表示标准 Agent Role 模板。

例如：

```text
Executor
Explorer
Reviewer
Tester
Security Reviewer
Java Worker
Frontend Worker
```

结构：

```text
AgentPreset
├── id
├── key
├── name
├── description
├── instructionTemplate
├── defaultSandboxPolicy
├── defaultReasoningPolicy
├── recommendedCapabilities
├── version
└── source
```

AgentPreset 与 Agent 的关系：

```text
AgentPreset
    ↓ instantiate
Agent
```

Preset 不是 Agent 本身。

---

# 48. ModelDefinition

ProviderPreset 中可以包含：

```text
ModelDefinition
```

用于描述已知模型。

结构：

```text
ModelDefinition
├── modelId
├── displayName
├── contextWindow
├── reasoningCapability
├── capabilities
├── compatibility
└── metadata
```

用户真正启用后，转换为：

```text
Model
```

实体。

---

# 49. ManagedResource

CAS 必须记录自己创建或管理的 Codex Resource。

定义：

```text
ManagedResource
├── id
├── resourceType
├── logicalKey
├── physicalLocation
├── ownership
├── semanticHash
├── fragmentHash
├── contentHash
├── generatedFrom
├── lastAppliedAt
├── createdAt
└── updatedAt
```

---

# 50. Managed Resource Type

推荐：

```text
CODEX_PROVIDER_CONFIG
CODEX_AGENT_CONFIG
MODEL_CATALOG
HELPER_CONFIG
OTHER
```

---

# 51. Ownership

推荐：

```text
CAS
EXTERNAL
SHARED
UNKNOWN
```

正常情况下 CAS 只创建：

```text
CAS
```

资源。

`SHARED` 表示：

> 同一个物理配置文件同时包含 CAS 与用户内容。

例如：

```text
config.toml
```

---

# 52. Physical Location

用于记录资源实际位置。

例如：

```text
~/.codex/config.toml
~/.codex/agents/executor.toml
```

注意：

```text
physicalLocation
```

不是实体 ID。

路径变化后允许更新。

---

# 53. Semantic Fingerprint 与 Content Hash

CAS 管理资源必须保存适合其 Ownership 粒度的语义指纹，并可选保存字节诊断 Hash：

```text
semanticHash / fragmentHash
contentHash
```

其中 `semanticHash / fragmentHash` 用于判断：

```text
CAS-owned 语义内容是否被外部修改
```

例如：

```text
CAS apply
   ↓
semantic hash = A

用户手动修改文件
   ↓
semantic hash = B

CAS 再次 apply
   ↓
检测外部变化
```

`contentHash` 只用于诊断展示，不参与 Ownership 与 Conflict 判定。

具体冲突策略不属于本文档。

---

# 54. GeneratedFrom

用于记录一个 ManagedResource 是由哪个逻辑对象生成。

例如：

```text
executor.toml
generatedFrom:
agent/executor
```

或者：

```text
deepseek catalog
generatedFrom:
provider/deepseek
```

推荐保存结构化引用：

```text
ResourceOrigin
├── entityType
└── entityId
```

---

# 55. ConfigurationSnapshot

ConfigurationSnapshot 表示 CAS 对外部 Codex 配置所做的历史快照。

结构：

```text
ConfigurationSnapshot
├── id
├── reason
├── codexHome
├── codexVersion
├── manifest
├── createdAt
└── status
```

---

# 56. Snapshot Manifest

Snapshot 内应通过 Manifest 描述包含的资源。

```text
SnapshotManifest
└── SnapshotResource[]
```

每项：

```text
SnapshotResource
├── relativePath
├── contentHash
├── resourceType
└── exists
```

实际文件内容如何保存属于存储实现，不属于数据模型职责。

---

# 57. Snapshot Status

推荐：

```text
VALID
PARTIAL
CORRUPTED
RESTORED
```

如果恢复失败，不应删除 Snapshot。

---

# 58. ApplicationSetting

仅保存 CAS 自身全局设置。

结构：

```text
ApplicationSetting
├── key
├── value
├── valueType
├── updatedAt
└── source
```

不得把：

```text
Provider
Agent
Profile
Model
```

塞入通用 Setting。

Setting 只适合：

```text
currentProfileId
autoBackupEnabled
updateChannel
```

这类真正的全局配置。

---

# 59. External Codex Configuration

CAS 可以读取 Codex 外部配置，但不得把所有 Codex 内容复制进入业务数据库。

如果需要记录外部信息，应采用只读引用模型：

```text
ExternalCodexState
├── codexHome
├── version
├── detectedProviders
├── detectedAgents
├── lastScannedAt
└── fingerprint
```

该对象属于：

```text
缓存 / 检测结果
```

而不是 CAS 的核心业务真相。

---

# 60. Imported Resource

如果用户希望将已有 Codex Agent 或 Provider 纳入 CAS 管理，应明确发生：

```text
Import
```

导入后：

```text
External Resource
        ↓
CAS Domain Entity
        ↓
CAS Managed Resource
```

不得在扫描过程中自动把用户配置变成 CAS-owned。

---

# 61. Entity Ownership

业务实体建议区分：

```text
source
managed
```

例如：

```text
Agent
source  = IMPORTED
managed = false
```

用户主动执行：

```text
Manage with CAS
```

后才变为：

```text
managed = true
```

避免 CAS 擅自接管用户已有资源。

---

# 62. Entity Reference

跨实体引用应统一使用强类型 ID：

```text
ProviderId
ModelId
AgentId
ProfileId
CredentialId
```

避免大量：

```text
String
```

在领域层互相传递。

例如：

```text
Model.providerId: ProviderId
```

而不是：

```text
Model.providerName: String
```

---

# 63. 删除约束

删除 Provider 前必须满足：

```text
不存在有效 Model 引用
```

否则应先：

```text
删除 / 迁移其 Models
```

关系：

```text
Provider
   ↓
Model
   ↓
Agent Binding
```

因此不得无检查级联删除整个关系树。

---

# 64. Model 删除约束

Model 被以下对象引用时：

```text
AgentModelBinding
ProfileAgentBinding
```

不得直接物理删除。

必须先解除引用。

---

# 65. Agent 删除约束

Agent 被 Profile 引用时，不允许直接删除。

必须处理：

```text
ProfileAgentBinding
```

后才能删除。

---

# 66. Profile 删除约束

Active Profile 不允许直接删除。

必须：

```text
先切换 Active Profile
```

或者：

```text
明确取消 Active Profile
```

---

# 67. Credential 删除约束

Credential 被 Provider 引用时：

```text
允许删除 Secret
```

但 Provider 必须进入：

```text
Credential Missing
```

状态。

CAS 不应因为 Secret 缺失自动删除 Provider。

---

# 68. Provider Status

推荐 Provider 派生状态：

```text
READY
DISABLED
MISSING_CREDENTIAL
INVALID_CONFIGURATION
UNREACHABLE
UNKNOWN
```

其中：

```text
UNREACHABLE
```

属于运行时诊断结果。

建议不要永久作为 Provider 主状态源。

---

# 69. Model Status

推荐：

```text
READY
DISABLED
INCOMPATIBLE
UNKNOWN
```

其有效性由：

```text
Provider
Compatibility
Capability
```

共同决定。

---

# 70. Agent Status

Agent 本身保存：

```text
enabled
```

其运行可用状态应派生：

```text
AgentAvailability
```

例如：

```text
READY
DISABLED
MODEL_MISSING
PROVIDER_UNAVAILABLE
INCOMPATIBLE_MODEL
INVALID_CONFIGURATION
```

不得将所有派生状态永久写回 Agent 表。

---

# 71. Profile Status

Profile 可以派生：

```text
READY
INCOMPLETE
INVALID
ACTIVE
```

例如：

```text
executor → Model A ✓
reviewer → Missing Model ✗
```

则：

```text
Profile = INCOMPLETE
```

---

# 72. Domain Validation

数据模型需要支持以下验证链：

```text
Provider Validation
       ↓
Model Validation
       ↓
Agent Binding Validation
       ↓
Profile Validation
```

例如：

```text
Agent requires:
WORKSPACE_WRITE
TOOL_CALLING
MULTI_AGENT
```

绑定模型不具备：

```text
TOOL_CALLING
```

则：

```text
Binding Invalid
```

而不是等 Codex Runtime 报错。

---

# 73. Capability Requirement

Agent 可以定义：

```text
requiredCapabilities
preferredCapabilities
```

推荐放入：

```text
AgentRequirement
```

值对象。

例如：

```text
Executor

required:
- TOOL_CALLING
- MULTI_AGENT

preferred:
- PARALLEL_TOOL_CALLING
- LARGE_CONTEXT
```

这样模型兼容性判断不依赖 Agent 名称。

---

# 74. AgentRequirement

结构：

```text
AgentRequirement
├── requiredCapabilities
├── preferredCapabilities
└── minimumContextWindow
```

Agent 不应该写：

```text
if executor then DeepSeek
```

而应该表达：

```text
executor 需要什么能力
```

然后由模型能力判断是否适合。

---

# 75. CompatibilityIssue

所有兼容问题统一使用：

```text
CompatibilityIssue
```

结构：

```text
CompatibilityIssue
├── code
├── severity
├── message
└── source
```

Severity：

```text
INFO
WARNING
ERROR
```

例如：

```text
code:
MODEL_NO_PARALLEL_TOOL_CALL

severity:
WARNING
```

---

# 76. Provider Connection Check

Provider 连接测试结果不属于 Provider 主实体。

定义临时/历史模型：

```text
ProviderCheckResult
├── providerId
├── status
├── latency
├── errorCode
├── checkedAt
└── details
```

避免：

```text
每一次 ping
```

都更新 Provider 核心字段。

---

# 77. Model Discovery Result

模型发现属于外部读取结果：

```text
ModelDiscoveryResult
├── providerId
├── models
├── discoveredAt
└── source
```

只有用户确认或自动同步策略允许时，才转为：

```text
Model Entity
```

---

# 78. 数据来源分类

系统中的数据来源必须明确分为：

```text
CAS Managed
User Defined
Preset
Imported
External Discovered
Derived
```

不能把：

```text
发现到的数据
```

和：

```text
CAS 正式管理的数据
```

混在一起。

---

# 79. 持久化模型与领域模型

建议逻辑上区分：

```text
Domain Model
```

与：

```text
Persistence Model
```

例如 Domain：

```text
Provider
```

并不要求 1:1 对应数据库表。

持久化实现可以为了查询和约束拆分为：

```text
providers
provider_capabilities
provider_headers
```

但这些属于数据库设计阶段。

本数据模型只定义：

```text
业务语义
关系
约束
```

---

# 80. 核心模型总览

```text
Provider
│
├── CredentialReference
├── Protocol
├── CapabilitySet
└── Model[]
        │
        ├── ReasoningCapability
        ├── CapabilitySet
        └── ModelCompatibility


Agent
│
├── AgentRequirement
├── SandboxPolicy
├── ReasoningPolicy
└── AgentModelBinding
          │
          └── Model


Profile
└── ProfileAgentBinding[]
        │
        ├── Agent
        └── Model


Preset
├── ProviderPreset
└── AgentPreset


Configuration Management
├── ManagedResource
└── ConfigurationSnapshot
```

---

# 81. V0.1 必须持久化的核心实体

V0.1 建议真正进入业务持久化的实体：

```text
Provider
CredentialReference
Model
Agent
AgentModelBinding
ManagedResource
ConfigurationSnapshot
ConfigurationState
ApplyTransaction
ApplicationSetting
```

以下可以先作为内置静态资源：

```text
ProviderPreset
AgentPreset
ModelDefinition
```

无需一开始数据库化。

---

# 82. V0.1 可以动态计算的数据

以下数据优先计算，不重复保存：

```text
ProviderAvailability
ModelAvailability
AgentAvailability
EffectiveCapability
```

原因：

这些状态依赖多个数据源。

重复持久化容易产生：

```text
状态不一致
```

---

# 83. 不进入数据库的敏感信息

以下内容禁止进入 CAS 普通数据库：

```text
API Key
Bearer Token
Access Token
Refresh Token
Password
Private Key
完整 Secret Header Value
```

数据库中只能出现：

```text
credentialRef
```

---

# 84. 不作为核心业务数据保存的内容

以下数据不得成为 CAS Core 的 Source of Truth：

```text
完整 config.toml 副本
完整 agents/*.toml 副本
Codex 运行时 Session
Agent 对话内容
LLM Prompt 历史
LLM Response
代码 diff
Shell 输出
Token 使用统计
```

除非未来出现独立需求，再单独设计对应领域。

---

# 85. 数据一致性规则

必须保证以下关系长期成立。

### 规则一

```text
Model.providerId
必须引用有效 Provider。
```

### 规则二

```text
AgentModelBinding.agentId
必须引用有效 Agent。
```

### 规则三

```text
AgentModelBinding.modelId
必须引用有效 Model。
```

### 规则四

```text
ProfileAgentBinding.profileId
必须引用有效 Profile。
```

### 规则五

```text
ProfileAgentBinding.agentId
必须引用有效 Agent。
```

### 规则六

```text
ProfileAgentBinding.modelId
必须引用有效 Model。
```

### 规则七

```text
Provider credentialRef
只能引用属于该 Provider 的有效凭据。
```

### 规则八

```text
一个 Profile 中，
同一个 Agent 最多存在一个有效 Model Binding。
```

### 规则九

```text
同一时刻最多一个 Profile 为 Active。
```

### 规则十

```text
CAS-owned ManagedResource
必须可以追溯到对应 CAS Domain Entity。
```

---

# 86. 删除策略

核心实体默认采用：

```text
显式删除 + 引用检查
```

而不是默认：

```text
数据库级联物理删除
```

因为 Provider、Model、Agent 和 Profile 存在较强引用关系。

删除流程必须优先保证：

```text
不存在悬空引用
```

---

# 87. CAS 内部核心关系

最终核心逻辑模型保持：

```text
                ┌────────────┐
                │  Provider  │
                └─────┬──────┘
                      │
                     1:N
                      │
                      ▼
                ┌────────────┐
                │   Model    │
                └─────┬──────┘
                      │
                      │
          ┌───────────┴───────────┐
          │                       │
          ▼                       ▼
┌──────────────────┐    ┌────────────────────┐
│ AgentModelBinding│    │ProfileAgentBinding │
└────────┬─────────┘    └─────────┬──────────┘
         │                         │
         ▼                         ▼
     ┌───────┐                ┌─────────┐
     │ Agent │                │ Profile │
     └───────┘                └─────────┘
```

辅助关系：

```text
Provider
   │
   └── CredentialReference


Provider / Model
   │
   └── Capability


Agent
   │
   └── AgentRequirement


CAS Domain
   │
   └── ManagedResource
            │
            ▼
       Codex Config
```

---

# 88. 数据模型核心不变量

项目开发过程中必须保持以下不变量。

### 不变量一

```text
Agent 不能拥有 Provider Credential。
```

### 不变量二

```text
Agent 不能以 Provider 名称表达自身身份。
```

### 不变量三

```text
Model 必须属于一个 Provider。
```

### 不变量四

```text
Secret 不得明文进入普通数据库。
```

### 不变量五

```text
Profile 只能组合现有 Agent 与 Model，
不能复制它们。
```

### 不变量六

```text
Codex Configuration 不是 CAS Domain Entity。
```

### 不变量七

```text
外部发现的数据不能自动成为 CAS-managed 数据。
```

### 不变量八

```text
Capability 应通过统一模型表达，
不得依赖 Provider 名称判断。
```

### 不变量九

```text
CAS-owned Resource 必须能够识别和追踪。
```

### 不变量十

```text
删除任何核心实体不得产生悬空引用。
```

---

# 89. 最终模型定义

CAS 的长期数据模型围绕四个核心对象；V0.1 只实现前三个，Profile 在 P1 加入：

```text
Provider
Model
Agent
Profile
```

建立。

其中：

```text
Provider
回答：
模型从哪里调用？
```

```text
Model
回答：
实际调用哪个模型？
```

```text
Agent
回答：
这个 Codex Subagent 负责什么？
```

```text
Profile
回答：
当前整套 Agent Team 如何组合？
```

四者通过：

```text
Credential
Capability
Binding
Compatibility
ManagedResource
Snapshot
```

等辅助模型连接。

最终形成：

```text
Profile
   │
   ▼
Agent
   │
   ▼
Model
   │
   ▼
Provider
   │
   ▼
Protocol
```

同时保持：

```text
Credential
Capability
Compatibility
Ownership
```

作为横向约束。

该模型必须保证：

> 新增 Provider 不需要修改 Agent 数据结构。

> 新增 Model 不需要修改 Agent Role。

> 切换 Model 不需要重新创建 Agent。

> 切换 Profile 不需要修改 Provider。

> Codex 配置格式变化不应该导致核心业务数据模型发生结构性变化。

这五点是 CAS 数据模型长期稳定性的核心。
