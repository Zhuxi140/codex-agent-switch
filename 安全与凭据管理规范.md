# Codex Agent Switch 安全与凭据管理规范

> 文档类型：Security & Credential Management Specification  
> 项目暂定名称：Codex Agent Switch  
> 简称：CAS  
> 当前目标版本：V0.1  
> 文档职责：定义 CAS 的安全边界、凭据生命周期、Secret Store、Credential Helper、认证注入、敏感信息处理、权限控制、日志脱敏、网络安全、备份与导出安全以及相关威胁模型。

---

# 0. V0.1 安全基线

V0.1 正式平台为 Windows，唯一 CAS-managed Secret Backend 是 Windows Credential Manager。macOS Keychain / Linux Secret Service 章节保留为 P1 平台规范，不属于首版实现与验收。

DeepSeek V4 Flash Preset 与 Custom Responses Provider 共用同一 CredentialReference、SecretService、`cas-helper` 和脱敏链路。DeepSeek 官方 Codex 示例中的 `experimental_bearer_token` 会把 Secret 写入 TOML，CAS 不采用；CAS 仍使用 command-backed Bearer Token，且 `auth.command` 只能是安装层解析的稳定绝对路径。

发布前必须真实验证 `Codex → cas-helper token <uuid> → Windows Credential Manager → DeepSeek Responses API`，并验证升级后旧配置中的 helper 路径继续有效。

---

# 1. 文档目标

CAS 需要管理第三方模型 Provider 的：

```text
API Key
Bearer Token
以及未来可能出现的其他认证凭据
```

这些凭据最终需要被：

```text
CAS
或
Codex
```

用于访问模型 Provider。

安全设计必须解决：

```text
Secret 保存在哪里？

Codex 怎样拿到 Secret？

Secret 是否会进入 config.toml？

Secret 是否会进入 SQLite？

Secret 是否可能出现在日志？

CAS Desktop 如何处理用户输入的 API Key？

cas-helper 应拥有什么权限？

怎样避免 API Key 出现在进程参数？

怎样避免 PATH 劫持 helper？

Credential 替换、删除如何处理？

备份和导出是否包含 Secret？

系统凭据库不可用怎么办？

Provider 请求怎样避免意外泄漏认证信息？

哪些攻击属于 CAS 能防御的范围？
```

核心原则：

> Secret 在整个 CAS 系统中必须遵循“最少存储、最少暴露、最短生命周期、最小权限”的原则。

---

# 2. 安全目标

CAS V0.1 必须优先保证以下目标。

## 2.1 Secret 不明文进入普通持久化

禁止写入：

```text
cas.db
config.toml
agents/*.toml
model catalog
application settings
backup manifest
普通日志
crash report
export file
```

---

## 2.2 Secret 不通过命令参数传递

禁止：

```text
cas-helper token sk-xxxxx
```

禁止：

```text
codex --api-key sk-xxxxx
```

因为命令参数可能暴露于：

```text
进程列表
Shell 历史
诊断工具
崩溃报告
```

---

## 2.3 Secret 不依赖 PATH 中的未知程序读取

Codex Provider 的：

```text
auth.command
```

必须指向 CAS 安装的可信 helper。

不得默认：

```toml
command = "cas-helper"
```

依赖 PATH 搜索。

应使用：

```text
绝对路径
```

定位 helper。

---

## 2.4 Credential 与 Provider 配置分离

Provider 保存：

```text
Credential Reference
```

而不是：

```text
Credential Value
```

---

## 2.5 CAS 不接管 Codex 官方认证

CAS 不读取、不复制、不修改：

```text
Codex auth.json
ChatGPT OAuth Token
OpenAI Login Credential
```

CAS 第三方 Provider 凭据体系必须与 Codex 官方认证体系分离。

---

# 3. 当前 Codex Credential 集成能力

Codex 当前支持 Custom Provider 使用：

```toml
[model_providers.<id>.auth]
command = "..."
args = [...]
```

作为 command-backed bearer token 获取方式。

Codex 在需要 Bearer Token 时执行该程序，并从该程序的 stdout 读取 Token。

该认证方式不能同时与：

```text
env_key
experimental_bearer_token
requires_openai_auth
```

组合使用。

Codex 当前同时提供 `env_key` 环境变量认证，而直接使用 `experimental_bearer_token` 保存 Token 被官方标记为不推荐。

因此 CAS V0.1 默认采用：

```text
Codex
  ↓
auth.command
  ↓
cas-helper
  ↓
OS Secret Store
```

作为第三方 Bearer Credential 的主要安全链路。

---

# 4. 总体 Credential 架构

```text
                    User
                      │
                      │ 输入 Secret
                      ▼
              CAS Desktop / CLI
                      │
                      │ transient
                      ▼
                 SecretService
                      │
                      ▼
              OS Secret Storage
                      │
                      │
               ┌──────┴──────┐
               │             │
               ▼             ▼
        Provider Test     cas-helper
                              │
                              │ stdout
                              ▼
                            Codex
                              │
                              ▼
                        Provider API
```

普通业务数据库只保存：

```text
CredentialReference
```

关系：

```text
SQLite
  │
  └── credential_id
           │
           ▼
      OS Secret Store
           │
           └── actual secret
```

---

# 5. Secret 分类

CAS 将敏感认证信息统一归类为：

```text
Secret
```

V0.1 至少考虑：

```text
API_KEY

BEARER_TOKEN
```

未来可能增加：

```text
ACCESS_TOKEN

REFRESH_TOKEN

CLIENT_SECRET

PRIVATE_KEY

CUSTOM_SECRET
```

不同类型可以共享 Secret Store，但不能假设：

```text
所有 Secret 都具有相同生命周期。
```

---

# 6. Credential Reference

业务系统只引用：

```text
CredentialId
```

例如：

```text
0198ae47-...
```

Credential ID：

```text
不是 Secret
```

可以安全存在于：

```text
SQLite
Codex auth.command args
日志
ManagedResource
```

但仍应避免无意义公开。

---

# 7. Secret Locator

推荐使用固定 Namespace：

```text
Application Namespace:
CodexAgentSwitch

Credential Namespace:
credential:<UUID>
```

例如：

```text
CodexAgentSwitch
credential:0198ae47-...
```

这样 helper 不需要知道：

```text
DeepSeek
OpenRouter
Provider Name
API Key
```

只需知道：

```text
Credential ID
```

---

# 8. 不使用 Provider Name 作为 Secret Key

错误：

```text
CodexAgentSwitch:DeepSeek
```

原因：

```text
Provider 可以重命名

可能存在多个 DeepSeek Provider

名称可能冲突
```

正确：

```text
CodexAgentSwitch:credential:<UUID>
```

---

# 9. OS Secret Store

V0.1 仅实现下列 Windows 路径；其余平台映射从 P1 起实现。

V0.1 默认依赖操作系统安全凭据存储。

平台 Adapter：

```text
SecretStorage
│
├── WindowsSecretStorage
├── MacOSSecretStorage
└── LinuxSecretStorage
```

推荐映射：

```text
Windows
→ Windows Credential Manager

macOS
→ macOS Keychain

Linux
→ Secret Service
```

Domain 和 Application 不直接调用平台 API。

---

# 10. Windows Credential Storage

Windows 推荐使用：

```text
Windows Credential Manager
```

逻辑 Target：

```text
CodexAgentSwitch:credential:<UUID>
```

Credential 应属于：

```text
当前用户
```

CAS 不应：

```text
写入系统范围共享 Credential
```

除非未来有明确企业部署需求。

---

# 11. macOS Credential Storage

macOS 推荐使用：

```text
Generic Password Item
```

逻辑：

```text
service:
CodexAgentSwitch

account:
credential:<UUID>
```

Secret：

```text
API Key / Token
```

---

# 12. Linux Credential Storage

Linux 推荐通过：

```text
Secret Service API
```

访问桌面环境的 Secret Store。

逻辑 Attribute：

```text
application = codex-agent-switch

credential_id = <UUID>
```

具体实现由 Platform Adapter 隔离。

---

# 13. Linux Secret Service 不可用

某些环境可能不存在可用：

```text
Secret Service
```

例如：

```text
部分 headless Linux

极简桌面环境

某些 WSL 环境
```

CAS 不得因此自动降级成：

```text
plaintext credential file
```

---

# 14. 禁止静默明文降级

错误：

```text
Secret Service unavailable
        ↓
自动写入 credentials.json
```

这是明确禁止的。

正确行为：

```text
Secret Store unavailable

→ 报告安全能力不可用

→ 禁止默认持久化 Secret

→ 提供安全兼容模式
```

---

# 15. V0.1 安全兼容模式

当 OS Secret Store 不可用时，可以支持：

```text
External Environment Variable
```

模式。

即：

```text
用户自己管理环境变量
        ↓
Codex env_key
```

CAS 只保存：

```text
环境变量名称
```

不保存其值。

---

# 16. 不实现明文 File Vault

V0.1 不提供：

```text
credentials.json
.env
secret.toml
plaintext vault
```

作为 Credential Store。

---

# 17. 不实现伪加密 Vault

禁止：

```text
API Key
  ↓
硬编码 AES Key
  ↓
credentials.enc
```

这种看似加密、实际密钥与程序一起发布的方案。

这并没有提供有效安全边界。

---

# 18. 自研加密 Vault

如果未来确实需要：

```text
Headless Linux
Portable Mode
```

的本地 Vault，应单独设计：

```text
Master Password
KDF
Authenticated Encryption
Key Derivation
Memory Hardening
Recovery
```

不属于 V0.1。

---

# 19. Credential 生命周期

标准 Credential 生命周期：

```text
Create
  ↓
Store
  ↓
Reference
  ↓
Use
  ↓
Replace / Rotate
  ↓
Revoke / Delete
```

每一步都必须避免 Secret 扩散。

---

# 20. Credential 创建

用户输入 Secret 后：

```text
UI / CLI
   ↓
Backend SecretService
   ↓
Validate
   ↓
OS Secret Store
   ↓
Create CredentialReference
```

必须保证：

```text
Secret Store 写入失败
```

时：

```text
不得创建一个看似有效的 CredentialReference。
```

---

# 21. Secret 输入验证

Secret 被视为：

```text
opaque sensitive string
```

CAS 不应过度猜测 Provider Key 格式。

至少验证：

```text
非空

不包含 NUL

不包含 CR

不包含 LF

长度处于合理安全范围
```

不得：

```text
自动 trim
```

后再保存。

因为：

```text
CAS 不应该擅自改变 Secret 内容。
```

---

# 22. Secret 最大长度

V0.1 应设置防御性最大长度。

例如：

```text
16 KiB
```

数量级。

避免异常输入导致：

```text
IPC 滥用
日志异常
内存异常
Secret Store API 异常
```

具体常量由实现定义。

---

# 23. Credential 保存成功条件

只有：

```text
Secret 已成功进入 Secret Store

+

Credential Reference 已成功持久化
```

才能返回：

```text
Credential Configured
```

---

# 24. Secret Store 成功但数据库失败

例如：

```text
OS Secret Store
写入成功

SQLite
写 CredentialReference 失败
```

应执行：

```text
补偿删除刚创建的 Secret
```

如果补偿失败：

记录：

```text
ORPHAN_SECRET
```

并进入安全清理流程。

不得在日志记录 Secret 本身。

---

# 25. Credential 读取

普通 Application Service 不应随意：

```text
getSecret()
```

Secret 读取必须限制在明确场景：

```text
Provider Connection Test

Model Probe

cas-helper Credential Delivery

Credential Replacement Validation
```

---

# 26. Secret Retrieval Interface

推荐不要暴露：

```text
String getSecret(id)
```

给整个应用。

而提供受限接口：

```text
withSecret(id, callback)

resolveSecretForProvider(id)

writeTokenToStdout(id)
```

减少：

```text
Secret 被长期持有或随意 clone
```

的机会。

---

# 27. Secret 内存类型

Rust 中应使用专门敏感值类型，例如：

```text
SecretString
SecretVec
```

并结合：

```text
zeroize
```

等机制降低内存残留。

不得将 Secret 普遍作为：

```text
String
```

在系统各层传递。

---

# 28. Zeroization 边界

内存清零属于：

```text
best effort
```

不能向用户承诺：

> API Key 在任何时候都绝不会残留在 RAM。

因为：

```text
HTTP library
OS API
WebView
allocator
Codex
```

都可能存在不可控副本。

设计目标应是：

```text
主动减少 Secret 生命周期和复制次数。
```

---

# 29. 禁止 Debug 输出 Secret

Secret 类型不得实现会打印明文的：

```text
Debug
Display
```

例如：

```rust
#[derive(Debug)]
struct Credential {
    api_key: String
}
```

这种模型不应出现在安全核心路径。

---

# 30. Credential Replace

用户替换 API Key 时，推荐保持：

```text
Credential ID 不变
```

仅替换 OS Secret Store 中：

```text
credential:<UUID>
```

对应 Secret。

这样：

```text
Codex config.toml
auth.command args
ManagedResource
```

无需改变。

---

# 31. Credential Replace 流程

推荐：

```text
Receive New Secret
        ↓
Validate
        ↓
Write / Replace Secure Item
        ↓
Verify Secret Exists
        ↓
Clear Temporary Memory
        ↓
Mark Credential Updated
```

---

# 32. Credential Rotation 优势

由于 Codex 配置仍然引用：

```text
Credential ID
```

而不是 Secret：

```text
Key Rotation
```

通常不需要重新生成 Provider 配置。

---

# 33. Credential Delete

删除 Credential：

```text
删除 OS Secret
```

和：

```text
删除 CredentialReference
```

必须被视为两个步骤。

如果 Provider 仍然存在：

可以允许：

```text
Credential Missing
```

状态。

不得因此自动删除 Provider。

---

# 34. 删除失败

如果：

```text
Secret Store Delete
```

失败：

不得告诉用户：

```text
Credential 已安全删除。
```

应该返回：

```text
CREDENTIAL_DELETE_FAILED
```

---

# 35. Provider 删除

Provider 删除时，可以尝试同时删除其 Secret。

顺序推荐：

```text
确认 Provider 可删除
      ↓
删除 / 失效 Codex Provider Reference
      ↓
删除业务 CredentialReference
      ↓
删除 Secret
```

具体业务事务由相关业务规范协调。

---

# 36. Credential Orphan

CAS 应能检测：

```text
Secret 存在
但 CredentialReference 不存在
```

或者：

```text
CredentialReference 存在
但 Secret 不存在
```

两者不是同一个问题。

---

# 37. Missing Secret

```text
CredentialReference
     ↓
Secret not found
```

状态：

```text
MISSING_SECRET
```

Provider 不应被自动删除。

---

# 38. Orphan Secret

```text
Secret exists
     ↓
No CAS CredentialReference
```

属于：

```text
Potential Orphan
```

清理必须谨慎。

不得：

```text
扫描 Secret Store
发现 CAS namespace
直接全部删除
```

---

# 39. `cas-helper`

`cas-helper` 是一个独立最小权限程序。

唯一核心职责：

> 根据非敏感 Credential ID，从 OS Secret Store 读取 Secret，并将 Token 提供给 Codex。

---

# 40. Helper 不属于 Desktop Runtime

`cas-helper` 不应依赖：

```text
React

WebView

完整 Tauri Runtime

Profile Service

Agent Service

Configuration Compiler

Provider Discovery

SQLite ORM 全套能力
```

保持：

```text
小
稳定
低依赖
低攻击面
```

---

# 41. Helper 最小依赖

推荐逻辑依赖：

```text
cas-helper
│
├── credential-id parser
├── secret-storage abstraction
├── platform adapter
└── secure output
```

理想情况下：

```text
不需要网络权限。
```

---

# 42. Helper 命令接口

V0.1 固定：

```text
cas-helper token <credential-id>
```

除此之外不提供任意命令执行能力。

---

# 43. Helper 参数

允许参数：

```text
token

UUID Credential ID
```

不得接受：

```text
API Key

Provider supplied shell arguments

任意文件路径

任意 Secret Store key

Shell expression
```

---

# 44. Credential ID 校验

Helper 必须严格验证：

```text
credential-id
```

格式。

例如：

```text
UUID
```

非法输入立即失败。

禁止通过：

```text
../../
:
shell metacharacters
arbitrary locator
```

访问其他 Secret。

---

# 45. Secret Namespace 固定

Helper 根据 Credential ID 自动构造：

```text
CodexAgentSwitch:credential:<UUID>
```

而不是接受：

```text
用户直接传 storage_key。
```

这样可以防止 helper 被利用读取：

```text
其他应用的 Secret。
```

---

# 46. Helper stdout 契约

成功时 stdout：

```text
只输出 Secret
```

例如：

```text
sk-xxxxxxxx
```

允许必要终止换行。

不得输出：

```text
Token:
Using credential...
Success
Debug info
Provider name
```

因为 Codex 会将 stdout 作为 Token 来源。

Codex 当前要求认证命令把 Token 输出至 stdout。

---

# 47. Helper stderr

错误和诊断只能写：

```text
stderr
```

且不得包含 Secret。

例如：

```text
Credential not found.
```

而不是：

```text
Unable to use token sk-xxxx.
```

---

# 48. Helper Exit Code

推荐：

```text
0
Success

2
Invalid arguments

3
Credential not found

4
Secret store unavailable

5
Permission denied

6
Secret retrieval failed
```

具体退出码由 CLI/Helper 接口规范最终确定。

---

# 49. Helper 不缓存 Secret

`cas-helper`：

```text
读取
↓
输出
↓
退出
```

不得：

```text
长期驻留
缓存全部 API Key
启动本地 Secret Server
```

V0.1 不做 Credential Daemon。

---

# 50. Codex Token Cache 边界

Secret 被输出给 Codex 后：

```text
CAS 无法控制 Codex 内部如何在进程生命周期保存该 Token。
```

安全边界必须明确：

```text
CAS 负责安全交付

Codex 负责收到 Token 后的运行时处理。
```

---

# 51. `auth.command` 安全配置

CAS 生成：

```toml
[model_providers.cas_xxx.auth]
command = "<ABSOLUTE_CAS_HELPER_PATH>"
args = ["token", "<credential-id>"]
```

禁止：

```toml
command = "cas-helper"
```

作为默认生成方式。

---

# 52. 禁止 PATH Lookup

原因：

攻击者或错误环境可能在 PATH 中放置：

```text
cas-helper
```

替代程序。

如果 Codex 执行错误程序：

```text
Credential 流程可能被劫持。
```

---

# 53. Helper Path

Configuration Writer 必须获取：

```text
真实安装 helper 的绝对路径
```

并进行：

```text
路径规范化
```

之后写入 Codex 配置。

---

# 54. 禁止 Shell Wrapper

不要生成：

```toml
command = "sh"
args = ["-c", "cas-helper token ..."]
```

也不要：

```toml
command = "cmd.exe"
args = ["/c", "..."]
```

标准情况下应直接执行：

```text
cas-helper executable
```

避免：

```text
Shell expansion
Command injection
Quoting ambiguity
```

---

# 55. Preset 不能控制 `auth.command`

Provider Preset 不允许提供：

```text
auth.command
```

任意可执行程序。

CAS 自己决定：

```text
Credential Helper
```

路径。

否则恶意 Preset 可以诱导 Codex：

```text
执行任意程序。
```

---

# 56. Custom Provider 不能注入命令

V0.1 用户创建 Custom Provider 时不得提供：

```text
Custom Credential Command
```

作为普通配置项。

这是：

```text
代码执行能力
```

而不是普通 Provider 数据。

---

# 57. Codex Auth Mode 互斥

CAS-managed Provider 使用：

```text
auth.command
```

时，不生成：

```text
env_key

experimental_bearer_token

requires_openai_auth
```

Codex 官方当前明确要求这些认证方式不能与 command-backed auth 混用。

---

# 58. 禁止 `experimental_bearer_token`

CAS V0.1 不生成：

```toml
experimental_bearer_token = "..."
```

Codex 当前也将直接 bearer token 配置标记为 discouraged。

---

# 59. Environment Variable 模式

作为高级兼容模式，可以生成：

```toml
env_key = "DEEPSEEK_API_KEY"
```

Codex 当前支持通过 `env_key` 指定 Provider API Key 环境变量。

但：

```text
CAS 不保存该环境变量值。
```

---

# 60. Environment Variable 风险

环境变量可能：

```text
被子进程继承

被调试工具读取

被错误日志打印

影响多个应用进程
```

因此：

```text
Environment Variable Mode
```

不是 CAS 推荐的默认 Credential Strategy。

---

# 61. Secret Header

某些 Provider 不使用：

```text
Authorization: Bearer
```

而需要：

```text
X-API-Key
```

等自定义 Secret Header。

当前 Codex提供 `env_http_headers` 从环境变量注入 Header，但 command-backed `auth` 专门解决 Bearer Token。

---

# 62. V0.1 Custom Header Credential 边界

为了避免 CAS 把 Secret 明文写入：

```text
http_headers
```

V0.1 不提供“OS Secret Store → arbitrary Codex HTTP header”的通用自动注入。

对于必须依赖自定义 Secret Header 的 Provider：

```text
V0.1
→ Environment Variable Advanced Mode

或

→ 当前不支持一键安全接入
```

未来可以通过：

```text
CAS Gateway
```

安全处理。

---

# 63. 禁止 Secret 放入 Static Header

禁止生成：

```toml
http_headers = {
    "X-API-Key" = "secret-value"
}
```

即使 Codex 允许静态 Header。

---

# 64. Secret 不进入 URL

禁止：

```text
https://user:password@provider.example.com
```

禁止：

```text
https://provider.example.com/?api_key=secret
```

Credential 不应编码到：

```text
Base URL
Query Params
Path
```

---

# 65. Provider Base URL 安全

Custom Provider Base URL 必须解析为合法 URL。

默认允许：

```text
https://
```

---

# 66. HTTP Endpoint

对于：

```text
http://
```

默认只建议允许：

```text
localhost

127.0.0.0/8

::1
```

等明确本地开发场景。

远程 HTTP Endpoint 应：

```text
默认拒绝
```

或者要求明确启用不安全高级模式。

---

# 67. TLS

远程 Provider 请求默认必须：

```text
验证 TLS Certificate
```

禁止 V0.1 提供：

```text
Disable TLS Verification
Accept Any Certificate
```

这样的普通开关。

---

# 68. 自签名证书

企业自签名证书需求未来应通过：

```text
系统可信 CA
```

或专门安全设计支持。

不得简单：

```text
danger_accept_invalid_certs = true
```

解决。

---

# 69. Redirect

携带 Authorization 的请求应：

```text
禁止跨 Origin 自动 Redirect
```

防止认证头意外发送给其他主机。

Provider API 正常请求不应依赖跨站认证 Redirect。

---

# 70. Endpoint Scheme

V0.1 Provider API 不允许：

```text
file://

ftp://

javascript:

data:
```

等非 HTTP(S) Scheme。

---

# 71. URL UserInfo

Base URL 中如果存在：

```text
username:password@
```

应拒绝。

Authentication 必须走 Credential 系统。

---

# 72. Cloud Metadata Endpoint

Custom Provider 允许访问用户明确配置的本地网络服务。

但应特别防止无意配置到：

```text
云实例 metadata service
```

等敏感 link-local endpoint。

至少应：

```text
识别常见 link-local metadata address
+
警告或拒绝
```

除非未来提供明确安全 override。

---

# 73. Provider Preset Endpoint

V0.1 内置 Provider Preset：

```text
随应用发布
```

其 Endpoint 必须经过项目 Review。

Preset 不允许动态执行网络脚本来决定 Endpoint。

---

# 74. Tauri 安全边界

Desktop 前端属于：

```text
Presentation Layer
```

不得拥有直接 Secret Store 权限。

正确：

```text
React
  ↓
Tauri Command
  ↓
Secret Application Service
  ↓
Secret Store
```

---

# 75. 前端不得读取已有 Secret

React 前端只能知道：

```text
Configured
Missing
```

不得调用：

```text
getApiKey()
```

取得已保存 Secret。

---

# 76. 用户输入 Secret

新增或替换 Credential 时：

```text
Secret
```

会暂时存在于 WebView / React 内存。

V0.1 必须尽量缩短生命周期。

---

# 77. 前端 Secret State

Secret 输入不得保存到：

```text
localStorage

sessionStorage

IndexedDB

Redux persistence

application settings

form autosave

URL

query string
```

---

# 78. Submit 后清理

Credential 提交完成后：

```text
清空 React State

清空 Input Value

释放相关引用
```

即使后端保存失败，也不要长期保留 Secret。

用户需要重新输入时可以重新填写。

---

# 79. 前端日志

禁止：

```javascript
console.log(apiKey)
```

也禁止：

```text
debug form state
```

中包含 Credential。

---

# 80. Tauri IPC 日志

Secret Command 必须明确标记：

```text
Sensitive
```

任何自动 RPC / tracing middleware 不得记录：

```text
request payload
```

---

# 81. Rust Tracing

涉及 Secret 的函数推荐：

```text
skip(secret)
```

处理。

例如概念：

```text
#[instrument(skip(secret))]
```

避免 tracing 自动记录敏感参数。

---

# 82. Production DevTools

正式发行 Desktop 默认不应开启：

```text
WebView Developer Tools
```

作为普通用户功能。

开发版本与 Production 必须区分。

---

# 83. Remote Script

CAS Desktop 主 WebView 不加载：

```text
远程 JavaScript
远程 React Bundle
```

应用代码应：

```text
本地打包
```

防止远程资源获得与 CAS WebView 相同的高权限上下文。

---

# 84. Content Security Policy

Desktop 应配置严格：

```text
CSP
```

至少限制：

```text
script-src

connect-src

object-src

frame-src
```

不允许：

```text
任意第三方脚本
eval
无约束 iframe
```

---

# 85. 外部链接

文档 URL、Provider 官网等外部链接应：

```text
调用系统默认浏览器
```

而不是在具有 Tauri 权限的应用 WebView 内打开第三方网页。

---

# 86. Tauri Capability

Tauri Command 权限坚持：

```text
least privilege
```

前端不直接获得：

```text
任意文件系统读取

任意进程执行

任意 Shell

系统 Credential Store

任意环境变量
```

能力。

---

# 87. Shell Plugin

如果项目使用 Tauri Shell 能力：

不得给前端：

```text
任意 command execution
```

白名单必须明确。

Credential Helper 的配置和执行由 Rust Backend 管理。

---

# 88. SecretManager 接口

建议核心接口：

```text
store(id, secret)

replace(id, secret)

exists(id)

delete(id)

withSecret(id, consumer)
```

不推荐向上层提供：

```text
listAllSecrets()

exportSecrets()

getSecretAsString()
```

---

# 89. SecretManager 不负责 Provider 逻辑

它不知道：

```text
DeepSeek

Executor

Model ID
```

只处理：

```text
CredentialId
SecretValue
StorageBackend
```

---

# 90. Provider Test Credential Flow

```text
ProviderService
      ↓
SecretManager
      ↓
SecretValue
      ↓
HTTP Authorization Header
      ↓
Provider
      ↓
SecretValue dropped / zeroized
```

---

# 91. Secret 不经过数据库查询结果输出

Database 返回：

```text
CredentialReference
```

然后 SecretService 独立读取 Secret。

不得设计：

```text
SELECT provider_with_api_key
```

这种 Repository 模型。

---

# 92. HTTP Header 敏感标记

如果使用 Rust `http` 类型，Authorization 等敏感 Header 应尽可能标记：

```text
sensitive
```

帮助 Debug/Tracing 层避免输出。

但不能仅依赖该机制。

---

# 93. HTTP Debug Logging

生产环境禁止开启会记录：

```text
Authorization Header

完整 HTTP Request

完整 Provider Request Headers
```

的 wire-level debug 日志。

---

# 94. Request Body

Provider Probe 的请求内容应：

```text
最小化
```

不要使用：

```text
用户项目内容

真实代码

私有文件
```

测试 Provider Credential。

---

# 95. Provider Test

Credential Connection Test 使用：

```text
无敏感业务数据
```

进行请求。

例如：

```text
模型列表
```

或者：

```text
最小固定测试请求
```

---

# 96. 错误响应

Provider 可能返回：

```text
请求详情
Header
请求 ID
调试内容
```

CAS 不应把完整原始响应：

```text
永久写日志
```

---

# 97. Provider Error Sanitization

普通错误保留：

```text
HTTP status

Provider error code

request id

简短安全 message
```

原始 Response Body：

```text
默认不长期保存
```

或者：

```text
严格截断 + Redaction
```

后用于诊断。

---

# 98. Redaction Engine

日志层应统一实现：

```text
Secret Redaction
```

而不是靠开发者每次记得：

```text
不要打印。
```

---

# 99. Redaction 内容

至少处理：

```text
Authorization

Bearer ...

API Key field

known current Secret values

credential input fields

secret headers

token-like known fields
```

---

# 100. Redaction 不是主要防线

不能因为有：

```text
Redaction
```

就允许代码大量记录：

```text
完整 Request
完整 Config Object
```

正确原则：

```text
先不记录 Secret

Redaction 只是最后防线。
```

---

# 101. Logging Allowed

允许日志：

```text
provider_id

provider_key

model_id

credential_id

operation type

HTTP status

request id

error code

latency

resource path
```

---

# 102. Logging Forbidden

禁止：

```text
Secret Value

Authorization Header

完整 SecretStore entry

完整 User Credential Input

完整 Environment Variable Value
```

---

# 103. Credential ID 日志

Credential ID 不是 Secret。

允许用于：

```text
诊断
关联错误
```

但普通日志不需要频繁输出。

---

# 104. Crash Report

Crash Report 必须遵守：

```text
No Secret
```

禁止包含：

```text
完整 process environment

完整 config dump

完整 request headers

完整 UI form state

完整 Secret Store error object
```

---

# 105. Panic

Rust panic message 不得拼接：

```text
Secret
```

例如禁止：

```text
panic!("invalid token: {}", token)
```

---

# 106. Error Type

安全相关错误只记录：

```text
Credential ID

错误分类

存储后端

平台错误码
```

而不是 Secret。

---

# 107. CLI Secret 输入

CLI 不允许：

```bash
cas provider add --api-key sk-xxxx
```

作为标准功能。

因为 Secret 会进入：

```text
Shell History
Process Arguments
```

---

# 108. CLI 推荐输入

CLI 使用：

```text
interactive hidden input
```

例如：

```text
API Key:
********
```

终端：

```text
echo disabled
```

---

# 109. CLI 自动化

如果未来支持非交互 Credential：

优先：

```text
stdin

existing environment variable

external credential command
```

而不是：

```text
CLI argument。
```

该能力不属于 V0.1 必须范围。

---

# 110. Clipboard

CAS 不提供：

```text
Copy existing API Key
```

能力。

因为应用本身也不应该重新展示已保存 Secret。

---

# 111. Secret UI

Credential 保存后只显示：

```text
Configured
```

或者：

```text
Missing
```

不显示：

```text
完整 Secret

Last 4 characters
```

V0.1 没有显示部分 Secret 的必要。

---

# 112. 为什么不显示 Last 4

部分 Token 信息通常没有实际产品价值。

同时：

```text
任何额外泄漏
```

都不值得。

用户如需确认 Credential，应：

```text
Test Connection
```

---

# 113. Secret 导出

普通：

```text
Export CAS Configuration
```

必须排除：

```text
Secret
```

---

# 114. Export 内容

允许：

```text
Provider Definition

Model

Agent

Profile

Credential Required 标识
```

例如：

```text
credential:
required
```

而不是：

```text
credential:
sk-xxx
```

---

# 115. Import

Import 文件不得默认接受：

```text
plaintext secret
```

字段。

如果发现：

```text
apiKey
token
password
```

等 Secret 字段：

V0.1 应：

```text
拒绝或明确忽略
```

而不是自动导入。

---

# 116. Credential Migration

用户迁移电脑时：

```text
CAS 配置可以迁移
```

但：

```text
Credential 需要在新设备重新设置
```

这是 V0.1 有意设计的安全行为。

---

# 117. Codex Configuration Snapshot

配置 Snapshot 不包含：

```text
CAS Secret Store
```

因为 Secret 本来就不应存在于：

```text
config.toml
agent files
catalog
```

---

# 118. CAS Database Backup

即使：

```text
cas.db
```

不包含 Secret 明文，

数据库 Backup 仍然属于：

```text
Private Application Data
```

因为包含：

```text
Provider Endpoint
Agent Instructions
路径
Credential Reference
```

不得公开上传。

---

# 119. OS Secret Store 不进入 CAS Backup

CAS Backup 不尝试导出：

```text
Windows Credential Manager

macOS Keychain

Linux Secret Service
```

内容。

---

# 120. Restore

Restore CAS Database 后，如果：

```text
CredentialReference
```

存在但：

```text
Secret Store Item
```

不存在：

应显示：

```text
Credential Missing
```

不得自动创建空 Secret。

---

# 121. 文件权限

CAS Data Directory 应尽可能限制为：

```text
当前用户
```

可访问。

Unix 推荐：

```text
directory 0700

private data 0600
```

具体安装文件权限根据用途决定。

---

# 122. Helper 文件权限

`cas-helper` 必须：

```text
可执行
```

但不应允许非授权用户修改安装文件。

发布安装器应尽可能使用平台正常权限模型保护应用二进制。

---

# 123. Windows ACL

Windows 安装和数据目录应使用合理 ACL。

用户私有数据至少避免：

```text
Everyone writable
```

---

# 124. Symlink

Secret Store 本身不依赖文件路径。

对于 helper 路径：

```text
Configuration Writer
```

应使用可信安装路径。

避免用户可控 Provider 数据决定：

```text
helper executable path。
```

---

# 125. Executable Integrity

CAS 不需要在每次 Token 获取时：

```text
自行 Hash 校验 helper
```

但发布版本应支持：

```text
平台代码签名
安装包签名
可信发布渠道
```

以降低二进制被替换风险。

具体签名流程由发布规范定义。

---

# 126. Secret Store Access Failure

Helper 读取失败时：

```text
不得 fallback 到 SQLite
不得 fallback 到 config.toml
不得搜索环境变量猜测
```

应直接失败。

---

# 127. Credential Strategy 明确化

一个 Provider 在任意时刻只能采用一种认证策略：

```text
OS_SECRET_HELPER

EXTERNAL_ENV

NONE
```

未来可以扩展。

禁止同一 Provider 同时尝试多个 Secret 来源：

```text
helper 失败
→ 尝试 env
→ 尝试 config plaintext
```

这种隐式 fallback。

---

# 128. 为什么禁止隐式认证 Fallback

它会导致：

```text
用户不知道实际使用了哪个 Credential

旧 Secret 意外继续生效

删除 Credential 后仍可认证
```

破坏安全可预测性。

---

# 129. Credential Strategy 切换

例如：

```text
OS_SECRET_HELPER
        ↓
EXTERNAL_ENV
```

必须是：

```text
显式配置变化。
```

切换后旧 Secret 是否删除由用户操作和业务规则决定。

---

# 130. Provider Test 与 Auth Strategy

Provider Test 必须使用：

```text
当前 Provider 正式配置的 Credential Strategy
```

不能：

```text
测试时使用 UI 临时 Key
保存后却使用另一套 Credential
```

导致测试结果与实际运行不一致。

新增 Credential 时可以：

```text
先安全保存
→ 再 Test
```

若用户最终取消 Provider 创建，可清理对应 Secret。

---

# 131. Secret Rotation

对于长期 API Key：

推荐用户能够：

```text
Replace Credential
```

而无需：

```text
删除 Provider
重新建立 Agent Binding
```

---

# 132. Short-lived Token

如果未来 Provider 使用：

```text
短期 Access Token
```

应由：

```text
Credential Broker / Auth Adapter
```

产生。

不应该频繁把短期 Token：

```text
写入 OS Secret Store
```

并人工轮换。

---

# 133. Codex Auth Refresh

Codex 当前 command-backed auth 支持：

```text
refresh_interval_ms
```

控制主动刷新，并支持：

```text
0
```

表示仅在认证重试后刷新；默认值为 300000ms。

CAS 应根据 Credential 类型决定策略，而不是把所有 Token 都假设为永久 API Key。

---

# 134. V0.1 Static API Key

V0.1 主要面向：

```text
长期 API Key / Bearer Credential
```

因此无需实现：

```text
OAuth Refresh Token Broker
```

---

# 135. Credential Revocation

CAS 删除本地 Secret：

```text
不等于
```

Provider 服务端 API Key 已被撤销。

用户如果怀疑泄漏，CAS 应明确：

> 还需要在 Provider 控制台撤销或轮换该凭据。

CAS V0.1 不负责第三方 Provider 的远程 Key 生命周期。

---

# 136. Network Client

Provider 请求必须统一通过：

```text
安全 HTTP Client
```

不能各 Provider 自己随意创建不同安全策略。

统一：

```text
TLS validation

Redirect policy

Timeout

Sensitive Header handling

Proxy policy

Logging policy
```

---

# 137. Provider Adapter 不获得 Secret Store

Provider Adapter 不应该：

```text
直接查询 OS Keychain。
```

Application 层先解析：

```text
Credential Handle
```

再以受控方式提供给网络执行层。

---

# 138. Least Privilege

模块权限：

```text
React
→ 无 Secret Store 权限

Agent Service
→ 无 Secret Store 权限

Profile Service
→ 无 Secret Store 权限

Configuration Compiler
→ 无 Secret Value 权限

Codex Config Writer
→ 无 Secret Value 权限

Secret Service
→ Secret Store 权限

Provider Connection Client
→ 临时 Secret 使用权限

cas-helper
→ 单 Credential 读取能力
```

---

# 139. Configuration Compiler 不接触 Secret

Compiler 只知道：

```text
credential_id
credential_strategy
```

生成：

```text
auth.command
args
```

不能知道：

```text
API Key value。
```

---

# 140. Config Writer 不接触 Secret

Config Writer 只写：

```text
Helper Path
Credential ID
Environment Variable Name
```

禁止让：

```text
SecretValue
```

类型进入 Config Writer API。

---

# 141. SQLite Repository 不接触 Secret

CredentialRepository 只存：

```text
CredentialReference
```

不允许：

```text
saveSecretValue()
```

---

# 142. Backup Manager 不接触 Secret

Backup Manager 复制的是：

```text
Codex 配置文件
```

Secret 不应在这些文件中。

如果发现 CAS-managed Provider Fragment 包含疑似：

```text
明文 Secret
```

Diagnostics 应报告：

```text
PLAINTEXT_SECRET_DETECTED
```

---

# 143. 外部 Codex 明文 Secret

用户自己的 External Provider 可能存在：

```toml
experimental_bearer_token = "..."
```

CAS 扫描时必须：

```text
知道该字段敏感
```

但不得：

```text
显示值
记录值
导出值
```

---

# 144. External Secret Preserve

由于 External Config 不属于 CAS：

CAS Patch：

```text
必须保留该字段
```

但读取后不得让其进入：

```text
普通 Domain DTO
日志
UI
```

---

# 145. Sensitive AST Node

Codex TOML Reader 应识别：

```text
experimental_bearer_token

可能的 secret static headers

auth-related sensitive values
```

并标记：

```text
Sensitive Node
```

---

# 146. Config Diff

用户查看：

```text
Configuration Diff
```

时：

敏感节点必须：

```text
***REDACTED***
```

---

# 147. Unknown Sensitive Fields

对于未知字段无法百分百判断敏感性。

因此：

```text
普通日志不输出完整 Config Diff
```

是必要安全原则。

---

# 148. Diagnostic Bundle

如果未来支持：

```text
Export Diagnostic Bundle
```

必须默认：

```text
Redacted
```

至少排除：

```text
Secret

Authorization

Environment Values

auth.json

OS Secret Store

full request headers
```

---

# 149. Telemetry

V0.1 不需要上传：

```text
Credential metadata
```

到远程。

如果未来有 Telemetry：

不得包含：

```text
Credential ID

Secret

Provider custom headers

完整 Base URL query
```

除非有明确隐私设计。

---

# 150. Analytics

本地产品分析也不应：

```text
记录用户 API Key
```

这是绝对禁止项。

---

# 151. Threat Model

CAS V0.1 至少考虑以下威胁。

### T1 — 配置文件泄漏

攻击者或用户分享：

```text
config.toml
```

目标：

```text
不应直接得到 CAS-managed API Key。
```

---

### T2 — SQLite 泄漏

攻击者得到：

```text
cas.db
```

目标：

```text
不能直接获得 API Key。
```

---

### T3 — 日志泄漏

用户上传：

```text
CAS logs
```

到 GitHub Issue。

目标：

```text
Secret 不应出现在日志。
```

---

### T4 — Shell History 泄漏

用户使用 CLI。

目标：

```text
不要求 Secret 作为命令参数。
```

---

### T5 — Process Argument 泄漏

系统其他程序读取：

```text
process command line
```

目标：

```text
只能看到 Credential ID
不能看到 Secret。
```

---

### T6 — PATH Hijacking

恶意或错误 PATH 中存在：

```text
cas-helper
```

目标：

```text
Codex 使用绝对可信 helper 路径。
```

---

### T7 — Provider Redirect 泄漏

恶意 Endpoint 将认证请求 Redirect 到其他域。

目标：

```text
Authorization 不自动跨 Origin 转发。
```

---

### T8 — WebView Exfiltration

远程页面或 XSS 获取用户刚输入的 API Key。

目标：

```text
CAS WebView 不加载远程脚本
严格 CSP
Secret 不长期存在前端
```

---

### T9 — Malicious Preset

恶意 Provider Preset 尝试：

```text
执行命令
修改 helper
读取文件
```

目标：

```text
V0.1 Preset 为纯数据
不能指定 executable。
```

---

### T10 — Backup Leakage

用户分享 Backup。

目标：

```text
CAS-managed Secret 不包含于 Backup。
```

---

# 152. 威胁模型之外

CAS V0.1 不承诺抵御：

```text
root / Administrator 已完全控制系统

攻击者已完全控制当前 OS 用户账户

恶意内核驱动

内存抓取型高权限 Malware

被篡改的操作系统 Credential Store

被篡改的 Codex executable

Provider 服务端自身泄漏 API Key

Codex 收到 Token 后自身发生安全漏洞
```

如果攻击者已经能够：

```text
以同一用户权限任意执行代码
```

通常也可以模拟 CAS / Codex 访问用户凭据。

CAS 仍应采用防御性设计，但不能宣称提供强隔离。

---

# 153. Provider 本身的信任边界

Credential 被配置给：

```text
Provider A
```

后，该 Token 必然需要发送给：

```text
Provider A
```

CAS 无法防止合法目标 Provider 自身：

```text
记录
滥用
泄漏
```

用户需要信任所配置 Provider。

---

# 154. Credential 与 Endpoint 绑定

为了避免用户修改 Provider Base URL 后：

```text
原 Credential
```

被意外发送给另一个服务，

Provider Endpoint 发生：

```text
Origin Change
```

时应产生：

```text
Security-sensitive configuration change
```

---

# 155. Origin Change

例如：

```text
https://api.deepseek.com/
        ↓
https://evil.example.com/
```

即使 Credential ID 没变：

CAS 不应把它视为普通 Display Setting 修改。

应要求明确确认。

---

# 156. Credential Reuse

V0.1 默认：

```text
一个 Provider Credential
服务一个 Provider 配置。
```

不主动提供：

```text
一个 Credential 同时绑定多个 Provider
```

功能。

这样减少：

```text
跨 Provider Secret 泄漏
```

可能。

---

# 157. Same Provider Multiple Instances

用户如果有：

```text
DeepSeek Personal

DeepSeek Company
```

应使用：

```text
两个独立 Credential ID。
```

---

# 158. Secret Fingerprint

CAS V0.1 不需要保存：

```text
SHA-256(API Key)
```

来判断 Credential 是否变化。

因为：

```text
Credential Replace
```

本身已经是显式事件。

减少不必要敏感派生信息。

---

# 159. Credential Metadata

允许保存：

```text
credential id

secret type

storage backend

created_at

updated_at

exists status
```

禁止保存：

```text
length

prefix

suffix

完整 fingerprint
```

除非未来出现明确功能需求。

---

# 160. Connection Test 日志

例如：

```text
Provider DeepSeek connection test succeeded.
```

允许。

禁止：

```text
Authorization Bearer sk-...
```

---

# 161. Model Probe

模型兼容测试同样使用：

```text
SecretManager
```

取得 Credential。

Probe 实现不得拥有另一套：

```text
API Key 配置机制。
```

---

# 162. Secret Store Error

平台错误应映射：

```text
SECRET_STORE_UNAVAILABLE

SECRET_NOT_FOUND

SECRET_ACCESS_DENIED

SECRET_WRITE_FAILED

SECRET_DELETE_FAILED

SECRET_BACKEND_ERROR
```

---

# 163. Helper Error

统一：

```text
HELPER_NOT_FOUND

HELPER_NOT_EXECUTABLE

HELPER_INVALID_PATH

HELPER_TIMEOUT

HELPER_CREDENTIAL_NOT_FOUND

HELPER_SECRET_STORE_UNAVAILABLE

HELPER_OUTPUT_INVALID
```

---

# 164. Credential Configuration Error

例如：

```text
AUTH_STRATEGY_CONFLICT

PLAINTEXT_SECRET_FORBIDDEN

SECRET_HEADER_UNSUPPORTED

ENVIRONMENT_VARIABLE_MISSING
```

---

# 165. Diagnostics

Security Diagnostics 至少检查：

```text
Secret Store 是否可用

Credential Reference 是否有对应 Secret

cas-helper 是否存在

helper 是否可执行

auth.command 是否使用预期 helper

auth args 是否只包含 Credential ID

是否出现冲突认证配置

CAS-managed Provider 是否存在明文 Token

远程 HTTP Endpoint 是否不安全
```

---

# 166. Diagnostics 不读取 Secret 明文进行展示

例如检查：

```text
Credential exists
```

只需要：

```text
exists()
```

不需要：

```text
读取后显示。
```

---

# 167. Diagnostics 可以做认证测试

用户主动执行：

```text
Provider Connectivity Test
```

时才读取 Secret 并访问 Provider。

普通：

```text
Security Check
```

不必自动发模型请求。

---

# 168. Plaintext Secret Detection

CAS 对自己管理的 Provider 配置发现：

```text
experimental_bearer_token
```

或 Secret-looking static auth header 时：

返回：

```text
PLAINTEXT_SECRET_DETECTED
```

CAS 不应继续生成同样配置。

---

# 169. External Plaintext Secret

External Provider 存在明文 Secret：

CAS 可以：

```text
Warning
```

但不能：

```text
自动删除
自动迁移
```

因为资源不归 CAS 所有。

---

# 170. Provider Preset 安全约束

Preset V0.1 必须满足：

```text
纯声明式

不能执行命令

不能指定 arbitrary helper

不能读取文件

不能定义 shell script

不能包含 Secret

不能包含用户 Credential
```

---

# 171. Remote Preset

V0.1 Preset 随安装包发布。

如果未来允许远程 Preset Registry：

必须增加：

```text
来源认证

签名

版本验证

Trust Level

安全审查
```

不能直接下载 JSON 后无条件信任。

---

# 172. Custom Provider 风险

用户自己创建：

```text
Custom Provider
```

属于显式用户行为。

CAS 仍必须：

```text
显示目标 Host

验证 Scheme

检查 Credential Strategy
```

但不需要假装知道该 Provider 是否可信。

---

# 173. Provider Endpoint 展示

Credential 首次发送前，应使用户能够明确知道：

```text
Secret 将发送到哪个 Provider Host。
```

例如：

```text
api.deepseek.com
```

具体 UI 由 UI 文档负责。

---

# 174. Preset Endpoint 被修改

用户将内置：

```text
DeepSeek
```

Base URL 改为自定义地址后：

应从安全语义上视为：

```text
Custom Endpoint
```

而不是继续默认假设：

```text
这是官方 DeepSeek Endpoint。
```

---

# 175. Authentication Header

Bearer Token 请求统一：

```text
Authorization: Bearer <secret>
```

Header 构造完成后：

```text
不得被缓存到普通 Domain Object。
```

---

# 176. HTTP Client 生命周期

V0.1 可以复用 HTTP Client：

```text
Connection Pool
```

但 Authorization Header 应：

```text
per request
```

构造。

不要创建：

```text
一个永久携带某用户 Token 的全局 Client
```

然后被其他 Provider 错误复用。

---

# 177. Cross-provider Isolation

Provider A 的 Secret：

```text
只能用于 Provider A Request Context。
```

禁止：

```text
Generic Global Authorization Header
```

在不同 Provider Client 之间共享。

---

# 178. Request Context

网络层建议构造：

```text
ProviderRequestContext
├── provider id
├── endpoint
├── credential handle
└── operation
```

然后在最接近 HTTP Request 的地方解析 Secret。

---

# 179. Credential Scope

V0.1 Credential Scope：

```text
Provider-level
```

不是：

```text
Model-level

Agent-level
```

Agent 不应该知道 API Key。

---

# 180. Agent Config

CAS-generated：

```text
agents/cas-executor.toml
```

只能包含：

```text
model
model_provider
helper-based authentication reference indirectly
```

不得包含 Secret。

---

# 181. Model Catalog

Model Catalog：

```text
绝不包含 Credential。
```

Catalog 只描述：

```text
模型能力和 Metadata。
```

---

# 182. Secret Store Availability

CAS 启动不要求：

```text
所有 Credential 都立即读取。
```

只检查：

```text
Secret Store backend 基本可用性
```

即可。

---

# 183. Lazy Secret Access

Credential 应在：

```text
真正需要
```

时读取。

例如：

```text
Provider Test

Probe

cas-helper call
```

避免：

```text
CAS 启动
→ 一次把所有 API Key 加载到内存。
```

---

# 184. No Global Secret Cache

禁止：

```text
HashMap<CredentialId, SecretString>
```

长期驻留整个 Desktop 生命周期。

---

# 185. Temporary Secret Cache

V0.1 原则上不需要应用级 Secret Cache。

由：

```text
OS Secret Store
```

作为长期存储。

---

# 186. Secret Store Lock

如果 macOS/Linux Secret Store 当前：

```text
locked
```

CAS 不应该：

```text
反复后台弹解锁请求。
```

只在用户触发需要 Credential 的操作时访问。

---

# 187. Background Activity

CAS 不进行：

```text
后台高频 Provider Polling
```

从而避免不断读取 Secret。

---

# 188. Auto Diagnostics

自动 Diagnostics 默认只检查：

```text
引用、文件、配置结构
```

需要 Secret 的网络诊断由用户显式触发或低频执行。

---

# 189. Application Update

升级 CAS 时：

```text
不得要求导出/重新保存 Secret
```

Credential ID 和 Secret Store Namespace 必须保持兼容。

---

# 190. Credential Schema Migration

如果未来 Secret Locator 格式变化：

采用：

```text
Copy
Verify
Switch Reference
Delete Old
```

迁移。

不能：

```text
先删除旧 Secret
再创建新 Secret。
```

---

# 191. Helper Upgrade

升级 helper：

```text
路径尽量保持稳定。
```

否则所有：

```text
auth.command
```

可能需要重新 Apply。

---

# 192. Stable Helper Location

推荐安装结构提供稳定路径：

```text
<app-install>/bin/cas-helper
```

而不是：

```text
<app-install>/versions/0.1.3/random/cas-helper
```

直接写入 Codex 配置。

如果安装框架使用版本目录，则需要稳定 launcher。

---

# 193. Portable Mode

V0.1 如果没有完整安全设计：

```text
不提供 Portable Mode Secret File。
```

Portable App 仍应使用：

```text
OS Secret Store。
```

---

# 194. Uninstall

普通卸载 CAS：

不应自动假设用户想：

```text
删除全部 Provider API Key。
```

但如果 helper 被删除：

```text
Codex CAS-managed Provider
```

将无法再获取 Credential。

因此卸载流程必须与配置清理策略协调。

具体产品流程属于发布规范。

---

# 195. Secret Cleanup Tool

可以提供明确：

```text
Remove CAS Credentials
```

维护操作。

但必须：

```text
用户显式触发。
```

---

# 196. Full Reset

Reset CAS 时涉及：

```text
Database

Managed Codex Config

Secret Store

Backup
```

这是高风险跨系统操作。

安全模块只负责：

```text
Secret 的可控删除。
```

不自行决定 Reset 范围。

---

# 197. Security Audit Points

代码审查时重点搜索：

```text
api_key

token

authorization

secret

password

bearer

credential

http_headers

env_key
```

检查是否存在：

```text
日志
Debug
持久化
IPC
CLI 参数
```

泄漏。

---

# 198. Static Analysis

建议项目引入：

```text
Secret-aware lint / review conventions
```

例如禁止：

```text
SecretValue 实现 Debug

Credential Command 接收 String Secret 参数

Config DTO 存 secret_value
```

---

# 199. Test Secret

测试环境不得使用：

```text
真实生产 API Key
```

普通单元测试统一使用：

```text
synthetic secret
```

例如：

```text
test-secret-not-real
```

---

# 200. Integration Test Secret

需要真实 Provider 的 Integration Test：

Credential 必须来自：

```text
CI Secret Store
```

不得：

```text
提交到 Git
写进 Fixture
写进 Snapshot
```

---

# 201. Test Log

Integration Test Failure：

不能打印：

```text
Environment Secret
Authorization
```

---

# 202. Fixture

HTTP Fixtures 中不得保存：

```text
真实 Authorization Header。
```

必须替换：

```text
Bearer TEST_TOKEN
```

或者完全移除。

---

# 203. Git Secret 防护

仓库应配置：

```text
secret scanning
```

或者至少使用：

```text
pre-commit / CI Secret Detection
```

作为供应链安全辅助。

---

# 204. `.env`

项目示例文件：

```text
.env.example
```

只能包含：

```text
变量名
```

例如：

```text
DEEPSEEK_API_KEY=
```

不得提交真实 `.env`。

---

# 205. Debug Build

开发环境允许更多日志。

但：

```text
Secret Redaction
```

在 Debug Build 同样必须开启。

不能：

```text
只有 Release 才保护 Secret。
```

---

# 206. Security Event

安全相关事件建议记录：

```text
CredentialCreated

CredentialReplaced

CredentialDeleted

CredentialMissing

HelperInvalid

PlaintextSecretDetected

AuthStrategyChanged
```

只记录：

```text
事件和 Credential ID。
```

---

# 207. 不记录 Secret Access 每次调用

`cas-helper` 每次为 Codex 读取 API Key：

不建议写入大量持久化 Audit Log。

否则：

```text
每个模型请求相关 Credential 读取
```

可能产生噪声和隐私问题。

V0.1 不做完整安全审计平台。

---

# 208. Rate Limiting Helper

Helper 是本地程序。

V0.1 不需要复杂：

```text
Token Retrieval Rate Limit
```

因为 Codex 合法运行可能多次调用。

安全边界依靠：

```text
OS User Permission
Credential Namespace
固定接口
```

---

# 209. Helper Network

`cas-helper` 本身：

```text
不访问 Provider
不访问 Internet
```

它只：

```text
读取 Secret
输出 Secret。
```

降低攻击面。

---

# 210. Helper Filesystem

Helper 不需要：

```text
扫描 Codex 项目
读取源码
读取 config.toml
```

如果可以通过：

```text
Credential ID → deterministic Secret Locator
```

完成，则也无需读取 CAS Database。

---

# 211. Helper 无数据库依赖

推荐最终设计：

```text
Credential ID
        ↓
Fixed Namespace Mapping
        ↓
OS Secret Store
```

这样 helper 不需要：

```text
打开 cas.db
```

可以显著减少依赖。

---

# 212. Deterministic Locator

例如：

```text
credential_id:
0198...

Windows target:
CodexAgentSwitch:credential:0198...

macOS:
service = CodexAgentSwitch
account = credential:0198...

Linux:
application = codex-agent-switch
credential_id = 0198...
```

---

# 213. Helper Injection Resistance

因为：

```text
Credential ID
```

严格解析 UUID，

且 locator 由程序内部生成，

所以用户无法让 helper：

```text
读取任意 Secret Store Item。
```

---

# 214. Provider API Key 不进入 Child Agent Prompt

Secret 仅由：

```text
Codex HTTP Provider Layer
```

使用。

不应成为：

```text
System Prompt

Developer Instruction

Tool Parameter

Agent Context
```

---

# 215. Prompt Injection

由于模型本身永远看不到：

```text
API Key
```

项目代码中的 Prompt Injection 不应该能够要求模型：

```text
“把 Provider API Key 打印出来”
```

并得到 Secret。

CAS 必须保持：

```text
Credential 不进入模型上下文。
```

---

# 216. Tool Access

Custom Agent 的：

```text
shell
filesystem
```

权限不应该让它自动知道 CAS Secret Store Locator。

但同一用户环境下，具备任意本机代码执行能力的 Agent 理论上可能尝试访问用户凭据。

这属于重要系统边界。

---

# 217. Agent 与本机 Secret

如果 Codex Agent 获得：

```text
danger-full-access
```

它可能以当前用户权限执行程序。

CAS 不能保证这样的 Agent：

```text
永远无法调用 cas-helper。
```

因此 helper 本身不能被设计为：

```text
“只有 Codex 才能调用”
```

这种虚假安全假设。

---

# 218. Helper Capability 现实边界

`cas-helper token <credential-id>` 本质上允许：

```text
当前用户环境中知道 Credential ID 的进程
```

尝试读取该 Credential。

因此真正的安全隔离来自：

```text
OS User Account
OS Secret Store
Codex Sandbox
```

而不是 helper 名称。

---

# 219. Credential ID 保密性

Credential ID：

```text
不是 Secret
```

不能依靠：

```text
隐藏 UUID
```

作为访问控制机制。

UUID 仅用于避免冲突和限定 Locator。

---

# 220. 更强 Access Control

未来如需更高安全级别，可以研究：

```text
OS-specific Keychain ACL

Signed caller verification

broker daemon

user-presence authorization

per-process capabilities
```

但不属于 V0.1。

---

# 221. V0.1 安全定位

V0.1 目标：

```text
显著优于：

API Key 明文写 config.toml
API Key 放 CLI 参数
API Key 写 SQLite
API Key 记录日志
```

而不是构建：

```text
硬件级 Secret Isolation System。
```

---

# 222. Security Defaults

V0.1 默认：

```text
OS Secret Store

Absolute cas-helper path

Bearer auth.command

HTTPS remote Provider

TLS verification ON

No cross-origin auth redirects

No plaintext credential config

No secret export

No secret display

No arbitrary credential command

No remote scripts
```

---

# 223. Advanced Insecure Options

如果未来允许：

```text
HTTP Remote Provider

Custom Certificate Bypass

Plaintext Credential

Arbitrary Auth Command
```

这些不得成为：

```text
普通设置。
```

其中：

```text
Plaintext Credential
```

建议始终不提供。

---

# 224. 安全失败原则

当安全前提无法满足时：

```text
Fail Closed
```

例如：

```text
Secret Store unavailable

Helper missing

Helper path invalid

Credential missing

Auth strategy conflict

TLS invalid
```

不得为了：

```text
“让它先跑起来”
```

自动转为不安全方案。

---

# 225. Credential Apply 前检查

CAS Provider 准备 Apply 时至少验证：

```text
Credential Strategy 合法

Credential Reference 存在

Secret Store Backend 可用

Secret 存在

helper 路径合法

auth.command 不冲突

不包含明文 Secret
```

---

# 226. Apply 不验证 Secret 内容

普通 Apply 不需要：

```text
读取 Secret 明文
```

只需要：

```text
exists()
```

即可。

真正认证正确性通过：

```text
Provider Test
```

确认。

---

# 227. 为什么 Apply 不读取 Secret

减少：

```text
不必要 Secret Access
```

并将：

```text
Configuration Validity
```

与：

```text
Provider Connectivity
```

分离。

---

# 228. Provider Credential Status

安全层可以提供：

```text
CONFIGURED

MISSING

STORE_UNAVAILABLE

INVALID_REFERENCE
```

不提供：

```text
VALUE
```

---

# 229. 环境变量缺失

如果 Provider 使用：

```text
EXTERNAL_ENV
```

CAS 可以检查：

```text
变量是否存在
```

但普通 UI 不显示变量值。

---

# 230. Process Environment 日志

禁止：

```text
dump all environment variables
```

到 Diagnostics。

因为其中可能包含：

```text
API Keys
Cloud Credentials
GitHub Tokens
```

---

# 231. PATH

Diagnostics 可以输出：

```text
helper resolved absolute path
```

但不需要打印整个：

```text
PATH
```

除非经过敏感信息审查。

---

# 232. Backup File Permission

Configuration Snapshot：

虽然不应包含 CAS Secret，

仍可能包含用户自己的：

```text
External Codex Secret
```

因为用户原本 `config.toml` 可能存在明文 token。

因此 Backup Directory 同样必须视为：

```text
Sensitive Private Data。
```

---

# 233. Snapshot Encryption

V0.1 不强制自研加密 Snapshot。

依赖：

```text
OS Filesystem Permission
```

但必须限制文件权限。

如果未来需要加密 Backup，应单独设计密钥管理。

---

# 234. Snapshot Export

普通用户不应误把：

```text
Raw Codex Snapshot
```

当作安全可分享文件。

UI / 文档需要明确：

```text
Snapshot may contain external user configuration.
```

---

# 235. CAS-managed Secret Guarantee

CAS 可以保证：

```text
自己创建的 Provider
```

不会把 Secret 写入 Snapshot 源配置。

但不能保证：

```text
用户已有 External Codex 配置
```

不存在 Secret。

---

# 236. Security-sensitive Configuration

以下变化必须归类为高敏感变更：

```text
Credential Strategy Change

Credential Replace

Provider Origin Change

Helper Path Change

Authentication Type Change

Remote HTTPS → HTTP

Secret Header Configuration
```

---

# 237. Ordinary Configuration

以下通常不是安全敏感变更：

```text
Provider Display Name

Model Display Name

Agent Description
```

---

# 238. Security Boundary Testing

V0.1 必须有专门测试验证：

```text
Secret 不进入 SQLite

Secret 不进入 Codex config

Secret 不进入 Agent config

Secret 不进入 Backup metadata

Secret 不进入 logs

Secret 不进入 exported config
```

---

# 239. Canary Secret Test

测试可以使用明显 Token：

```text
CAS_TEST_SECRET_71a98...
```

执行完整流程后，在：

```text
database

files

logs

exports

snapshots
```

中搜索该字符串。

预期：

```text
除 Secret Store 和允许的临时测试内存外
零命中。
```

---

# 240. Helper Test

验证：

```text
stdout 只有 Secret

stderr 无 Secret

invalid UUID 不读取任何 item

不存在 Credential 返回非零

helper 不使用网络
```

---

# 241. PATH Hijack Test

测试环境：

```text
PATH 前置 fake cas-helper
```

运行 Codex 配置生成。

预期：

```text
配置仍使用 CAS 安装目录中的绝对 helper 路径。
```

---

# 242. Config Secret Test

故意向 CAS 输入：

```text
CAS_TEST_SECRET
```

Apply 后检查：

```text
config.toml

agents/*.toml

catalog
```

不得存在该字符串。

---

# 243. Logging Test

开启：

```text
Debug logging
```

执行：

```text
Credential Store

Provider Test

Apply

Credential Replace

Delete
```

最终所有日志：

```text
不得出现 Canary Secret。
```

---

# 244. Frontend Persistence Test

输入 Secret 后：

检查：

```text
localStorage

sessionStorage

IndexedDB

application saved state
```

不得存在。

---

# 245. Export Test

导出：

```text
CAS Configuration
```

不得出现 Secret。

---

# 246. Snapshot Test

对 CAS-managed Provider Apply：

Snapshot 不应因 CAS 自身设计包含 Secret。

如果用户 External Config 原本存在 Secret：

测试应确认：

```text
Snapshot 权限严格
且 UI 不将其作为可公开导出配置。
```

---

# 247. Redirect Test

Mock Provider：

```text
HTTPS Provider A
→ 302 Provider B
```

带认证请求。

预期：

```text
Authorization 不发送给 Provider B。
```

---

# 248. TLS Test

错误证书：

```text
Connection Test
```

必须失败。

不得自动忽略。

---

# 249. HTTP Test

远程：

```text
http://example.com
```

默认拒绝。

本地：

```text
http://127.0.0.1
```

可以按照本地 Provider 策略允许。

---

# 250. Security Review Checklist

每次涉及以下模块变化必须进行安全 Review：

```text
Secret Storage

Credential DTO

cas-helper

Provider HTTP Client

Tauri Command

Config Writer Auth

Export

Backup

Logging

Diagnostics

Remote Preset
```

---

# 251. 安全不变量

## 不变量一

```text
Secret 明文不得进入 SQLite。
```

## 不变量二

```text
CAS-managed Secret 明文不得进入 Codex TOML。
```

## 不变量三

```text
Secret 不得作为命令行参数。
```

## 不变量四

```text
Secret 不得进入普通日志。
```

## 不变量五

```text
Secret 不得进入普通配置导出。
```

## 不变量六

```text
Credential Helper 参数只能包含非敏感引用。
```

## 不变量七

```text
Codex auth.command 必须使用可信 helper 的绝对路径。
```

## 不变量八

```text
Provider Preset 不得定义任意可执行 Credential Command。
```

## 不变量九

```text
CAS 不读取或修改 Codex 官方 auth.json。
```

## 不变量十

```text
Secret Store 不可用时不得静默降级为明文存储。
```

## 不变量十一

```text
React 前端不得读取已保存 Secret。
```

## 不变量十二

```text
Configuration Compiler 和 Writer 永远不需要 Secret Value。
```

## 不变量十三

```text
External Secret 必须 Preserve，但不得展示或记录。
```

## 不变量十四

```text
跨 Origin Redirect 不得携带 Provider Authorization。
```

## 不变量十五

```text
远程 Provider 默认必须使用有效 TLS。
```

## 不变量十六

```text
Credential ID 不是安全访问控制机制。
```

## 不变量十七

```text
CAS 不声称能够抵御已经完全控制当前用户账户的攻击者。
```

## 不变量十八

```text
所有安全失败默认 Fail Closed。
```

---

# 252. V0.1 最终 Credential Flow

用户配置 DeepSeek V4 Flash（Custom Responses Provider 使用同一流程）：

```text
User
  │
  │ API Key
  ▼
React Credential Input
  │
  │ transient IPC
  ▼
Rust Credential Command
  │
  ▼
SecretService
  │
  ▼
OS Secret Store
  │
  └── credential:<UUID> = SECRET


SQLite
  │
  └── CredentialReference
        id = UUID


Codex config.toml
  │
  └── auth.command
       │
       ├── absolute cas-helper path
       └── args:
            token
            UUID


Codex Runtime
  │
  ▼
cas-helper
  │
  ▼
OS Secret Store
  │
  ▼
Secret
  │
  │ stdout pipe
  ▼
Codex
  │
  ▼
Authorization: Bearer ***
  │
  ▼
Provider
```

V0.1 的 Provider 实例在此为 DeepSeek，但 SecretService / helper 协议和存储 key 均不得包含品牌分支。

整个长期持久化链中：

```text
Secret
```

只存在于：

```text
OS Secret Store
```

---

# 253. 最终安全边界

CAS 的安全设计最终保持以下分工：

```text
OS Secret Store
负责：
Secret at rest


SecretService
负责：
Credential 生命周期


cas-helper
负责：
Secret → Codex 的最小化交付


Codex
负责：
获得 Token 后的模型请求生命周期


Provider
负责：
服务端 Credential 验证


SQLite
只保存：
Credential Reference


Config Writer
只保存：
Credential Reference + Helper Command
```

---

# 254. 最终设计结论

CAS V0.1 不应该为了“开箱即用”而采用：

```text
API Key 写 TOML

API Key 写 SQLite

API Key 写 .env

API Key 写 CLI 参数

API Key 写 Provider Preset
```

这些快捷方案。

默认安全链必须保持：

```text
User Secret
     ↓
OS Secret Store
     ↓
Credential ID
     ↓
Absolute cas-helper
     ↓
Codex auth.command
     ↓
Provider
```

其中：

```text
Agent
Model
Configuration Compiler
SQLite Repository
```

都不需要知道实际 Secret。

CAS 的目标不是保证本地设备被完全攻陷后 Secret 仍绝对不可读取，而是在正常桌面安全边界下系统性避免最常见、最现实的 Credential 泄漏路径：

```text
配置文件泄漏
日志泄漏
Shell 历史泄漏
进程参数泄漏
Backup 泄漏
UI 持久化泄漏
PATH 劫持
错误 Redirect
不安全 HTTP
远程脚本
Preset 代码执行
```

只要某项功能需要通过扩大 Secret 暴露范围才能实现，默认设计应选择：

```text
不实现
或
要求显式安全方案
```

而不是静默降低安全等级。

这应作为 CAS 整个 Credential 系统长期保持的最高优先级约束。
